### Verdict: REVISE

### Summary

The SP-108 implementation is sound: `computeOutputHeadroom` enforces the output floor, `resolveDelegationOptions` sets explicit `maxTokens`, and `routeAndDelegate` blocks undersized dispatch with SP-095 overflow fallback plus zero-output `length` retry. Build, lint, tests, and coverage gates all pass, and `router-pipeline.ts` was not modified.

However, PROMPT Step 4 and contract completion criteria require an **integration** test that mocks a provider `length` stop with 0 output tokens and asserts retry or structured error. That scenario is covered only in `tests/unit/smart-router-extension.test.ts`; `tests/integration/pi-extension.test.ts` adds only the pre-dispatch headroom escalation case.

### Build / typecheck

Commands run (all passed):

- `npm run typecheck`
- `npm run lint`
- `npm test` — 58 files, 1025 tests passed
- `npm run coverage:check` — global line coverage 93.95%; `src/domain/delegation/output-headroom.ts` 84.61% lines (≥77%)

### Blocking issues

1. **`tests/integration/pi-extension.test.ts:711-779`** — The `delegation output headroom (SP-108)` describe block has only `escalates before provider dispatch when input leaves no output headroom`; PROMPT Step 4 and contract `completionCriteria` require an integration test that mocks provider `stopReason: "length"` with 0 output and expects escalation to a larger model or a structured overflow error.
   - **Missing test:** `tests/integration/pi-extension.test.ts` — add a case mirroring `tests/unit/smart-router-extension.test.ts:1821` (`retries larger model after zero-output length stop from provider`) through the extension `streamSimple` path, asserting two delegate calls and successful completion (or structured error when no fallback exists).

```json
{"verdict":"REVISE","feedback":"Implementation and unit coverage are solid and gates pass, but the integration test suite is missing the required 0-output length-stop retry/error scenario from PROMPT Step 4 completion criteria. Add it to tests/integration/pi-extension.test.ts."}
```

### Suggestions (non-blocking)

- Add a test for `emitContextOverflowNoFit` when no larger-fit model exists (structured error on the outer stream).
- `output-headroom.ts` env-var resolution paths (lines 37-48, 105-107) remain uncovered; optional unit cases if env override behavior matters in production.
