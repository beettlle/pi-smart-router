### Verdict: APPROVE

### Summary

Step 4 delivers the required integration coverage for SP-108: pre-dispatch headroom escalation and zero-output `length` stop retry are exercised in both unit (`tests/unit/smart-router-extension.test.ts`) and integration (`tests/integration/pi-extension.test.ts`) suites. The implementation adds a pure `computeOutputHeadroom` helper, pre-dispatch guards in `route-and-delegate.ts`, and explicit `maxTokens` in `resolveDelegationOptions`, all within file scope and without touching `router-pipeline.ts`.

Build, lint, test, and coverage gates all pass (`npm run verify:ci` exit 0; `output-headroom.ts` at 84.6% line coverage).

### Build / typecheck

| Command | Result |
|---------|--------|
| `npm run typecheck` | Pass |
| `npm run lint` | Pass |
| `npm test` | Pass (1026 tests) |
| `npm run coverage:check` | Pass |
| `npm run verify:ci` | Pass |

### Scope and contract

- **Must change:** `delegation-runtime.ts`, `route-and-delegate.ts` — updated with headroom guard, `maxTokens`, and fallback escalation.
- **May change:** `output-headroom.ts`, unit/integration tests — added with solid coverage of no-fit, healthy margin, and escalation paths.
- **Must NOT change:** `router-pipeline.ts` — confirmed zero diff vs `main`.
- **Completion criteria:** Pre-dispatch no-dispatch on insufficient headroom, explicit `maxTokens`, and 0-output length failure retry are all implemented and tested.

### Suggestions (non-blocking)

1. **`route-and-delegate.ts:328-371`** — When `targetProfile` is absent, the pre-dispatch headroom guard and `maxTokens` injection are skipped; consider falling back to registry model limits for models not in the fleet profile map.
2. **Missing test:** `tests/integration/pi-extension.test.ts` — no case for `emitContextOverflowNoFit` when no larger-fit model exists; contract allows "fallback or structured error," but adding a no-fit-only fleet test would harden the error path.

```json
{"verdict":"APPROVE","feedback":"Step 4 integration tests cover pre-dispatch headroom escalation and zero-output length-stop retry; verify:ci and coverage gates pass; changes stay within file scope."}
```
