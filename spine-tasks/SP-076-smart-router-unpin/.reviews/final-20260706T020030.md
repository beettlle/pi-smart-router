### Verdict: PASS

### Summary

SP-076 is complete: `/smart-router unpin` is wired through parsing, completions, and the command handler in `commands.ts` / `command-formatters.ts`, calling `sessionPinner.breakPin(sessionId)` for the current session only (in-memory and persisted store). README and completion tests are updated; three extension unit tests cover clear, no-op, and cross-session isolation. Machine contract checks passed, forbidden paths are untouched, and `npm run typecheck && npm test` succeeded (810/810).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run typecheck && npm test`) | PASS — typecheck exit 0; 45 files, 810 tests passed |
| fileScopeMustChange (`.pi/extensions/smart-router/index.ts`) | PASS — pre-landed on main per machine verifier; handler registered via `registerSmartRouterCommand` in `commands.ts` imported by `index.ts` |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS — no diff vs main |
| completionCriteria | PASS — unpin clears current session pin via `breakPin`; completions and README updated; tests verify session isolation |

### Build / typecheck

Commands run: `npm run typecheck && npm test`

- Typecheck: exit 0 (`tsc --noEmit`)
- Tests: 810 passed, including `smart-router unpin command (SP-076)` (3 cases) and parse/completion updates in `smart-router-pricing.test.ts`

### Scope and implementation notes

- In-scope changes: `.pi/extensions/smart-router/{command-formatters,commands,types}.ts`, `tests/unit/smart-router-extension.test.ts`, `tests/unit/smart-router-pricing.test.ts`, `README.md`, task `STATUS.md`.
- Handler no-ops with info when no pin exists; errors when `sessionPinner` unavailable.
- Does not add `unpin all` or modify `SessionPinner` break rules (`src/domain/pinning/session-pinner.ts` unchanged).

```json
{"verdict":"PASS","feedback":"Unpin subcommand, tests, and docs satisfy SP-076 completion criteria and contract checks. Typecheck and full test suite pass."}
```
