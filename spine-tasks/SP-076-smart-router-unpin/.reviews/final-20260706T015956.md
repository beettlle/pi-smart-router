### Verdict: PASS

### Summary

SP-076 delivers `/smart-router unpin` end-to-end: parsing and completions in `command-formatters.ts` / `commands.ts`, handler wiring via `registerSmartRouterCommand` calling `sessionPinner.breakPin(sessionId)` for the current session only, README operator table update, and three focused extension unit tests (clear pin, no-op, other sessions untouched). Contract checks passed (machine verifier), `src/domain/pipeline/router-pipeline.ts` is unchanged, and `npm run typecheck && npm test` completed successfully (810 tests).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run typecheck && npm test`) | PASS — typecheck clean; 810/810 tests passed |
| fileScopeMustChange (`.pi/extensions/smart-router/index.ts`) | PASS — pre-landed on main per machine verifier; unpin handler correctly lives in `commands.ts` imported by `index.ts` |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS — no diff vs main |
| completionCriteria | PASS — unpin clears current session pin via `breakPin` (in-memory + store delete); completions/README updated; tests assert session isolation |

### Build / typecheck

Commands run: `npm run typecheck && npm test`

- Typecheck: exit 0
- Tests: 45 files, 810 tests passed (includes `smart-router unpin command (SP-076)` suite and updated parse/completion cases in `smart-router-pricing.test.ts`)

### Scope and implementation notes

- Changes confined to in-scope paths: `.pi/extensions/smart-router/{command-formatters,commands,types}.ts`, `tests/unit/smart-router-extension.test.ts`, `tests/unit/smart-router-pricing.test.ts`, `README.md`, task `STATUS.md`.
- Handler correctly no-ops when no pin or when `sessionPinner` is unavailable; does not add `unpin all`; does not alter `SessionPinner` break rules.

```json
{"verdict":"PASS","feedback":"Unpin subcommand, tests, and docs satisfy SP-076 completion criteria and contract checks. Typecheck and full test suite pass."}
```
