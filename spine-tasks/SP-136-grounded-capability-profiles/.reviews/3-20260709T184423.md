### Verdict: APPROVE

### Summary

Step 3 delivers the required unit and integration tests for benchmark-grounded capability profiles, with clear assertions that grounded scores differ from the prior hardcoded 0.95 frontier defaults and that the shortfall gate rejects economically grounded models below requirement thresholds. Build, lint, typecheck, and full test suite pass (1256 tests), and `pi-model-mapper.ts` line coverage is 96.09%, well above the 77% gate. Changes stay within task intent: `router-pipeline.ts` is untouched, mapper integration is complete, and fallback behavior is documented in `config/models.yaml.example`.

### Build / typecheck

Commands run: `npm run typecheck && npm run lint && npm test` and `npm run coverage:check`.

- Typecheck: pass
- Lint: pass (`models.yaml.example` validation included)
- Tests: 68 files, 1256 passed
- Coverage: overall 92.91% lines; `src/config/pi-model-mapper.ts` 96.09% lines

### Scope and diff

Reviewed `main...HEAD` (3 SP-136 commits). Changed files:

- `src/config/pi-model-mapper.ts` — benchmark artifact loader, `withBenchmarkCapabilities` overlay, test hooks
- `config/benchmark-profiles.json` — checked-in ingest artifact (necessary runtime dependency)
- `config/models.yaml.example` — fallback documentation
- `tests/unit/pi-model-mapper.test.ts` — unit cases for grounded, missing-row, disabled, and missing-file fallbacks
- `tests/integration/pi-extension.test.ts` — integration proving shortfall gate uses grounded haiku vs sonnet selection
- `spine-tasks/SP-136-grounded-capability-profiles/STATUS.md`

`src/domain/pipeline/router-pipeline.ts` has zero diff (contract `fileScopeMustNotChange` satisfied).

### Test coverage assessment

| Requirement | Evidence |
|-------------|----------|
| Known model gets benchmark scores | Unit test `uses ingest artifact scores for known benchmark model ids`; integration asserts haiku reasoning ≈ 0.5685, not 0.95 |
| Unknown/missing falls back to regex | Tests for `claude-3.5-sonnet` (no row), disabled path (`null`), and missing file |
| Shortfall uses grounded profile | Integration test rejects haiku with `shortfall_gate`, selects sonnet |
| Behavior change vs 0.95 | Multiple `not.toBe(0.95)` / `toBeCloseTo` assertions on benchmark values |

### Suggestions (non-blocking)

1. **`tests/unit/pi-model-mapper.test.ts`** — Consider adding a case with a malformed `benchmark-profiles.json` to exercise the `console.warn` fallback path (`src/config/pi-model-mapper.ts:84-85,94-100`), currently uncovered.

```json
{"verdict":"APPROVE","feedback":"Step 3 tests, build gates, and coverage meet Review Level 2 requirements. Benchmark grounding, regex fallback, and shortfall integration are verified; router-pipeline.ts unchanged."}
```
