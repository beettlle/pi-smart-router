### Verdict: PASS

### Summary

SP-136 delivers benchmark-grounded capability profiles in `mapPiModelToProfile` by loading `config/benchmark-profiles.json` at mapper init, overlaying ingest scores when a model id matches, and preserving regex/pattern defaults for missing rows or absent artifacts. Unit and integration tests demonstrate the behavior change versus hardcoded 0.95 frontier defaults and confirm the shortfall gate consumes grounded economical capabilities without altering `router-pipeline.ts`. Machine contract verification passed, and independent `npm run verify:ci` succeeded with `pi-model-mapper.ts` line coverage at 96.09%.

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run verify:ci`) | PASS — build, typecheck, lint, 1256 tests, coverage gate |
| fileScopeMustChange (`src/config/pi-model-mapper.ts`) | PASS — benchmark loader, cache, `withBenchmarkCapabilities` integration |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS — zero diff vs main |
| completionCriteria | PASS — grounded scores, regex fallback, tests vs 0.95, fallback documented in module header and `models.yaml.example` |

### Build / typecheck

Command: `npm run verify:ci`

Result: exit 0 — `tsc --project tsconfig.build.json`, `tsc --noEmit`, eslint, vitest (68 files / 1256 tests), `coverage:check` all passed.

### Coverage

`src/config/pi-model-mapper.ts`: 96.09% lines (threshold ≥77%).

### Completion criteria

- Benchmark-grounded profiles when artifact row exists — `loadBenchmarkCapabilitiesMap` + `withBenchmarkCapabilities` applied across local, pattern-matched, and unknown paths.
- Regex fallback for missing models — tests cover missing row, disabled path, and missing file.
- Tests prove change vs 0.95 — unit (`claude-opus-4-5` ≈0.77) and integration (haiku ≈0.5685, sonnet stays 0.95).
- Shortfall unchanged algorithmically — integration test exercises `HydraMatcher` shortfall_gate with mapped fleet; no pipeline edits.

```json
{"verdict":"PASS","feedback":"Benchmark profile loader and mapper integration are complete, scoped, and verified. Contract checks, verify:ci, and coverage gate pass; tests demonstrate grounded capabilities and shortfall behavior vs hardcoded defaults."}
```
