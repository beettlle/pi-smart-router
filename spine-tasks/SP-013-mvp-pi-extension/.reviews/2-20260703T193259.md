# Code Review — Step 2: Testing and verification

### Verdict: APPROVE

### Summary

Build and typecheck pass cleanly (`npm run typecheck && npm test` — 129 tests, 0 failures). The test files added in this step (`tests/unit/pi-router-middleware.test.ts` with 25 tests, `tests/unit/create-router.test.ts` with 7 tests) provide thorough coverage of the middleware integration and factory export. No changes to forbidden `src/domain/triage/` scope; all changed files are within the declared File Scope.

### Build / typecheck

```
$ npm run typecheck && npm test
> tsc --noEmit  ✓
> vitest run    ✓ 10 files, 129 tests passed (442ms)
```

### Coverage gate

The `coverage:check` script referenced in `.spine/spine-config.json` is not defined in `package.json` and `@vitest/coverage-v8` is not installed. This is a pre-existing infrastructure gap — not introduced by SP-013. Manual inspection of the test files confirms the middleware module's branches (register, context, compact, model_select, before_provider_request, routeSync fallback, deriveSessionId, extractPromptText, deriveTurnType) and the factory (`createRouter`) are all exercised.

### Scope verification

| Changed file | In File Scope? |
|---|---|
| `src/api/middleware/pi-router-middleware.ts` | ✓ |
| `src/index.ts` | ✓ |
| `tests/unit/pi-router-middleware.test.ts` | ✓ (test) |
| `tests/unit/create-router.test.ts` | ✓ (test) |
| `spine-tasks/SP-013-mvp-pi-extension/STATUS.md` | ✓ (contract) |

No changes to `src/domain/triage/**` (fileScopeMustNotChange).

### Suggestions (non-blocking)

1. **`src/index.ts:40`** — `catalog.models as unknown as ModelProfile[]` double-cast could be replaced with a runtime validation or a typed loader overload in a future task.
2. **`src/api/middleware/pi-router-middleware.ts:217-220`** — The synchronous `.then()` trick works because current pipeline stages are sync, but an explicit comment noting this should be revisited when async stages are introduced would aid future maintainers (comment already partially addresses this).

```json
{"verdict":"APPROVE","feedback":"Build/typecheck green, 129 tests pass, scope clean. Coverage tooling missing (pre-existing) but test depth is adequate for the changed paths."}
```
