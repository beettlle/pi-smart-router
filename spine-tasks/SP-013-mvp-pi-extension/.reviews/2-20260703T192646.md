# Code Review — SP-013 Step 2: Testing and verification

### Verdict: REVISE

### Summary

Build and typecheck pass (97 tests, 0 errors). The new `src/api/middleware/pi-router-middleware.ts` (234 lines) and the refactored `src/index.ts` factory are well-structured, correctly implement the pi-middleware.md v1.0.0 contract, and stay within File Scope. However, there are **zero tests** covering the new middleware or the `createRouter` factory — the coverage gate fails at 0% against the required ≥77% threshold on changed modules.

### Build / typecheck

```
npm run typecheck && npm test  →  exit 0
tsc --noEmit: clean
vitest: 97 passed (8 files), 0 failures
```

Coverage tooling (`npm run coverage:check`) is not wired — script missing and `@vitest/coverage-v8` not installed. Regardless, no test files reference `pi-router-middleware` or `createRouter`, so coverage on changed modules is 0%.

### Blocking issues

1. **`src/api/middleware/pi-router-middleware.ts` (entire file, 234 lines)** — No unit tests exist. This file contains significant logic: session ID derivation (lines 88–95), turn type classification (lines 115–137), message mapping (lines 107–112), compaction flag lifecycle (lines 83–84, 147–151, 160–165), `model_select` force override (lines 168–171), and the sync routing wrapper with fallback (lines 209–234). All of these paths are untested.
   - **Missing test:** `tests/unit/pi-router-middleware.test.ts` — needs cases for:
     - `register()` wires all four event hooks
     - `context` event updates internal message state
     - `session_compact` / `session_before_compact` sets compaction flag
     - `model_select` with `source === 'set'` sets `forceModelId`; other sources ignored
     - `before_provider_request` builds a valid `RoutingRequest` and returns modified event
     - `before_provider_request` falls back to unmodified event on pipeline error
     - `routeSync` falls back to `safeCloudDefault` when pipeline doesn't resolve synchronously
     - `deriveSessionId` uses session file when available, hash fallback otherwise
     - `deriveTurnType` returns correct types for tool messages, planning keywords, empty messages, and default
     - `extractPromptText` extracts last user message, returns empty string when none found
     - `getLastDecision` returns the most recent routing decision

2. **`src/index.ts:35-51`** — `createRouter` factory function has no tests. Should verify it returns a valid `RouterHandle` with all expected properties and that `register` delegates to middleware.
   - **Missing test:** `tests/unit/create-router.test.ts` (or add to `tests/unit/package.test.ts`) — needs cases for:
     - Default options produce a valid `RouterHandle`
     - Custom `modelsPath` is forwarded to `loadModels`
     - Returned `register` matches `middleware.register`

### Suggestions (non-blocking)

- `src/index.ts:40` — `catalog.models as unknown as ModelProfile[]` is a double type assertion that bypasses safety. Consider aligning the return type of `loadModels` or adding a runtime validation step.
- `src/api/middleware/pi-router-middleware.ts:10-11` — Two separate imports from `node:crypto` can be consolidated into one import statement.
- The `routeSync` pattern (lines 209–234) relying on same-tick microtask resolution is fragile if any pipeline stage becomes truly async in the future. Consider documenting this constraint more prominently or adding a runtime warning when the fallback is hit unexpectedly.
- Wire `npm run coverage:check` script and install `@vitest/coverage-v8` to enable automated coverage enforcement.

```json
{"verdict":"REVISE","feedback":"Zero test coverage on 234-line new middleware file and createRouter factory. Add tests/unit/pi-router-middleware.test.ts covering hook registration, event handling, session ID derivation, turn type classification, sync routing fallback, and error paths. Add createRouter factory tests. Build/typecheck pass; scope is clean."}
```
