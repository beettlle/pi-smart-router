# Final Review — SP-008: Sqlite Core

### Verdict: PASS

### Summary

The SQLite store implementation satisfies all completion criteria: schema migrations create the four required tables (pins, rate_limits, price_cache, telemetry) with proper constraints matching the data-model spec; WAL journal mode is enabled via pragma; and the token bucket uses `BEGIN IMMEDIATE` (via better-sqlite3's `.immediate` transaction property) to prevent TOCTOU races. Build and typecheck pass cleanly, all 64 tests pass (including 23 dedicated sqlite-store tests), and no forbidden files were modified.

### Build / typecheck

```
npm run typecheck && npm test → exit 0

tsc --noEmit: clean
vitest run: 4 test files, 64 tests passed (613ms)
```

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run typecheck && npm test`) | PASS |
| fileScopeMustChange (`src/infrastructure/persistence/sqlite-store.ts`) | PASS — file added (344 lines) |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS — no diff |

### Completion criteria

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Migrations run | PASS | `MIGRATION_V1` creates `pins`, `rate_limits`, `price_cache`, `telemetry` tables; schema versioning via `user_version` pragma (line 248–253) |
| WAL enabled | PASS | `this.db.pragma('journal_mode = WAL')` in `initialize()` (line 241) |
| Token bucket uses BEGIN IMMEDIATE | PASS | `this.db.transaction(...).immediate` in `buildConsumeTokenTx()` (line 286) |

### Scope review

- Only `src/infrastructure/persistence/sqlite-store.ts` changed under `src/` — within declared File Scope.
- `tests/unit/sqlite-store.test.ts` added with 23 tests covering: initialization/WAL/idempotent migrations, SessionPin CRUD (get/put/upsert/delete/round-trip booleans/nulls), ModelProfile read-only, PriceCatalog singleton upsert, telemetry append, and token bucket (allow/drain/reject/retry-after/unknown key error/idempotent init/fractional costs).
- No health check fallback implemented (correctly excluded per "Do NOT" clause).
- Schema aligns with `specs/001-build-smart-router/data-model.md` entities and `StorePort` interface.

### Coverage note

The `coverage:check` script is not yet configured in `package.json` (`@vitest/coverage-v8` not installed). This is a project infrastructure gap predating this task, not a task-specific omission. Manual inspection confirms all public methods and error paths of `sqlite-store.ts` are exercised by tests.

### Suggestions (non-blocking)

1. Consider adding `@vitest/coverage-v8` and a `coverage:check` script in a future infrastructure task so the coverage gate can run automatically.
2. Prepared statements in CRUD methods (e.g., `getSessionPin`, `putSessionPin`) are re-prepared on each call. For hot paths, caching them as instance fields (like `consumeTokenTx`) would improve performance — acceptable for MVP.

```json
{"verdict":"PASS","feedback":"All completion criteria met. Schema migrations create all 4 required tables with correct constraints. WAL mode enabled. Token bucket uses BEGIN IMMEDIATE via .immediate transaction. Build, typecheck, and 64 tests pass. No scope violations."}
```
