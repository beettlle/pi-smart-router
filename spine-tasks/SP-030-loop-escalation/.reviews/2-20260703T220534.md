# Code Review — SP-030 Step 2: Testing and verification

### Verdict: REVISE

### Summary

The loop-escalation module (`src/domain/pinning/loop-escalation.ts`) is well-designed: pure evaluation function, correct once-per-session guard, proper djb2 signature hashing, and clean pipeline integration at the correct stage ordering. Typecheck passes and all 488 existing tests pass. However, the new 181-line module has **zero dedicated unit tests** — no test file exercises `evaluateLoopEscalation`, `extractToolFailureSignature`, or the pipeline integration path with `loopEscalationConfig`. This violates the ≥77% coverage gate (FR-REV-07) since coverage on `src/domain/pinning/loop-escalation.ts` is effectively 0%.

### Build / typecheck

```
$ npm run typecheck && npm test
tsc --noEmit — OK
vitest run — 488 tests passed (24 files)
```

Both gates pass.

### Blocking issues

1. **Missing test:** `tests/unit/loop-escalation.test.ts` — No unit test file exists for `src/domain/pinning/loop-escalation.ts`. Required test cases:
   - `extractToolFailureSignature` returns null for non-tool messages
   - `extractToolFailureSignature` returns null for tool messages without failure patterns
   - `extractToolFailureSignature` returns a deterministic signature for failures
   - `evaluateLoopEscalation` returns `no_pin` when pin is null
   - `evaluateLoopEscalation` returns `already_escalated` when pin reason is `loop_escalation`
   - `evaluateLoopEscalation` returns `not_tool_result` for non-tool-result turns
   - `evaluateLoopEscalation` resets counter on success (`success_reset`)
   - `evaluateLoopEscalation` increments counter on identical failures (`below_threshold`)
   - `evaluateLoopEscalation` resets counter to 1 on different failure signature
   - `evaluateLoopEscalation` fires escalation at threshold (`threshold_exceeded`)
   - `evaluateLoopEscalation` returns `no_frontier_available` when no target exists
   - `selectEscalationTarget` picks a healthy frontier model different from current

2. **Missing test:** `tests/unit/router-pipeline.test.ts` or `tests/integration/session-pinning.test.ts` — No integration test exercises the `loopEscalation` stage in the pipeline with `loopEscalationConfig` set. Required:
   - Pipeline with `loopEscalationConfig` triggers escalation after N identical tool failures
   - Pipeline without config passes through (no-op)
   - Escalation fires once then subsequent tool failures do not re-escalate

### Suggestions (non-blocking)

- `selectEscalationTarget` uses `find()` which returns the first match — consider sorting by a preference score or priority field for deterministic multi-frontier fleets.
- The `FAILURE_PATTERNS` list could benefit from `'rejected'` or `'unauthorized'` for API-level failures, but this is a follow-up concern.

```json
{"verdict":"REVISE","feedback":"New src/domain/pinning/loop-escalation.ts (181 lines) has zero dedicated test coverage. Add tests/unit/loop-escalation.test.ts covering extractToolFailureSignature, evaluateLoopEscalation branches (no_pin, already_escalated, not_tool_result, success_reset, below_threshold, threshold_exceeded, no_frontier_available), and a pipeline integration test showing escalation fires once per session."}
```
