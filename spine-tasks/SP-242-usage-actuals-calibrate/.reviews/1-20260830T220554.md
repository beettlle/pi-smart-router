# Plan Review — SP-242 Step 1: Rolling calibration prior

### Verdict: APPROVE

### Summary

The plan aligns with PROMPT.md Step 1 outcomes: per-model/per-tier rolling actual/estimate prior built on SP-241's persisted telemetry rows (no new persistence layer needed), a single soft-bias application point in the expected-cost path, cold fail-open to catalog, and an optional `SMART_ROUTER_LOG_ROUTING` rationale note. File scope is respected — `expected-cost.ts` is the delivery artifact (per the preflight amendment), the must-not-change `router-pipeline.ts` is untouched by making calibration an optional input, and `price-broker.ts` stays out of the change set. Impact analysis on the HIGH-risk `estimateRoutingCost` was done and mitigated via an optional no-op-default parameter.

### Plan strengths

- Cold-path fail-open and warmup gating (`minSamples`) satisfy mission rules 2–4.
- Single application point in `resolveTierVirtualCost` avoids double-apply through explicit `costPer1M` — a real correctness risk handled up front.
- Outlier clamping and soft clamp [0.5, 2.0] bound the bias, consistent with "soft bias, not hard-ban" (Do NOT rules).
- Bonus `cost_calibration` stats surface stays within May-change scope (`session-stats.ts`) and reuses the existing fail-closed-omission pattern.

### Suggestions (non-blocking)

- Consider asserting in tests that the calibration ratio is applied exactly once per estimate even when both tier selection and `estimateRoutingCost` run in the same turn (the plan mentions "no double-apply" tests; keep this explicit at code review).
- README wording should make clear the bias affects estimates/selection, not reported actuals.

```json
{"verdict":"APPROVE","feedback":"Plan is sound and PROMPT-aligned: rolling prior from SP-241 telemetry rows, single soft-bias application point, cold fail-open, scope respected (expected-cost.ts delivery, pipeline untouched via optional param, HIGH-risk estimateRoutingCost mitigated). Proceed."}
```
