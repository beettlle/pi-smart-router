### Verdict: PASS

### Summary

SP-179 delivers fixture-default, `--live`/`--record-dir`, and `--recorded` ingest modes with checked-in recorded snapshots under `tests/fixtures/benchmark-leaderboards/recorded/`, provenance (`source_url`/`scrape_date`) preserved, and fleet aliases covered by unit tests. Machine contract verification passed; independent re-run of `npm run typecheck && npx vitest run tests/unit/ingest-benchmark-profiles.test.ts` (22 passed) and `npm run routing:verify-benchmark-profiles` succeeded. File scope is respected (`scripts/ingest-benchmark-profiles.ts` + allowed may-change paths; no changes to `router-pipeline.ts` or `.github/workflows/**`).

### Contract verification

- Machine verifier: **PASS**
  - OK testCommand
  - OK fileScopeMustChange: `scripts/ingest-benchmark-profiles.ts`
  - OK fileScopeMustNotChange: `src/domain/pipeline/router-pipeline.ts`, `.github/workflows/**`
- Reviewer re-run: typecheck + scoped vitest **PASS** (22/22); `routing:verify-benchmark-profiles` **PASS**

### Completion criteria

| Criterion | Evidence |
|-----------|----------|
| Default ingest fixture-only (CI-safe) | CLI default `mode: 'fixtures'`; help documents default; unit test asserts no-network default |
| Live/record path + CLI help | `--live`, `--recorded`, `--record-dir`, `--live-url` in `buildUsageText()`; help unit test |
| Offline recorded replay tests | `replays checked-in recorded live snapshots offline with provenance` |
| Provenance on recorded artifacts | Recorded JSON has `source_url` + `scrape_date`; write/persist tests |
| Fleet aliases preserved | Recorded replay + live success tests assert SP-174 aliases |

```json
{"verdict":"PASS","feedback":"SP-179 completion criteria met: fixture default, live/record + recorded replay, provenance and aliases covered by unit tests; contract verifier and scoped typecheck/vitest/verify all green; file scope respected."}
```
