### Verdict: APPROVE

### Summary

Step 3 verification is complete: unit and pipeline tests cover high-P economical selection, low-P frontier selection, price-delta deferral, pin/cache economics (FR-008), and zero-tier readiness. Changes stay within File Scope; `.pi/extensions/smart-router/index.ts` is untouched. Build, lint, test, and coverage gates all pass (`npm run verify:ci` exit 0; `expected-cost.ts` line coverage 94.61%).

### Build / typecheck

Commands run (all passed):

- `npm run typecheck && npm run lint && npm test`
- `npm run coverage:check`
- `npm run verify:ci`

Results: 1014 tests passed; project line coverage 94.04%; `src/domain/routing/expected-cost.ts` at 94.61% lines.

### Scope and diff

Baseline `5e0c6cc..HEAD` (3 SP-106 commits):

| Path | Role |
|------|------|
| `src/domain/routing/expected-cost.ts` | Pure `computeExpectedCost` + `selectTierByExpectedCost` |
| `src/domain/pipeline/router-pipeline.ts` | Wires expected-cost into `low_intensity` gate; telemetry via `mergeFeatureCandidates` and `logExpectedCostExplain` |
| `tests/unit/expected-cost.test.ts` | 8 unit tests (formula, subscription cost, alpha, selection cases) |
| `tests/unit/router-pipeline.test.ts` | SP-106 integration tests + updated SP-105 expectations |
| `spine-tasks/SP-106-expected-cost-tier-selection/STATUS.md` | Step tracking |

Context-fit is respected indirectly: `selectTierByExpectedCost` receives `this.activeFleet` after `contextFitStage` narrows the fleet.

### Step 3 criteria

- **Cheap when P high + significant price delta:** `expected-cost.test.ts` and `router-pipeline.test.ts` SP-106 describe blocks.
- **Frontier when P low despite lower per-token economical cost:** covered in both test files.
- **`npm run verify:ci`:** passes in reviewer run.

### Suggestions (non-blocking)

1. **`src/domain/routing/expected-cost.ts:266-273`** — `expected_cost_no_viable_tier` path is untested; add a fleet-with-only-unhealthy-models case if this branch matters in production.
2. **`src/domain/routing/expected-cost.ts:270`** — rationale string mentions "context-fit" but `listViableTiers` only filters healthy/local-zero; comment could clarify that context-fit is applied upstream via `activeFleet`.

```json
{"verdict":"APPROVE","feedback":"Step 3 verification complete: high-P economical and low-P frontier scenarios tested at unit and pipeline levels; pin economics and telemetry wired; verify:ci and coverage gates pass within scope."}
```
