# Code Review — SP-019 Step 2: Testing and verification

### Verdict: REVISE

### Summary

Build and typecheck pass cleanly (153 tests, `tsc --noEmit` clean). The implementation code in `hardware-probe.ts` and `local-zero-tier.ts` is well-structured with good DI patterns and correct three-state logic. However, neither new source file has any corresponding test file — 266 lines of new production code under `src/` have 0% test coverage, which is blocking per FR-REV-07.

### Build / typecheck

- `npm run typecheck` — PASS (exit 0, no errors)
- `npm test` — PASS (153 tests, 11 test files, 747ms)

### Coverage gate

- `npm run coverage:check` — script does not exist (pre-existing gap)
- `@vitest/coverage-v8` — not installed (pre-existing gap)
- **Changed paths lack tests:** No test files exist for `src/infrastructure/hardware/hardware-probe.ts` or `src/infrastructure/local/local-zero-tier.ts`. Coverage is effectively 0% on in-scope modules.

### Blocking issues

1. **Missing test:** `tests/unit/hardware-probe.test.ts` — No tests for `src/infrastructure/hardware/hardware-probe.ts`. Needed cases: (a) returns `full_local` on Apple Silicon with ≥min_memory_gb_full, (b) returns `classification_only` when memory between classification and full thresholds, (c) returns `disabled` on non-darwin/non-arm64, (d) returns `disabled` when battery below threshold and not on AC, (e) returns `full_local` when battery low but on AC power.

2. **Missing test:** `tests/unit/local-zero-tier.test.ts` — No tests for `src/infrastructure/local/local-zero-tier.ts`. Needed cases: (a) returns `anyModelReady: true` when LM Studio reports loaded models, (b) returns `anyModelReady: true` when Ollama reports loaded models, (c) both unavailable returns all-false result, (d) timeout triggers abort and returns unavailable, (e) HTTP error (non-ok response) returns `available: true, hasLoadedModel: false`.

### Scope compliance

- `fileScopeMustChange`: `src/infrastructure/hardware/hardware-probe.ts` ✓, `src/infrastructure/local/local-zero-tier.ts` ✓
- `fileScopeMustNotChange`: `src/domain/pipeline/router-pipeline.ts` — no changes ✓

### Suggestions (non-blocking)

- Consider installing `@vitest/coverage-v8` and adding a `coverage:check` script to `package.json` so the coverage gate can run in future reviews.
- `parseBatteryInfo` in `hardware-probe.ts` (line 70) is a private helper that could benefit from targeted unit tests via export-for-test pattern if needed.

```json
{"verdict":"REVISE","feedback":"Both new source files (hardware-probe.ts, local-zero-tier.ts) lack any test coverage. Add unit tests for the pure probe logic and HTTP ping functions using the existing DI ports before approval."}
```
