### Verdict: PASS

### Summary

SP-181 delivers per-benchmark live → recorded → fixture fallback via a stub adapter registry, removing fail-fast-all-four behavior without inventing scores or implementing native parsers. Must-change paths are present, `router-pipeline.ts` is untouched, and the contract test command passes (typecheck + 29 scoped unit tests). Machine contract verification also PASS; completion criteria are met.

### Contract verification

- Machine verifier: **PASS** (testCommand, fileScopeMustChange ×3, fileScopeMustNotChange)
- Re-ran: `npm run typecheck && npx vitest run tests/unit/ingest-benchmark-profiles.test.ts tests/unit/benchmark-leaderboard-fetch.test.ts` → **PASS** (29 tests)

### Completion criteria

| Criterion | Evidence |
|-----------|----------|
| Fail-fast-all-four removed | `fetchAllLiveLeaderboards` / `resolveBenchmarkLeaderboardWithFallback` resolve each benchmark independently; live errors swallowed per id |
| Adapter registry + stubs | `scripts/lib/leaderboard-adapters/{types,index,*stub*}.ts` — four stubs, fixture-shaped JSON only |
| Provenance recorded; no invented scores | Stub parses fixture schema / throws; ingest logs `source=` + detail per load |
| Scoped + full tests green | Mixed live/recorded/fixture unit test present; contract suite green |

### Scope notes

- Out-of-May-change one-liner in `tests/unit/release-refresh-benchmark-profiles.test.ts` (`.ts` → `.js` import) is documented in STATUS and required for typecheck — acceptable.
- No native SWE/LCB/BFCL/TB parsers; no npm version bump; no `router-pipeline.ts` / smart-router extension changes.

```json
{"verdict":"PASS","feedback":"SP-181 complete: per-benchmark live→recorded→fixture fallback, stub adapter registry for four benchmarks, mixed success/fallback tests green, contract checks PASS. Safe to keep .DONE."}
```
