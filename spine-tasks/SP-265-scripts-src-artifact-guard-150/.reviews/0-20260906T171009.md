# Plan Review — SP-265, Step 0: Preflight

### Verdict: APPROVE

### Summary
The recorded preflight plan satisfies Step 0's outcomes and the task's Mission. The inventory is concrete (40 tracked compiled files under `scripts/src/**`, stale compiled siblings of `calibration-aggregate.ts`, no importers, demonstrable drift), and the chosen approach — remove tracked artifacts, add a CI/release drift guard, gitignore re-tracking — directly closes #150 while keeping documented entrypoints. The decision to also switch broken `node --experimental-strip-types` npm scripts to `tsx` is in scope (`package.json scripts only` is explicitly allowed) and preserves the "entrypoints still run" contract. All planned changes fall within the File Scope (Must change: `scripts/`; May change: ci.yml, package.json scripts, .gitignore); no scope creep, and `router-pipeline.ts` and the version field remain untouched.

### Contract alignment
- Completion criteria ("no silent scripts/src drift path", calibration entrypoints working) are addressed by both the removal and the guard.
- Guard wiring into `ci.yml` + `release:check` gives a non-silent failure path, matching the mission.
- Bonus tsx fix is documented in STATUS Discoveries with impact — good hygiene.

### Suggestions (non-blocking)
- In Step 2 verification, note the `SMART_ROUTER_SKIP_LIVE_BENCHMARK_REFRESH=1` usage prominently next to the release:check claim (it is present, but the pre-existing upstream failure should stay visible to the operator).

```json
{"verdict":"APPROVE","feedback":"Plan is sound and in-scope: remove tracked scripts/src artifacts plus CI drift guard closes #150; tsx switch for broken strip-types entrypoints is covered by the allowed package.json-scripts scope. No scope creep; contract achievable."}
```
