### Verdict: PASS

### Summary

SP-134 delivers a complete benchmark profile ingest CLI with zod-validated schema, fixture-based ingestion for all four required benchmarks (SWE-bench Verified, Terminal-Bench, LiveCodeBench, BFCL), normalized per-model capability scores, and provenance metadata (source URLs, scrape date, catalog freeze date). Unit tests cover normalization, aggregation, validation, and directory ingest; `npm run verify:ci` passes independently. Contract checks and file-scope constraints are satisfied, and out-of-scope paths (`src/domain/pipeline/router-pipeline.ts`) were not modified.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS — build, typecheck, lint, 1205 tests, coverage gate all green |
| `fileScopeMustChange` (`scripts/ingest-benchmark-profiles.ts`) | PASS — 428-line ingest script added |
| `fileScopeMustNotChange` (`src/domain/pipeline/router-pipeline.ts`) | PASS — zero diff in task commit |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Ingest script produces normalized benchmark records | Met — `aggregateBenchmarkProfiles` emits versioned artifact with per-model `capabilities` and `benchmark_sources` |
| Provenance metadata included | Met — `provenance.source_urls`, `scrape_date`, `catalog_freeze_date` |
| Fixture tests pass | Met — 9 unit tests in `tests/unit/ingest-benchmark-profiles.test.ts` |
| `npm run verify:ci` passes | Met — exit 0 (reviewer-run) |

### Scope and constraints

- **Must change:** `scripts/ingest-benchmark-profiles.ts` — present and functional.
- **May change:** `package.json` (`routing:ingest-benchmarks` script), fixtures under `tests/fixtures/benchmark-leaderboards/`, unit tests — all within scope.
- **Do NOT:** Runtime mapper unchanged; no AST validation added — respected.

```json
{"verdict":"PASS","feedback":"Benchmark ingest CLI, fixtures, tests, and npm script satisfy SP-134 completion criteria. verify:ci passes; contract file-scope checks confirmed; router-pipeline untouched."}
```
