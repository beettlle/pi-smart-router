### Verdict: PASS

### Summary
The worker successfully updated the README and shadow dogfood protocol to document the zero-manual-label bootstrap fields (model override, compaction pin break, loop-escalation proxies, stop_reason) and cross-linked the aggregate, train, and verify commands. The file scope constraints were respected, with no changes made to the calibration weights or config files, and the contract checks passed.

```json
{"verdict":"PASS","feedback":"Excellent documentation update covering the zero-manual-label bootstrap fields and providing cross-links to relevant commands without overstepping file scope."}
```