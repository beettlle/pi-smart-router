### Verdict: PASS

### Summary

SP-091 is complete. `buildRoutingRequest` in `.pi/extensions/smart-router/routing-context.ts` now sets `estimated_input_tokens` via a preferred-token-API lookup (options/context keys) with a chars/4 fallback over mapped messages and system prompt. Unit and integration tests assert non-zero population, monotonic growth with history, explicit override from stream options, and dataset persistence through stream delegation. Machine contract checks passed and `npm run verify:ci` succeeded independently in this review.

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run verify:ci`) | PASS (exit 0) |
| fileScopeMustChange (`.pi/extensions/smart-router/routing-context.ts`) | PASS |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Non-zero `estimated_input_tokens` when messages exist | Met — `estimateInputTokens` returns `Math.max(1, Math.ceil(charCount / 4))` |
| Stable/monotonic across turns | Met — unit test `buildRoutingRequest grows estimated_input_tokens as history grows` |
| Test asserts field populated | Met — `tests/unit/smart-router-extension.test.ts`, `tests/integration/pi-extension.test.ts` |
| `npm run verify:ci` passes | Met |

### Build / typecheck

Command: `npm run verify:ci` — passed (exit 0).

```json
{"verdict":"PASS","feedback":"estimated_input_tokens implemented in buildRoutingRequest with API-first lookup and chars/4 fallback; unit and integration tests cover population, growth, and dataset persistence; contract checks and verify:ci pass."}
```
