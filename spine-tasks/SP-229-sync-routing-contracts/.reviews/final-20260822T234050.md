### Verdict: PASS

### Summary
The task successfully syncs routing contracts with the live pipeline. The `routing-decision.schema.json`, Zod schemas, and TypeScript models were extended to include the `features` sidecar and the updated `PIPELINE_STAGE_ORDER` enum. The contract tests have been updated with strict validation and live `route()` round-trip checks, and all builds, typechecks, and test suites pass successfully. Contract file scope invariants were respected.

```json
{"verdict":"PASS","feedback":"All completion criteria and contract checks are satisfied."}
```