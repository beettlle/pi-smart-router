### Verdict: PASS

### Summary
The task successfully ships the checked-in `routing-calibration.json` with the required provenance, noting non-synthetic sources and replacing synthetic weights. The README has been properly updated to document the behavioral-first path, and test isolation was maintained to ensure `release:check` remains green. All contract verification checks have passed successfully.

```json
{"verdict":"PASS","feedback":"All completion criteria and contract checks have been successfully met. The calibration artifact includes proper provenance, the README is updated, and test isolation is maintained."}
```