# Plan Review — SP-259 Step 0: Preflight

### Verdict: APPROVE

### Summary

The Step 0 plan (and its recorded execution) is sound and fully aligned with PROMPT.md. The preflight located the cache path (`hydra.artifact_cache_path`, default `.pi-smart-router/models/`) and correct model ids, and chose a dedicated pin file `config/onnx-artifact-pins.json` with env-based activation — a reasonable choice given `src/domain/types/schemas.ts` and `hydra-matcher.ts` are outside File Scope, making operator-config schema plumbing impossible. The decision is documented in STATUS Discoveries/Notes as required, and downstream steps (digest pin, fail-closed verify, tests) follow coherently from it.

### Plan vs PROMPT outcomes

- **Locate download/cache path + model ids** — done: `hydra.artifact_cache_path` → `cache_dir` for transformers.js; model ids `Xenova/all-MiniLM-L6-v2` and `onnx-community/granite-embedding-97m-multilingual-r2-ONNX`. Cache layout verification targets `<cache>/<modelId>/<relPath>`, consistent with the transformers.js FS cache.
- **Choose pin config location (STATUS note)** — done: `config/onnx-artifact-pins.json` + `SMART_ROUTER_ONNX_PIN_MODE` (`off|verify|enforce`, default `off`) and `SMART_ROUTER_ONNX_PIN_FILE`. This satisfies mission item 3 (unpinned dogfood unchanged by default) and the "document in STATUS" requirement.
- No scope creep: all artifacts (`config/onnx-artifact-pins.json`, `src/domain/matching/embedding-provider.ts`, `tests/unit/embedding-provider.test.ts`) are inside Must-change/May-change scope; none of the Do-NOT items were touched.

### Notes (non-blocking)

- The review request arrives after implementation (STATUS shows all steps complete); this review evaluates the plan retroactively against the recorded worktree, which matches the plan.
- One preflight artifact to keep an eye on later (not blocking Step 0): real HF digests drift if upstream models are re-uploaded — a refresh procedure note in SP-261 docs would be a good home for that.

```json
{"verdict":"APPROVE","feedback":"Step 0 preflight plan is sound: cache path and model ids correctly located, pin config location chosen (dedicated config/onnx-artifact-pins.json + env activation) with STATUS documentation, no scope creep, and all PROMPT constraints respected."}
```
