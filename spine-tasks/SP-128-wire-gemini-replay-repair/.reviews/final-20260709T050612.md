### Verdict: PASS

### Summary

SP-128 correctly wires `repairGeminiReplayContext` into `buildDelegationContext` after `normalizeDelegationContext` for Google delegation targets only, leaving the non-Google path unchanged. The new extension test exercises multi-turn Gemini tool history with a cross-model router selection (gemini-flash → gemini-pro) and asserts `delegateStream` receives aligned provider/model identity plus preserved and sentinel `thoughtSignature` values. Machine contract checks pass, and independent verification of `npm run typecheck && npm test` completed with 1153 tests passing.

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run typecheck && npm test`) | PASS — 1153 tests, 64 files |
| fileScopeMustChange (`.pi/extensions/smart-router/delegation-runtime.ts`) | PASS |
| fileScopeMustNotChange (`delegation-context.ts`, `route-and-delegate.ts`) | PASS |

### Implementation review

- **`delegation-runtime.ts:144-163`** — Imports `isGoogleDelegationTarget` and `repairGeminiReplayContext` from SP-127; applies repair only when `isGoogleDelegationTarget(targetModel)` after normalization.
- **`tests/unit/smart-router-extension.test.ts:731-846`** — Cross-model Gemini replay test asserts prior turn keeps original signature and latest turn receives `GEMINI_SKIP_THOUGHT_SIGNATURE_SENTINEL`, with both assistant messages aligned to target `gemini-pro`.

### Build / typecheck

```
npm run typecheck && npm test
```

- `tsc --noEmit` — exit 0
- `vitest run` — 64 test files, 1153 tests passed

```json
{"verdict":"PASS","feedback":"Gemini replay repair is wired at the delegation boundary for Google targets; cross-model extension test and full test suite pass; contract scope and forbidden paths respected."}
```
