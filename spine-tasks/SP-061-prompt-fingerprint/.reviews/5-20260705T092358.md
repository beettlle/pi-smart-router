### Verdict: APPROVE

### Summary

SP-061 implements install-local HMAC-SHA256 prompt fingerprints gated on `SMART_ROUTER_DATASET_FINGERPRINT=1`, with pepper management in `.pi-smart-router/.dataset-key`, SQLite v3 migration, extension export hardening, and README documentation. Build, typecheck, lint, and all 769 tests pass; changed paths stay within File Scope and `router-pipeline.ts` is untouched. The `npm run coverage:check` script referenced in spine-config and Step 5 is absent from `package.json` (pre-existing gap), but new behavior is covered by targeted unit tests in `dataset-recorder.test.ts`, `sqlite-store.test.ts`, and `smart-router-pricing.test.ts`.

### Build / typecheck

Commands run (per `.spine/spine-config.json` `testing.build`):

- `npm run typecheck` — passed
- `npm run lint` — passed
- `npm test` — 44 files, 769 tests passed
- `npm run build` — passed

Coverage gate attempt:

- `npm run coverage:check` — **Missing script** (not defined in `package.json`; pre-existing project gap)

### Scope and implementation

| Requirement | Status |
|-------------|--------|
| HMAC-SHA256(install_pepper, normalized_prompt) | Implemented in `dataset-recorder.ts` |
| Pepper in gitignored `.dataset-key`, never exported | `.gitignore` updated; export forbidden keys extended in extension |
| Gate on `SMART_ROUTER_DATASET_FINGERPRINT=1` (requires dataset=1) | `isDatasetFingerprintEnabled()` enforces both |
| `prompt_fingerprint` schema + persistence | SQLite migration v3; entity type extended |
| README opt-in + rainbow-table warning | Added to env var table |
| `router-pipeline.ts` unchanged | Verified (0-line diff vs baseline) |

### Tests

New and updated tests exercise fingerprint enable/disable gating, normalization, HMAC computation, pepper file create/reuse, SQLite round-trip, and export privacy (pepper excluded, fingerprint allowed).

### Suggestions (non-blocking)

1. **`package.json`** — Consider adding a `coverage:check` script so spine-config and Step 5 gates can run automatically in future tasks.
2. **`src/infrastructure/telemetry/dataset-recorder.ts`** — `loadOrCreateDatasetPepper` could validate hex length when reading an existing `.dataset-key` to fail fast on corrupt files.

```json
{"verdict":"APPROVE","feedback":"SP-061 deliverables meet PROMPT outcomes: opt-in HMAC fingerprint, gitignored install pepper never exported, schema migration, README warning, and comprehensive unit tests. Build/typecheck/lint/test/build all pass. coverage:check script is absent (pre-existing); manual test inspection confirms new paths are exercised."}
```
