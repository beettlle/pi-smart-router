### Verdict: PASS

### Summary

SP-061 delivers opt-in install-local prompt fingerprints via HMAC-SHA256 with pepper management in gitignored `.pi-smart-router/.dataset-key`, SQLite v3 migration, export privacy guards, README rainbow-table warning, and targeted unit tests. Machine contract verification passed; build, typecheck, lint, test (769), and build gates all pass independently. File scope is respected (`dataset-recorder.ts` changed; `router-pipeline.ts` untouched).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS |
| fileScopeMustChange (`src/infrastructure/telemetry/dataset-recorder.ts`) | PASS |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS |

### Build / typecheck

Commands run (per `.spine/spine-config.json` `testing.build`):

- `npm run typecheck` — passed
- `npm run lint` — passed
- `npm test` — 44 files, 769 tests passed
- `npm run build` — passed

Coverage gate:

- `npm run coverage:check` — script absent from `package.json` (pre-existing project gap). New fingerprint paths are covered by unit tests in `dataset-recorder.test.ts`, `sqlite-store.test.ts`, and `smart-router-pricing.test.ts`.

### Completion criteria

| Criterion | Status |
|-----------|--------|
| `SMART_ROUTER_DATASET_FINGERPRINT=1` stores HMAC fingerprint (requires dataset=1) | `isDatasetFingerprintEnabled()` + `computePromptFingerprint()` in `dataset-recorder.ts` |
| Pepper in gitignored `.dataset-key` | `.gitignore` line 13; `loadOrCreateDatasetPepper()` |
| Pepper never exported | `DATASET_EXPORT_FORBIDDEN_KEYS` in extension; tests assert exclusion |
| README opt-in + rainbow-table warning | `README.md` env var table |
| Tests pass | 769/769 |

### Suggestions (non-blocking)

1. **`package.json`** — Add a `coverage:check` script so spine-config coverage gates can run automatically in future tasks.
2. **`src/infrastructure/telemetry/dataset-recorder.ts:49-51`** — Consider validating hex length when reading an existing `.dataset-key` to fail fast on corrupt files.

```json
{"verdict":"PASS","feedback":"All SP-061 completion criteria and contract checks satisfied. Opt-in HMAC fingerprint, gitignored install pepper (never exported), schema migration, README warning, and comprehensive unit tests. Build/typecheck/lint/test/build pass. Worker may create .DONE."}
```
