### Verdict: REVISE

### Summary

SP-105 wires online P(success) inference into the `low_intensity` gate with lazy weight loading, alpha-configurable routing, telemetry sidecar fields, and graceful fallback when weights are untrained or missing. Build, typecheck, lint, tests (1003), and coverage gates all pass, and changed modules exceed the 77% line-coverage threshold.

Step 3 requires unit coverage for both low-P outcomes (frontier **or** defer). The pipeline test suite exercises the frontier branch only; the new `p_success_below_alpha` defer path in `applyPSuccessTierHint` is untested and must be covered before approval.

### Build / typecheck

Commands run (all exit 0):

- `npm run typecheck`
- `npm run lint`
- `npm test` — 56 files, 1003 tests passed
- `npm run build`
- `npm run coverage:check` — overall 93.78% lines; `router-pipeline.ts` 94.06%; `p-success-classifier.ts` 83.18%

### Blocking issues

1. **`tests/unit/router-pipeline.test.ts:1209-1240`** — Step 3 requires "low P → frontier or defer"; the test `defers or routes frontier when P_success is below alpha` only asserts the frontier branch (`tier_hint: 'frontier-cloud'`). The defer branch in `src/domain/pipeline/router-pipeline.ts:785-795` (`structuralScore >= high_threshold` with `P < alpha` → `tier_hint: null`, `tier_hint_reason_code: 'p_success_below_alpha'`) is never exercised.
   **Missing test:** `tests/unit/router-pipeline.test.ts` — add a case with low-P trained weights, high structural score (e.g. `high_threshold: 0.1`, simple prompt), and assert `tier_hint` is `null` and `tier_hint_reason_code` is `'p_success_below_alpha'`.

### Suggestions (non-blocking)

- **`src/domain/types/entities.ts`, `src/domain/types/schemas.ts`** — Outside declared File Scope but reasonably required for telemetry sidecar fields; consider updating PROMPT File Scope in a follow-up for clarity.
- **`tests/unit/p-success-classifier.test.ts`** — No test for `resolvePSuccessWeights` when the artifact file exists but JSON is invalid (warn + neutral fallback at `p-success-classifier.ts:443-447`).

```json
{"verdict":"REVISE","feedback":"Build, tests, and coverage pass. Add a pipeline unit test for the low-P defer path (high structural score + P < alpha → tier_hint null, reason p_success_below_alpha) to satisfy Step 3 acceptance criteria."}
```
