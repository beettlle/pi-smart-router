### Verdict: APPROVE

### Summary

Step 3 delivers the required SAAR unit tests and verification for SP-122. The implementation adds `SaarSessionStateTracker` and integrates SAAR policy into `SessionPinner` with buffer-window routing, post-buffer hard-lock with tool-loop tier upgrades, and idle-timeout reopen — all within the declared file scope and without touching `router-pipeline.ts`. Build, typecheck, lint, full test suite, and coverage gates all pass.

### Build / typecheck

| Command | Result |
|---------|--------|
| `npm run typecheck` | Pass |
| `npm run lint` | Pass |
| `npm test` | Pass — 65 files, 1166 tests |
| `npm run coverage:check` | Pass — overall 93.15% lines |

Changed-module coverage (≥77% threshold):

- `src/domain/pinning/saar-session-state.ts` — 96.61% lines
- `src/domain/pinning/session-pinner.ts` — 95.96% lines

### Scope and diff review

Changed files (vs `main`):

- `src/domain/pinning/saar-session-state.ts` (new)
- `src/domain/pinning/session-pinner.ts` (SAAR integration)
- `tests/unit/saar-session-state.test.ts` (new)
- `tests/unit/session-pinner.test.ts` (SAAR policy tests)
- `spine-tasks/SP-122-saar-pin-state-machine/STATUS.md`, `.DONE`

`src/domain/pipeline/router-pipeline.ts` — unchanged (confirmed).

### Test coverage vs PROMPT Step 3

| Requirement | Covered by |
|-------------|------------|
| Buffer transitions (turns 0–1, buffer=2) | `saar-session-state.test.ts` `isInBufferWindow`, `recordTurn`; `session-pinner.test.ts` buffer window describe block |
| Hard-lock during tool loop; tier upgrade allowed | `session-pinner.test.ts` "allows tier upgrade during active tool loop" |
| Hard-lock blocks non-upgrade outside tool loop | `session-pinner.test.ts` "blocks non-upgrade candidate switches when hard-locked outside tool loop" |
| Idle timeout reopen | `saar-session-state.test.ts` `isIdleExpired`, `resetForIdleReopen`; `session-pinner.test.ts` idle timeout describe block |
| SAAR opt-in (legacy behavior preserved) | `session-pinner.test.ts` "SAAR disabled by default" |

### Suggestions (non-blocking)

1. **`src/domain/pinning/saar-session-state.ts:70-71`** — The NaN guard on `Date.parse(last_activity_at)` is untested; a small test with an invalid timestamp would close the remaining uncovered branch.
2. **`src/domain/pinning/session-pinner.ts:327-333`** — Hard-lock path when candidate equals pinned model (no switch attempted) is exercised indirectly; an explicit assertion would document intent.

```json
{"verdict":"APPROVE","feedback":"Step 3 meets SP-122 completion criteria: SAAR state machine and SessionPinner integration are unit-tested in isolation for buffer window, hard-lock with tool-loop tier upgrade, and idle-timeout reopen. All build/typecheck/lint/test/coverage gates pass; router-pipeline.ts unchanged."}
```
