### Verdict: PASS

### Summary

The workflow path filters in `eval-harness-smoke.yml` and `calibration-verify.yml` have been successfully updated to ensure CI quality gates trigger on routing code and extension changes. The documentation in `README.md` now comprehensively lists the full PR and pre-release quality gate set, including the note that branch protection requires a human operator decision. All task completion criteria are met, and the machine verifier confirms that the contract and file scope constraints were respected.

```json
{
  "verdict": "PASS",
  "feedback": "Excellent work. The GitHub Actions workflows were correctly extended to run on the necessary `src/**` and `.pi/extensions/smart-router/**` paths, ensuring PRs won't falsely go green when core routing logic changes. The documentation updates provide clear visibility into the CI quality gate set."
}
```