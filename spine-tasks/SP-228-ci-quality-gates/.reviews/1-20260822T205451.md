### Verdict: APPROVE

### Summary
The plan and implementation for Step 1 precisely satisfy the PROMPT outcomes. Triggers for `eval-harness-smoke.yml` now include `src/**` and `.pi/extensions/smart-router/**`, while `calibration-verify.yml` includes routing-relevant domain and CLI source paths without violating file scope constraints.

```json
{"verdict":"APPROVE","feedback":"Step 1 plan and changes accurately configure workflow path triggers on routing source code and extension directories as specified in PROMPT.md."}
```
