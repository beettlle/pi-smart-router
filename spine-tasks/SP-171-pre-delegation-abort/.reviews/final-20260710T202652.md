### Verdict: PASS

### Summary

SP-171 completion criteria are met: `throwIfAborted` is wired at all required phase boundaries in `route-and-delegate.ts` and `planning-delegate.ts`, the slow-dispatch abort test proves delegation never starts, and the HyDRA mid-ONNX cancel limitation is documented in README and JSDoc. Machine contract verification passed (`testCommand`, `fileScopeMustChange` → `tests/unit/pre-delegation-abort.test.ts`, `fileScopeMustNotChange` → `src/domain/pipeline/**`); independent re-run of the contract command also passed (typecheck + 77 tests).

### Contract verification

Machine verifier: **PASS**
- OK `testCommand`: passed (re-verified: `npm run typecheck && npx vitest run tests/unit/pre-delegation-abort.test.ts tests/unit/smart-router-extension.test.ts` — 77 passed)
- OK `fileScopeMustChange`: `tests/unit/pre-delegation-abort.test.ts` present and in diff
- OK `fileScopeMustNotChange`: `src/domain/pipeline/**` untouched

### Completion criteria

- [x] Abort checks at entry, before `ensureFleetFresh`, before dispatch, before planning delegate, and each failover loop iteration
- [x] Slow-dispatch abort test in `tests/unit/pre-delegation-abort.test.ts` (delegation never starts; early-abort before fleet refresh covered)
- [x] HyDRA mid-ONNX limitation documented (`README.md` + `routeAndDelegate` JSDoc)
- [x] Closes #90 (parent #87 closable when #88–#91 done)

```json
{"verdict":"PASS","feedback":"SP-171 complete: phase-boundary throwIfAborted wired, pre-delegation abort tests pass, HyDRA mid-ONNX limitation documented, contract checks PASS, pipeline untouched."}
```
