### Verdict: PASS

### Summary

SP-174 meets completion criteria: fleet aliases in `config/benchmark-profiles.json` ground common Cursor/Gemini/Anthropic/OpenAI scoped-fleet IDs to existing ingest rows, `capability_source` / `getCapabilitySource()` expose benchmark vs pattern_default, README documents add-fleet-ID after ingest, and unit tests prove real fleet IDs (e.g. `cursor/auto`) are not pattern-default-only. Machine contract checks PASS; re-ran `npm run typecheck && npx vitest run tests/unit/pi-model-mapper.test.ts tests/unit/ingest-benchmark-profiles.test.ts` (49 tests passed). File scope respected (no `.pi/extensions/smart-router/**` or `router-pipeline.ts` changes); aliases target existing `model_id`s without inventing scores.

### Contract verification

- Machine verifier: PASS
- OK testCommand: re-verified — typecheck + scoped vitest (49 passed)
- OK fileScopeMustChange: `src/config/pi-model-mapper.ts`, `config/benchmark-profiles.json`
- OK fileScopeMustNotChange: `.pi/extensions/smart-router/**`
- Completion criteria: aliases + docs + non-pattern-default test + capability source signal all present

```json
{"verdict":"PASS","feedback":"Completion criteria satisfied: fleet aliases ground common scoped-fleet IDs to benchmark rows, capability_source is visible, README add-ID docs exist, and tests prove cursor/auto (and peers) are not pattern-default-only. Contract checks PASS; scoped typecheck+vitest re-verified. Safe to create .DONE."}
```
