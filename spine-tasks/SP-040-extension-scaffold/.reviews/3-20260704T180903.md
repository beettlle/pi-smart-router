# Code Review — SP-040 Step 3: Testing and verification

### Verdict: APPROVE

### Summary

Step 3 updates STATUS.md to record verification; the deliverable code lives in commits `153ab29`–`9018dfe` (`.pi/extensions/smart-router/package.json` and `index.ts`). The extension exports an async factory that discovers models via `ModelRegistry.getAvailable()`, maps them with `mapFleetFromRegistry`, builds a router via `createRouterFromFleet`, registers the `smart-router`/`auto` provider with a placeholder `streamSimple`, and wires `context`/`session_compact`/`model_select` hooks through `router.register`. Build gate `npm run typecheck && npm test` passes (630 tests). `src/domain/**` is untouched and file scope is respected.

### Build / typecheck

| Command | Exit code |
|---------|-----------|
| `npm run typecheck` | 0 (`tsc --noEmit`) |
| `npm test` | 0 (32 files, 630 tests passed) |

### Coverage gate

`npm run coverage:check` is not defined in `package.json` (pre-existing configuration gap). Per PROMPT contract, verification is `npm run typecheck && npm test` only. Extension-specific integration tests are scoped to follow-up task SP-043; at Review Level 2 the scaffold appropriately reuses tested library paths (`mapFleetFromRegistry`, `createRouterFromFleet`, `safeCloudDefault`, middleware `register` delegation covered by `tests/unit/fleet-factory.test.ts` and related suites).

### Scope & contract verification

| Contract field | Expected | Actual | Status |
|---------------|----------|--------|--------|
| `fileScopeMustChange` | `.pi/extensions/smart-router/index.ts` | Created (136 lines) | ✅ |
| `fileScopeMustNotChange` | `src/domain/**` | No diff | ✅ |
| `testCommand` | `npm run typecheck && npm test` | Exit 0 | ✅ |
| Provider registration | `smart-router` with single `auto` model | `pi.registerProvider` at `index.ts:116-135` | ✅ |
| Hooks | `context`, `session_compact`, `model_select` | Via `router.register(createHooksAdapter(pi))` delegating to middleware | ✅ |
| Placeholder stream | Safe cloud default text | `streamSimplePlaceholder` uses `safeCloudDefault(fleet)` | ✅ |

### Diff notes (Step 3 + task)

- **Step 3 commit (`7d3e2f7`):** `STATUS.md` only — marks verification checkboxes complete.
- **Task code (`41337c9..9018dfe`):** New extension package and entry point; no scope creep outside `.pi/extensions/smart-router/**` and task STATUS.

### Suggestions (non-blocking)

1. **`.pi/extensions/smart-router/index.ts:23-30`** — Extension imports from `../../../src/...` rather than the declared `pi-smart-router` package dependency; consider aligning imports with the `file:../../..` dep once `dist/` is built for runtime consumers outside pi's TS loader.

2. **`tsconfig.json`** — `.pi/extensions/**` is outside root `include`; extension TS is not checked by `npm run typecheck`. Acceptable for pi extension layout; SP-043 integration tests will add runtime coverage.

3. **`index.ts:109-110`** — Factory instantiates a standalone `ModelRegistry` instead of `ctx.modelRegistry` (ctx is unavailable at factory init). Functionally sound; consider refreshing fleet on `session_start` if authenticated models change mid-session.

```json
{"verdict":"APPROVE","feedback":"Extension scaffold complete: package.json and index.ts implement model discovery, provider registration, middleware hooks, and placeholder streamSimple. Build gate passes (630 tests). src/domain untouched. Extension integration tests appropriately deferred to SP-043. Step 3 STATUS update is accurate."}
```
