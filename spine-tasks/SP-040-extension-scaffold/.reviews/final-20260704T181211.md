# Final Verdict — SP-040 Extension Scaffold

### Verdict: PASS

### Summary

SP-040 deliverables are complete: `.pi/extensions/smart-router/package.json` and `index.ts` implement the async extension factory with model discovery, `createRouterFromFleet` wiring, middleware hook registration, and a placeholder `streamSimple` that surfaces the safe cloud default. Machine contract verification passed, `src/domain/**` is unchanged, and the independent build gate (`npm run typecheck && npm test`) passes with 630 tests. All PROMPT steps and completion criteria are satisfied; extension integration tests remain appropriately scoped to SP-043.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run typecheck && npm test`) | PASS (machine verifier + reviewer re-run) |
| `fileScopeMustChange` (`.pi/extensions/smart-router/index.ts`) | PASS — 136-line entry point added |
| `fileScopeMustNotChange` (`src/domain/**`) | PASS — no diff vs `main` |

### Build / typecheck

| Command | Exit code | Notes |
|---------|-----------|-------|
| `npm run typecheck` | 0 | `tsc --noEmit` |
| `npm test` | 0 | 32 files, 630 tests passed |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Step 1: extension `package.json` with `@earendil-works/pi-ai`, `@earendil-works/pi-coding-agent`, local `pi-smart-router` | ✅ |
| Step 2: async factory, fleet discovery, provider registration (`smart-router` / `auto`), hooks via `router.register`, placeholder `streamSimple` | ✅ |
| Step 3: typecheck + test run | ✅ (reviewer verified) |
| Step 3: extension dry run | ✅ (worker attested; scaffold follows pi `.pi/extensions` layout and prior step-3 code review approved) |
| Do NOT: modify `src/domain/**` | ✅ |
| Do NOT: real stream delegation (SP-041) | ✅ placeholder only |
| Do NOT: user-facing commands (SP-042) | ✅ none added |

### Implementation notes

- **`.pi/extensions/smart-router/index.ts:108-135`** — Factory creates `ModelRegistry` via `AuthStorage` and `getAvailable()` rather than `ctx.modelRegistry` (ctx unavailable at factory init); functionally satisfies model discovery intent from PROMPT.md.
- **`.pi/extensions/smart-router/index.ts:114`** — `router.register(createHooksAdapter(pi))` delegates `context`, `session_compact`, and `model_select` to existing middleware (verified by `tests/unit/fleet-factory.test.ts` and `tests/unit/pi-router-middleware.test.ts`).
- Commits on branch: `153ab29`, `9018dfe`, `7d3e2f7` — scoped to extension scaffold and STATUS updates only.

### Suggestions (non-blocking)

1. **SP-043** — Add extension load/integration tests and consider refreshing fleet on `session_start` if authenticated models change mid-session.
2. **`.pi/extensions/smart-router/index.ts:23-30`** — Align runtime imports with the declared `pi-smart-router` package dependency once `dist/` is the primary consumer surface.

```json
{"verdict":"PASS","feedback":"Extension scaffold complete and contract-compliant. package.json and index.ts deliver model discovery, smart-router/auto provider registration, middleware hooks, and placeholder streamSimple. Build gate passes (630 tests); src/domain untouched. Worker may create .DONE."}
```
