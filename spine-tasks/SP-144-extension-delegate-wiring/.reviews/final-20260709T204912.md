### Verdict: PASS

### Summary

SP-144 delivers planning-delegate wiring in the pi extension: when the pipeline emits `planning_delegate`, the extension runs a compressed frontier sub-call, injects the result as an observation, and keeps primary inference on the pinned economical model. Fallback to direct frontier routing with `planning_delegate_unavailable` is implemented and operator-visible via console warnings. Contract verification passed, required file-scope constraints were respected, and `npm run verify:ci` plus build/typecheck/lint/test gates all succeeded.

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Extension wires planning delegate spawn per #71 | Met — `planning-delegate.ts` + integration in `route-and-delegate.ts` |
| Compressed context and fallback documented in code paths | Met — module header and inline comments; `buildCompressedDelegateContext` honors SP-142 limits |
| Unit tests cover delegate and fallback | Met — happy path, spawn failure, and missing-registry fallback in `smart-router-extension.test.ts` |
| `npm run verify:ci` passes | Met — exit 0 |

### Contract verification

Machine verifier: **PASS**
- OK `testCommand`
- OK `fileScopeMustChange`: `.pi/extensions/smart-router/index.ts`
- OK `fileScopeMustNotChange`: `src/domain/pipeline/router-pipeline.ts` (unchanged in SP-144 commit)

### Build / typecheck

Commands run independently:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — 1294 tests passed (69 files)
- `npm run verify:ci` — pass
- `npm run coverage:check` — pass

### Implementation notes

- New `planning-delegate.ts` implements compressed context (`exclude_execution_history`, message/token caps), injectable `spawnPlanningDelegate` hook, observation injection, and direct-frontier fallback.
- `route-and-delegate.ts` calls `resolvePlanningDelegatePath` when `isPlanningDelegateActive(decision)` before primary delegation.
- Scope: commit `1278d93` touched only extension paths and tests; no pipeline or domain-type changes.

```json
{"verdict":"PASS","feedback":"Planning delegate spawn wiring, compressed context, observation injection, and fallback paths are implemented and tested. Contract checks and verify:ci pass; router-pipeline.ts was not modified."}
```
