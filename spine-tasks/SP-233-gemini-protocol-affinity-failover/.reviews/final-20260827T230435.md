### Verdict: PASS

### Summary
The task successfully implements the protocol-affinity failover for Gemini `thought_signature` 400 errors. The implementation updates the router loop correctly to allow exactly one non-Google fallback, properly restricts this distinct behavior from triggering circuit breakers, adds the requested telemetry `reason_code` (`gemini_replay_incompatible`), and documents the change in the README. All tests and coverage requirements have passed cleanly.

```json
{"verdict":"PASS","feedback":"Implementation looks solid and covers all edge cases requested. Good job properly isolating protocol-affinity failovers from provider infrastructure failure counts."}
```