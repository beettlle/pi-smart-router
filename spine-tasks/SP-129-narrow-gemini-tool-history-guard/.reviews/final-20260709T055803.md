### Verdict: PASS

### Summary

SP-129 successfully narrows the Gemini tool-history guard from blunt SP-077 exclusion to Google-origin replay risk detection using SP-127 helpers. OpenAI tool sessions no longer exclude or deprioritize Gemini; repairable Google tool history defers to the SP-128 repair path; unrepairable Google replay state still emits `gemini_tool_history_excluded` with SP-084 empty-fleet coverage. Contract verification passed, changes stay within file scope, must-not-change paths are untouched, and build/lint/test/coverage gates all pass.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` | PASS — `npm run typecheck && npm test` |
| `fileScopeMustChange` | PASS — `src/domain/routing/tool-history-guard.ts` changed |
| `fileScopeMustNotChange` | PASS — `delegation-runtime.ts`, `circuit-breaker.ts` unchanged |

### Build / typecheck

Command: `npm run typecheck && npm run lint && npm test` (per `.spine/spine-config.json` `testing.build`)

- `npm run typecheck` — passed
- `npm run lint` — passed
- `npm test` — 1162 tests passed (64 files)

Coverage (`npm run coverage:check`): passed; `src/domain/routing/tool-history-guard.ts` at 92.22% line coverage (≥77% threshold).

### Completion criteria

- OpenAI-only tool sessions can route to economical Gemini — covered in `tool-history-guard.test.ts` and `smart-router-extension.test.ts`
- Google tool sessions rely on SP-128 repair, not blunt ban — repairable Google history no longer excludes Gemini
- Telemetry `gemini_tool_history_excluded` preserved for unrepairable exclusion cases
- SP-080 deprioritize uses shared `sessionHasGoogleReplayRiskForDeprioritize` detector
- Tests updated and passing

### Suggestions (non-blocking)

1. **`src/domain/pipeline/router-pipeline.ts:286`** — `sessionHasGoogleReplayRiskForDeprioritize` accepts optional `contextMessages`, but the pipeline only passes `request`. Google-origin deprioritize cannot trigger from pi-ai context on this path; current tests document this as intentional for routing messages lacking provider metadata.

```json
{"verdict":"PASS","feedback":"Narrowed guard correctly uses hasGoogleReplayRiskFromContext and hasUnrepairableGoogleReplayRiskFromContext; resolveEffectiveFleet excludes only unrepairable risk; SP-080 shares sessionHasGoogleReplayRiskForDeprioritize; unit and extension tests cover OpenAI allowance, repairable Google history, unrepairable exclusion, and SP-084 edge cases. Contract checks, build, lint, test, and coverage gates pass."}
```
