### Verdict: APPROVE

### Summary

SP-129 replaces blunt SP-077 `hasToolCallHistory` exclusion with Google-origin replay detectors from SP-127: OpenAI tool sessions no longer exclude or deprioritize Gemini, repairable Google tool history relies on the SP-128 repair path, and unrepairable Google replay state still triggers `gemini_tool_history_excluded` with SP-084 empty-fleet coverage. Changes stay within file scope, must-not-change paths are untouched, and build/lint/test/coverage gates all pass.

### Build / typecheck

Command: `npm run typecheck && npm run lint && npm test` (per `.spine/spine-config.json` `testing.build`)

- `npm run typecheck` — passed
- `npm run lint` — passed
- `npm test` — 1162 tests passed (64 files)

Coverage (`npm run coverage:check`): passed; `src/domain/routing/tool-history-guard.ts` at 92.22% line coverage (≥77% threshold).

### Suggestions (non-blocking)

1. **`src/domain/pipeline/router-pipeline.ts:286`** — `sessionHasGoogleReplayRiskForDeprioritize` accepts optional `contextMessages`, but the pipeline only passes `request`, so Google-origin deprioritize cannot trigger from pi-ai context on this path. Current tests document this as intentional for routing messages without provider metadata; consider a follow-up if context-aware deprioritize is needed outside the extension guard path.

```json
{"verdict":"APPROVE","feedback":"Narrowed guard correctly uses hasGoogleReplayRiskFromContext and hasUnrepairableGoogleReplayRiskFromContext; resolveEffectiveFleet excludes only unrepairable risk; SP-080 shares sessionHasGoogleReplayRiskForDeprioritize; unit and extension tests cover OpenAI allowance, repairable Google history, unrepairable exclusion, and SP-084 edge cases. Build, lint, test, and coverage gates pass."}
```
