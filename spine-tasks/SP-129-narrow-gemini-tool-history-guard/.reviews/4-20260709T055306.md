### Verdict: REVISE

### Summary

SP-129 correctly narrows the Gemini tool-history guard: OpenAI tool sessions no longer exclude or deprioritize Gemini, repairable Google-origin history relies on the repair path, and unrepairable Google replay state still triggers `gemini_tool_history_excluded` with SP-084 empty-fleet coverage. Unit and extension tests align with PROMPT outcomes and file scope is respected, but the configured build gate fails on ESLint.

### Build / typecheck

Command: `npm run typecheck && npm run lint && npm test` (per `.spine/spine-config.json` `testing.build`)

- `npm run typecheck` — passed
- `npm run lint` — **failed**

```
/Users/cdelgado/Documents/github/pi-smart-router/.worktrees/spine-20260709T050244/lane-3/src/domain/routing/tool-history-guard.ts
  140:3  error  '_messages' is defined but never used  @typescript-eslint/no-unused-vars

✖ 1 problem (1 error, 0 warnings)
```

`npm test` run separately: 1162 tests passed (64 files).

Coverage (`npm run coverage:check`): passed; `src/domain/routing/tool-history-guard.ts` at 92.17% line coverage (≥77% threshold).

### Blocking issues

1. **`src/domain/routing/tool-history-guard.ts:139-142`** — `hasGoogleReplayRisk` declares `_messages` but ESLint `@typescript-eslint/no-unused-vars` rejects it, causing `npm run lint` to fail and blocking the configured build gate. Remove the unused parameter, omit the name (e.g. empty rest), or apply the project’s approved unused-arg pattern.

### Suggestions (non-blocking)

1. **`src/domain/pipeline/router-pipeline.ts:286`** — `sessionHasGoogleReplayRiskForDeprioritize` accepts optional `contextMessages`, but the pipeline only passes `request`; Google-origin deprioritize therefore cannot trigger from pi-ai context. Current tests document this as intentional for routing messages without provider metadata; consider a follow-up if context-aware deprioritize is needed outside the extension guard path.

```json
{"verdict":"REVISE","feedback":"Implementation and tests match SP-129 intent (narrowed Google replay detector, shared SP-080 helper, updated extension tests, SP-084 edge cases). Build gate fails on ESLint: unused _messages parameter in hasGoogleReplayRisk at tool-history-guard.ts:140. Fix lint, then re-run npm run typecheck && npm run lint && npm test."}
```
