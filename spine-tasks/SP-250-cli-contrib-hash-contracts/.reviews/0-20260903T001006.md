# Plan Review — SP-250 Step 0: Preflight

### Verdict: APPROVE

### Summary

The Step 0 preflight plan (import the SP-249 HMAC helper and locate CLI duplicate hash sites) directly satisfies the step's checkbox and correctly sets up Step 1. The plan identifies the right artifacts: `hashSessionIdForTelemetryExport` in `src/infra/telemetry.ts` as the shared helper and `hashSessionIdForContribExport` (unsalted SHA-256, `src/cli/smart-router-cli.ts`) as the duplicate to remove, matching the PROMPT mission and dependency on SP-249. The approach stays within File Scope: `src/infra/telemetry.ts` is import-only per the Must-NOT-change note.

Note: STATUS already shows all steps complete (review arrived after execution; engine-owned per SP-195). Evidence in STATUS (typecheck clean, 27/27 contract tests, full suite 2117/2117, coverage 93.64% ≥ 77%) is consistent with the plan and contract `testCommand`. This does not affect the Step 0 plan verdict.

### JSON

```json
{"verdict":"APPROVE","feedback":"Step 0 preflight plan is sound and aligned with PROMPT.md: it imports the SP-249 helper from src/infra/telemetry.ts (import-only, respecting Must-NOT-change) and locates the unsalted duplicate hashSessionIdForContribExport in src/cli/smart-router-cli.ts. No scope creep or missing dependencies. The later schema.json edit outside the src/** glob is a justified, documented deviation serving the explicit 'bump contrib export schema/version' instruction — flagged as non-blocking."}
```

### Suggestions (non-blocking)

1. `specs/001-build-smart-router/contracts/telemetry-contrib.schema.json` sits outside the literal May-change glob (`src/**`, `tests/**`). The deviation is justified by the mission text ("bump contrib export schema/version field"), but future packets should list contract schema paths explicitly in File Scope to avoid ambiguity.
2. The SP-253 discovery (v1/v2 hash incomparability) is correctly deferred to STATUS rather than README prose — keep this pattern for the docs handoff.
