### Verdict: PASS

### Summary
The implementation successfully adds the `--encoder` flag to bootstrap routing centroids and validates encoder flavor consistency. The contract checks pass, file scopes are respected, and test coverage is provided with 19 new test cases covering the new behavior, including the rejection of mixed-encoder bundles and the enforcement of the honest-untrained gate for the Granite encoder.

```json
{"verdict":"PASS","feedback":"All completion criteria are satisfied. The `--encoder granite` flag is implemented and namespaced correctly. Mixed-encoder bundles are rejected fail-closed, and Granite projection is gated as honest-untrained. Contract checks and scoped tests pass successfully."}
```
