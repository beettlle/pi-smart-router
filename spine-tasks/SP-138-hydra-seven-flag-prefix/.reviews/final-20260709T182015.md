### Verdict: PASS

### Summary

SP-138 extends `buildHydraInput` from the SP-112 four-flag prefix to a HyDRA-aligned seven-flag format (`compact`, `loop`, `attach`), derives all flag values from `RoutingRequest` metadata only, and embeds the latest user turn while excluding prior assistant text per #76. The data-model delta is documented, unit and matcher regression tests cover the new flags and metadata sensitivity, and machine contract checks plus build/test/coverage gates all pass.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS |
| `fileScopeMustChange` (`src/domain/matching/hydra-input.ts`) | PASS |
| `fileScopeMustNotChange` (`src/domain/pipeline/router-pipeline.ts`) | PASS |

### Completion criteria

| Criterion | Result |
|-----------|--------|
| Extended seven-flag prefix implemented | PASS — `src/domain/matching/hydra-input.ts:121-131` |
| HyDRA delta documented in data-model | PASS — `specs/001-build-smart-router/data-model.md:148-188` |
| No raw prompt leakage in metadata | PASS — prefix uses counts/flags only; body is latest user text |
| Encoder excludes prior assistant responses (#76) | PASS — `resolveHydraPromptText` + test at `tests/unit/hydra-input.test.ts:153-170` |
| Tests for new flags and metadata regression | PASS — 16 tests in `hydra-input.test.ts`; compaction regression in `hydra-matcher.test.ts:440-455` |

### Build / typecheck

Commands run independently:

- `npm run typecheck` — exit 0
- `npm run lint` — exit 0
- `npm test` — 65 files, 1204 tests passed
- `npm run verify:ci` — exit 0
- `npm run coverage:check` — exit 0; `src/domain/matching/hydra-input.ts` at 100% line coverage

### Scope

Changes stay within File Scope: required change in `hydra-input.ts`; allowed updates in `data-model.md`, `hydra-input.test.ts`, and `hydra-matcher.test.ts`. `router-pipeline.ts` unchanged.

```json
{"verdict":"PASS","feedback":"Seven-flag HyDRA prefix, data-model delta, assistant-exclusion encoder body, and metadata regression tests are complete. Contract checks, verify:ci, and coverage gates pass; task may retain .DONE."}
```
