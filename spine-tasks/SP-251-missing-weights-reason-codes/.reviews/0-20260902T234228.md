### Verdict: APPROVE

### Summary

The Step 0 preflight plan is sound and fully aligned with PROMPT.md. The worker located both placeholder fallback sites (`resolveHydraProjectionWeights` in `src/domain/matching/hydra-matcher.ts` and `resolveModernBertK4HeadWeights` in `src/domain/matching/modernbert-heads.ts`), identified that both currently surface degradation only via `console.warn`, and mapped the decision-metadata shape within File Scope (`MatchResult` → consumed by `router-pipeline.ts`). The discovery that sidecar/schema propagation lies outside File Scope and is deferred to SP-252 is correctly reasoned and does not conflict with the task contract, which only requires codes in decision metadata — surfacing them on `MatchResult.requirement_reason_codes` plus predictor/provider accessors satisfies "not stderr-only" without scope creep. No Do-NOT items are touched by the plan.

```json
{"verdict":"APPROVE","feedback":"Preflight plan approved: placeholder fallback sites and decision metadata shape correctly identified within File Scope; deferral of sidecar schema propagation to SP-252 is well-justified and keeps the plan scoped to the PROMPT contract."}
```
