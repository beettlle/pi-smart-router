### Verdict: PASS

### Summary

SP-022 integrates the Step 2b turn envelope classifier into `RouterPipeline` as specified by T030. The implementation correctly maps turn types to tiers via a static `TURN_TIER_MAP`, falls through for `main_loop`/`unknown`, and selects a healthy fleet model for actionable turn types. All 468 tests pass, typecheck is clean, `fileScopeMustChange` (`src/domain/pipeline/router-pipeline.ts`) is satisfied, `fileScopeMustNotChange` (`src/domain/pinning/sub-route-policy.ts`) is respected, and the machine contract verifier reports PASS.

### Build / typecheck

- `npm run typecheck` — passed (tsc --noEmit, zero errors)
- `npm test` — 468/468 tests passed across 24 test files

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS — 468 tests, 0 failures |
| fileScopeMustChange | PASS — `src/domain/pipeline/router-pipeline.ts` modified |
| fileScopeMustNotChange | PASS — `src/domain/pinning/sub-route-policy.ts` unchanged |
| completionCriteria | PASS — Step 2b runs after Step 2 (triage) in pipeline order; static map + fleet lookup is well within <2ms budget |

```json
{"verdict":"PASS","feedback":"Turn envelope stage correctly integrated into pipeline. Typecheck clean, all 468 tests pass, contract checks satisfied. Implementation is minimal and within scope — no sub-route policy changes (Do NOT respected)."}
```
