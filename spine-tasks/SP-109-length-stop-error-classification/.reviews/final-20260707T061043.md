### Verdict: PASS

### Summary

SP-109 delivers length-stop classification in `provider-error.ts`, wiring context-pressure vs output-truncation messaging through delegation-runtime sanitization with `contextWindow` hints from the delegate path. Unit tests cover both classification paths (zero-output near context limit and genuine truncation), machine contract checks pass, and `npm run verify:ci` succeeds end-to-end.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS — build, typecheck, lint, 1030 tests, coverage gate |
| `fileScopeMustChange` (`provider-error.ts`) | PASS — classifier, formatters, and sanitizer added |
| `fileScopeMustNotChange` (`router-pipeline.ts`) | PASS — unchanged in SP-109 commit |

### Completion criteria

- **Context vs output messaging:** `formatLengthStopMessage` surfaces "Context window exceeded" for context-pressure cases and "maximum output token limit" for genuine truncation; `sanitizeLengthStopMessage` rewrites context-pressure length stops to `stopReason: error` for operator visibility.
- **Unit tests:** `tests/unit/provider-error.test.ts` exercises zero-output/near-window → context pressure, generated output → output truncation, and sanitization rewrite vs passthrough.
- **Verification:** `npm run verify:ci` exit 0; `provider-error.ts` line coverage 80.83% (≥77%).

### Build / typecheck

Ran `npm run verify:ci` (includes `tsc --project tsconfig.build.json`, `tsc --noEmit`, eslint, vitest, `coverage:check`). All stages passed.

```json
{"verdict":"PASS","feedback":"Length-stop classifier, user-facing messages, delegation-runtime wiring, and unit tests satisfy SP-109 completion criteria; contract checks and verify:ci pass."}
```
