### Verdict: PASS

### Summary
The `EncoderCascadeConfig` schema and the pure `selectEncoderForPrompt` gate have been implemented cleanly and precisely according to the design requirements. All tests pass with 100% coverage on the new gate logic, and the default configuration safely maintains backward compatibility for existing installations. The changes perfectly respect the specified file scope with no scope creep.

```json
{"verdict":"PASS","feedback":"Excellent work. The schema and pure gate are well-isolated, well-documented, and fully covered by unit tests. The file scope and default behaviors are exactly as requested. Contract checks all pass."}
```