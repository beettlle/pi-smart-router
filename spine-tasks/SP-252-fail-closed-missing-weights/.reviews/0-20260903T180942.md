# SP-252 — Plan Review: Step 0 (Preflight)

### Verdict: APPROVE

### Summary

The Step 0 plan is minimal, well-scoped, and correctly framed: confirm SP-251 reason-code importability and choose a schema home before implementation. Both preflight outcomes recorded in STATUS.md are verified on disk — `src/domain/matching/missing-weights-reason-codes.ts` exists with the SP-251 constants, and the chosen schema home (`degraded_route` nested, `schemas.ts:597,660`, default `false`) matches the PROMPT's Must-change scope and Do-NOT constraints (no default flip, no README/package changes). The choice to reuse the existing `neural_misconfigured` NeuralFailureKind rather than invent a fourth cascade directly satisfies Mission item 3.

### JSON

```json
{"verdict":"APPROVE","feedback":"Step 0 preflight plan is sound: SP-251 reason codes confirmed importable (src/domain/matching/missing-weights-reason-codes.ts) and schema home chosen as DegradedRouteConfigSchema nested (verified schemas.ts:597 default false). Reusing neural_misconfigured avoids a new cascade kind per PROMPT. No scope creep; aligns with File Scope and Do NOT constraints."}
```

### Notes (non-blocking)

- Preflight choices keep all downstream work within Must-change/May-change paths; no PROMPT.md amendments needed.
- The flag default `false` correctly preserves fail-open behavior per the Do NOT section.
