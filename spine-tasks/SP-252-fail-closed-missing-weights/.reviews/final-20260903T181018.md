### Verdict: PASS

### Summary
The task successfully implements the `fail_closed_on_missing_weights` config option to optionally prevent fail-open behavior when HyDRA projection or K=4 weights are missing. The integration correctly triggers the degraded routing sandwich with the `neural_misconfigured` kind, surfaces the SP-251 reason codes on the decision path, and leaves the default behavior as fail-open. Contract verification results (testCommand, file scope must change, file scope must not change) all passed, and independent build, lint, and coverage checks confirm the solution is safe to ship.

```json
{"verdict":"PASS","feedback":"All completion criteria are met, testing passes, and contract verification is fully satisfied."}
```