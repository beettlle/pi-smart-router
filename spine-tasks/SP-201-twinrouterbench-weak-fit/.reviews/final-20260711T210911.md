### Verdict: PASS

### Summary

SP-201 meets all completion criteria: ci-subset weak-pack path is documented in `PROVENANCE.md`/`README.md`, `--include-excluded-in-fit` is wired through CLI → `includeExcludedInFit`, and unit tests prove fit grows while `ece_eligible_rows` / holdout counts stay verifier-grade. Machine contract checks PASS; re-ran `testCommand` (typecheck + 9 unit tests) green; #106 is CLOSED with a closing comment; must-not-change paths (`config/release-gates.json`, `src/config/defaults.ts`, `router-pipeline.ts`) are untouched.

### Contract verification

| Check | Result |
|-------|--------|
| Machine verifier | PASS |
| `testCommand` (re-run) | PASS — typecheck OK; 2 files / 9 tests passed |
| `fileScopeMustChange` | matched: `scripts/verify-routing-calibration.ts`, `tests/unit/calibration-dry-run-include-excluded.test.ts` |
| `fileScopeMustNotChange` | respected: `config/release-gates.json`, `src/config/defaults.ts` |
| #106 closable | CLOSED (2026-07-11) with SP-201 completion comment |

### Completion criteria

1. **Weak pack path from real CI subset** — PROVENANCE preferred-input table + README ingest example for `ci-subset.json`; ingest unit coverage extended.
2. **`--include-excluded-in-fit` wired + tested** — `parseDryRunCliArgs` + `main` → `runCalibrationDryRunFrom*` options; tests assert fit growth and ECE boundary.
3. **Weak never in holdout ECE metrics** — excluded samples join fit only; `ece_eligible_rows` / `holdout_sample_count` unchanged with flag.
4. **#106 closable** — issue CLOSED.

### GitNexus

`detect_changes` vs `main`: HIGH (dry-run calibration flows) — expected for this task; covered by unit + contract tests.

```json
{"verdict":"PASS","feedback":"Completion criteria satisfied: ci-subset weak packs documented, --include-excluded-in-fit wired and tested with ECE boundary held, contract testCommand green, #106 closed, must-not-change respected."}
```
