### Verdict: PASS

### Summary
The task implementation successfully fulfills the required completion criteria for SP-266. The `qa:dogfood-soft-feed` CLI script was properly introduced to wire dogfood Track B exports to release gates as a soft-feed dry-run, ensuring it avoids altering `config/release-gates.json` or relaxing frugality defaults. The documentation updates in `docs/qa/shadow-dogfood-protocol.md` successfully outline the new testing sequence, and all contract/verifier constraints pass cleanly. 

```json
{"verdict":"PASS","feedback":"Excellent work providing the CLI tooling and documenting the shadow dogfood soft-feed protocol while strictly following the 'Do NOT' constraints."}
```