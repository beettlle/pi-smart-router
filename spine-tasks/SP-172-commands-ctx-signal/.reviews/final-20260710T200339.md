### Verdict: PASS

### Summary

SP-172 wires `ctx.signal` through long `/smart-router` command handlers via abort guards and a signal-aware fetch wrapper, with a pre-`rebuildFleet` check to avoid partial fleet updates on cancel. File scope is respected (only `commands.ts`, `litellm-fetch.ts`, and allowed tests changed); machine contract verification and re-run of `testCommand` both pass (89 tests). Issue close for #91/#87 is correctly deferred to integrate per PROMPT Step 3.

### Contract verification

Machine verifier: **PASS**
- OK `testCommand`: re-ran `npm run typecheck && npx vitest run tests/unit/smart-router-extension.test.ts tests/unit/litellm-fetch.test.ts` — typecheck clean; 89 tests passed
- OK `fileScopeMustChange`: `.pi/extensions/smart-router/commands.ts` changed
- OK `fileScopeMustNotChange`: `route-and-delegate.ts` and `src/domain/pipeline/**` untouched (0-line diff vs main)

### Completion criteria

- Long handlers honor abort: `throwIfCommandAborted` + `createSignalAwareFetch` in `commands.ts`; LiteLLM fetch accepts `signal` in `litellm-fetch.ts`
- Abort tests: mid-fetch abort, pre-aborted pricing/export, and post-fetch pre-rebuild abort cases in `tests/unit/smart-router-extension.test.ts` and `tests/unit/litellm-fetch.test.ts`
- No partial fleet update on cancel: abort check immediately before `rebuildFleet` after pricing refresh
- Closes #91: code ready; GitHub close deferred to integrate (PROMPT Step 3)

```json
{"verdict":"PASS","feedback":"Completion criteria and contract checks satisfied. ctx.signal abort wiring, fleet-guard, and abort tests are in place within File Scope; testCommand and machine verifier PASS. Issue close deferred to integrate as specified."}
```
