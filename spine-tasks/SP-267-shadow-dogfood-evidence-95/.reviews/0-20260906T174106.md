# Plan Review — SP-267 Step 0: Preflight

### Verdict: APPROVE

### Summary

The Step 0 plan is sound and fully aligned with PROMPT.md. Confirming SP-266 protocol commands from `docs/qa/shadow-dogfood-protocol.md` (which documents the session matrix, export commands, and soft-feed path) is the right preflight, and honestly recording that operator dogfood sessions are not scheduled — rather than claiming progress on human-owned work — matches the packet's Do-NOT-invent-labels contract and mission item 5. The subsequent steps (scaffold + offline dry-runs only, live collection left to the operator, honest gate recommendation) trace directly to the completion criterion "Closes #95 or documents unmet floor."

### Verification

- `docs/qa/shadow-dogfood-protocol.md` exists and documents the protocol commands the plan references (`npm run qa:shadow-dogfood`, session matrix rows 1–6, export commands, no-invent-labels rule).
- `spine-tasks/_authoring/release-v1.0.0/shadow-dogfood-evidence.md` exists and reflects exactly this plan: 0 human sessions, floor UNMET, keep-frugality recommendation, synthetic fixtures explicitly excluded from evidence counts.
- No File Scope violations observed: the only must-change artifact was created; no `config/` gate or default flips; no version bump.

### Suggestions (non-blocking)

- When the operator later supplies real exports, cite the `.pi-smart-router/qa-runs/` archive paths in the evidence artifact for traceability.

```json
{"verdict":"APPROVE","feedback":"Step 0 plan correctly scopes preflight to confirming SP-266 protocol commands and honestly recording that operator dogfood sessions are not scheduled (human-owned). Aligned with PROMPT.md File Scope, Do-NOT constraints, and the 'documents unmet floor' contract path. No changes needed."}
```
