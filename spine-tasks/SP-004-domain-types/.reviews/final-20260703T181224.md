# Final Review: SP-004 — Domain Types

### Verdict: PASS

### Summary

All domain entity types and the `StorePort` persistence port interface are correctly defined in `src/domain/types/`, faithfully matching the data model spec (`specs/001-build-smart-router/data-model.md`). Typecheck and tests pass. File scope constraints are respected — only `src/domain/types/**` was modified, no infrastructure files were touched, and no Zod schemas were introduced.

### Build / typecheck

```
npm run typecheck && npm test
```

- `tsc --noEmit` — exit 0, no errors
- `vitest run` — 1 test passed, exit 0

### Entity verification against data-model.md

| Entity | Status | Notes |
|--------|--------|-------|
| RoutingRequest | ✅ | All 8 fields match spec; optional fields correctly marked |
| SessionPin | ✅ | All 9 fields match spec including nullable `last_tool_failure_signature` |
| ModelProfile | ✅ | Decomposed into ModelCapabilities, ModelPerformance, ModelPricing sub-interfaces |
| RoutingDecision | ✅ | All 9 fields match spec; CandidateScore sub-type included |
| PriceCatalog | ✅ | All 4 fields match spec; `Readonly<Record>` for map types |
| RoutingTelemetry | ✅ | All 10 fields match spec |
| Message | ✅ | role + content + tool_blocks per spec |
| StorePort (T015) | ✅ | CRUD for SessionPin, read for ModelProfile fleet, read/write for PriceCatalog |

### Enums

| Enum | Values match spec |
|------|-------------------|
| TurnType | ✅ planning, tool_result, subagent, main_loop, unknown |
| PinReason | ✅ initial, user_forced, loop_escalation, compaction, cache_economics |
| Tier | ✅ zero-tier, economical-cloud, frontier-cloud |
| RoutingStage | ✅ triage, turn_envelope, session_pin, local_zero, hydra_match, fallback |
| PriceSource | ✅ override, registry, yaml_fallback |

### Scope compliance

- **fileScopeMustChange:** `src/domain/types/index.ts` — confirmed changed
- **fileScopeMustNotChange:** `src/infrastructure/**` — confirmed untouched (directory does not exist)
- **Do NOT (Zod schemas):** No Zod imports or schemas present

### Coverage gate

Not applicable — deliverables are pure TypeScript type definitions and interfaces (erased at compile time) with no runtime code paths to cover.

### Contract verification

Machine verifier: **PASS**
- ✅ testCommand passed
- ✅ fileScopeMustChange matched
- ✅ fileScopeMustNotChange respected

### Suggestions (non-blocking)

- The `Message.tool_blocks` field uses `readonly unknown[]`. A future task could introduce a `ToolBlock` type for stronger typing when tool-use patterns are defined.
- ISO8601 timestamp fields (`created_at`, `updated_at`, `timestamp`, `last_updated`) are typed as `string`. A branded type alias (e.g., `type ISO8601 = string & { readonly __brand: 'ISO8601' }`) could provide additional type safety in a future iteration.

```json
{"verdict":"PASS","feedback":"All entity types and StorePort interface match the data-model spec exactly. Typecheck and tests pass. File scope respected. No Zod schemas introduced. Pure type-definition task with no runtime code — coverage gate N/A."}
```
