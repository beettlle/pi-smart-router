### Verdict: PASS

### Summary

SP-140 delivers consumer-safe `resolveModelScope` resolution in `.pi/extensions/smart-router/pi-model-scope.ts`. The implementation tries the public `@earendil-works/pi-coding-agent` export first, then falls back through module resolution, ancestor walks, `~/.pi/agent/npm`, Homebrew, and the global `pi` binary install tree, with a clear install hint on failure. Unit tests cover dev-repo, consumer fixture, global-pi, and missing-package paths; machine contract checks pass and `npm run verify:ci` completed successfully in this review run.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS — build, typecheck, lint, tests, and coverage:check all succeeded |
| `fileScopeMustChange` (`.pi/extensions/smart-router/pi-model-scope.ts`) | PASS — commit `4d7592c` modifies this file |
| `fileScopeMustNotChange` (`src/domain/pipeline/router-pipeline.ts`) | PASS — no changes in SP-140 commit |
| Machine verifier | PASS (reported in review request) |

### Completion criteria

- **Consumer layout resolution (#86):** `findPiCodingAgentDir` and lazy `loadResolveModelScopeFn` resolve from agent npm root and module-resolution fallbacks without requiring dev-repo `node_modules`.
- **Unit tests:** `tests/unit/pi-model-scope.test.ts` adds six cases covering dev-repo walk, consumer `~/.pi/agent/npm` fixture, end-to-end `resolveModelScope` without repo cwd dependencies, optional global-`pi` path, install-hint error, and source-level strategy documentation.
- **`npm run verify:ci`:** Passed independently during final review.

### Scope and constraints

- Changes limited to in-scope paths (`pi-model-scope.ts`, `tests/unit/pi-model-scope.test.ts`, `STATUS.md`).
- Do NOT items respected: no `verify-consumer-pack.sh` extension, no extension index wiring changes.

```json
{"verdict":"PASS","feedback":"Consumer-safe resolveModelScope resolution is implemented with public-import-first and multi-path fallback discovery. Unit tests cover consumer and dev layouts; contract checks and npm run verify:ci pass. Task completion criteria satisfied."}
```
