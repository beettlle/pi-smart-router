### Verdict: PASS

### Summary
The task has been successfully completed according to the mission requirements. The verifier-grade train path was correctly implemented in `scripts/train-routing-calibration.ts` to aggregate label packs, filter by ship-eligible provenance, evaluate hard ECE gates, and produce the gate report. As expected per the risk plan and honestly reported in STATUS, the candidate artifacts do not pass the hard gates on the CI-scale corpus and the checked-in artifacts were appropriately left untouched. 

```json
{"verdict":"PASS","feedback":"All completion criteria are satisfied. The code changes meet the file scope boundaries, correctly enforce the verifier-grade label constraints without inventing provenance, and safely handle the hard gate failures by treating the output as evidence-only. Tests, typechecks, and the contract command run successfully."}
```