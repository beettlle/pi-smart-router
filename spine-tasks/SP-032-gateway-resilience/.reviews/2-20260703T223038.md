### Verdict: APPROVE

### Summary

Step 2 (Testing and verification) delivers all three SP-032 deliverables: circuit breaker with infra-only tripping (T055/FR-018), weighted failover distribution (T056), and per-key rate limiting with 429 + Retry-After (T057/FR-017). Build and typecheck pass (`npm run typecheck && npm test` — 555 tests, 0 failures). Code is well-structured, stays within file scope, and does not touch `hydra-matcher.ts`.

### Build / typecheck

```
> npm run typecheck && npm test
> tsc --noEmit  ✓
> vitest run    ✓  26 test files, 555 tests passed (933ms)
```

### Contract verification

| Check | Result |
|-------|--------|
| `fileScopeMustChange: STATUS.md` | ✓ Changed |
| `fileScopeMustNotChange: hydra-matcher.ts` | ✓ Not changed |
| `completionCriteria: Infra-only failover; 429 + Retry-After` | ✓ Implemented |
| `testCommand: npm run typecheck && npm test` | ✓ Passes |

### Coverage

`npm run coverage:check` is not available (pre-existing — `@vitest/coverage-v8` not installed, no `coverage:check` script in `package.json`). Manual inspection of test coverage:

- **`circuit-breaker.ts`** (194 lines) — 21 unit tests covering all public methods, all state transitions (CLOSED→OPEN→HALF_OPEN→CLOSED, HALF_OPEN→OPEN), `isInfraError` classification (5xx, 429, network codes, policy rejection exclusion), `reset`, `getOpenCircuits`, `getSnapshot`.
- **`gateway-dispatch.ts`** (new code: ~165 lines) — 13 new tests covering circuit breaker integration (failover, policy rejection exclusion, infra errors, success reset, network errors), rate limiting (429 result, normal routing, no limiter, null `retryAfterSeconds` edge case), and weighted selection (empty, single, multiple, zero-cost).

All changed paths are exercised. Test depth is appropriate for Review Level 3.

### Diff review

**New file: `src/infrastructure/gateway/circuit-breaker.ts`**
- Clean circuit breaker implementation with configurable thresholds
- `isInfraError` correctly classifies network error codes (ECONNREFUSED, ECONNRESET, ETIMEDOUT, ENOTFOUND, UND_ERR_*) and HTTP status codes (≥500, 429) as infra; excludes policy/safety rejections (4xx except 429) per FR-018
- State machine transitions are correct and tested

**Modified: `src/infrastructure/gateway/gateway-dispatch.ts`**
- `applyCircuitBreaker` failover cascade: same-tier weighted → any-tier weighted → original decision
- `weightedSelect` uses inverse-cost weighting with proper floating-point fallback
- `dispatchWithRateLimit` checks token bucket before routing, returns 429-compatible body with `retry_after_seconds` (nullish coalescing `?? 0` handles null correctly)
- `recordOutcome` filters through `isInfraError` before recording failures
- `RateLimitPort` interface uses dependency injection — clean separation

**No scope creep** — all source changes within declared file scope; test files and STATUS.md are expected artifacts.

### Suggestions (non-blocking)

1. `weightedSelect` is exported but could benefit from a brief JSDoc note that it uses inverse-cost weighting (higher weight = cheaper model gets more traffic). The existing doc comment is sufficient but terse.
2. Consider adding `coverage:check` script and `@vitest/coverage-v8` as a follow-up task to enable automated coverage gating.

```json
{"verdict":"APPROVE","feedback":"All gates pass. Circuit breaker, weighted failover, and rate limiting are correctly implemented with comprehensive tests (34 new tests). Build/typecheck clean. Contract satisfied. No scope creep."}
```
