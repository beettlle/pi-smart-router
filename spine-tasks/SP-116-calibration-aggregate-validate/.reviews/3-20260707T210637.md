### Verdict: REVISE

### Summary

Step 3 adds a focused unit test file covering valid contrib rows, tainted-key rejection, JSONL/directory aggregation, and minimum-sample constants. Build and test gates pass for the SP-116 commit (`dad5c29`) when unrelated SP-117 WIP files are excluded; `npm run verify:ci` succeeds in that state.

However, the pepper-field test exercises only `sanitizeContribRecord` in isolation. The ingest path (`parseContribJsonl` / `collectContribFromDir`) runs `assertContribRecordSafe` before sanitization, and that assertion treats `dataset_key`, `pepper`, and `request_id` as forbidden — so otherwise-valid rows containing install-local pepper fields are **rejected**, not stripped. This contradicts PROMPT.md (“Strip install-local pepper fields (`dataset_key`)”) and leaves the new test unable to catch the regression.

### Build / typecheck

Commands run (SP-116 scope, SP-117 WIP stashed):

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — pass (1095 tests, including 5 in `calibration-aggregate.test.ts`)
- `npm run verify:ci` — pass at commit `dad5c29`

Note: current worktree contains untracked SP-117 files (`scripts/train-routing-calibration.ts`, etc.) that break `npm run lint` / `npm run verify:ci`; those are out of SP-116 file scope but pollute the lane worktree.

### Blocking issues

1. **`scripts/calibration-aggregate.ts:30-34,84-88,150-151`** — `CALIBRATION_CONTRIB_FORBIDDEN_KEYS` (including `dataset_key`, `pepper`, `request_id`) is checked in `assertContribRecordSafe` before `sanitizeContribRecord` runs, so install-local pepper fields cause whole-record rejection instead of stripping as required by PROMPT.md mission and completion criteria.
2. **Missing test:** `tests/unit/calibration-aggregate.test.ts` — the case `'strips install-local pepper fields from otherwise valid rows'` calls `sanitizeContribRecord` directly (lines 63–75) and does not assert that `parseContribJsonl` or `collectContribFromDir` accept and strip pepper-bearing rows through the real ingest pipeline.

### Suggestions (non-blocking)

- Add coverage for `readContribFromStdin`, invalid JSONL error paths, and `parseContribJson` array input to harden the aggregate script edge cases.
- Consider splitting reject keys (prompt/messages/secrets/tainted patterns) from strip-only keys (pepper/dataset identifiers) to make the two-stage contract explicit.

```json
{"verdict":"REVISE","feedback":"Pepper fields are rejected in the ingest path (assert before sanitize) despite PROMPT requiring strip; unit test only covers sanitize in isolation and misses the regression."}
```
