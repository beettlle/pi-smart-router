### Verdict: PASS

### Summary

SP-116 delivers the calibration aggregate-and-validate pipeline: `scripts/calibration-aggregate.ts` collects privacy-safe JSONL from `data/contrib/` or stdin, rejects tainted payloads (prompt/message/secret key patterns), strips install-local pepper fields, and documents minimum training sample thresholds in code, schema, and example config. Unit tests cover valid contrib acceptance, tainted rejection, pepper stripping, and directory aggregation; machine contract checks and `npm run verify:ci` both pass.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS — build, typecheck, lint, 1106 tests, coverage gate |
| `fileScopeMustChange` (`scripts/calibration-aggregate.ts`) | PASS — 351-line aggregate/validate script implemented |
| `fileScopeMustNotChange` (`src/domain/pipeline/router-pipeline.ts`) | PASS — zero diff vs `main` |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Training minimum sample size documented | PASS — `MINIMUM_TRAINING_SAMPLES` in `scripts/calibration-aggregate.ts`, `minimum_training_samples` in schema and `config/routing-calibration.json.example` |
| Artifact version field in schema | PASS — bundle `version: 1` and per-artifact version fields in `routing-calibration.schema.json` |
| No prompt text in training pipeline | PASS — `assertContribRecordSafe` rejects prompt/message/secret keys; schema description enforces feature vectors only |
| Validation rejects tainted payloads | PASS — explicit reject keys + `CONTRIB_TAINTED_KEY_PATTERN`; covered by unit tests |
| `npm run verify:ci` passes | PASS — verified in this review |

### Build / typecheck

Command: `npm run verify:ci` (includes `npm run build`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run coverage:check`)

- Build and typecheck: exit 0
- Tests: 61 files, 1106 passed
- Coverage: 93.07% lines (threshold ≥77%)

### Scope and quality notes

- **Must-change deliverable:** `scripts/calibration-aggregate.ts` implements collection, taint rejection, pepper stripping, CLI (`routing:calibration-aggregate`), and stderr warnings when row count is below P(success) minimum.
- **May-change artifacts present:** `routing-calibration.schema.json`, `routing-calibration.json.example`, `package.json` script entry, `data/contrib/.gitkeep`, `tests/unit/calibration-aggregate.test.ts` (5 tests).
- **Review Level 2:** Changed `scripts/` path is covered by dedicated unit tests; in-scope `src/` coverage remains above threshold.

### Suggestions (non-blocking)

1. **`scripts/calibration-aggregate.ts:284-285`** — CLI `usage()` text says pepper fields are rejected, but `sanitizeContribRecord` strips them after validation; align help text with strip-on-ingest behavior.

```json
{"verdict":"PASS","feedback":"SP-116 completion criteria satisfied: aggregate script, bundle schema, example config, taint rejection, pepper stripping, minimum-sample documentation, and unit tests are in place. Contract checks and npm run verify:ci pass; router-pipeline.ts unchanged."}
```
