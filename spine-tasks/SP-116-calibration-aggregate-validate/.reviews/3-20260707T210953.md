### Verdict: APPROVE

### Summary

Step 3 delivers `tests/unit/calibration-aggregate.test.ts` with five cases covering minimum-sample constants, valid contrib acceptance, tainted-key rejection (top-level and nested), pepper-field stripping through the real ingest path (`parseContribJsonl`, `collectContribFromDir`), and multi-file directory aggregation. A follow-up fix (`d4629e1`) correctly splits reject vs strip key lists so install-local pepper fields are stripped rather than rejected, and the pepper test now exercises the ingest pipeline end-to-end. Build, lint, test, and coverage gates all pass; `router-pipeline.ts` is unchanged.

### Build / typecheck

Commands run:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — pass (1106 tests, including 5 in `calibration-aggregate.test.ts`)
- `npm run coverage:check` — pass (93.07% line coverage on `src/**`)
- `npm run verify:ci` — pass

### Suggestions (non-blocking)

- `scripts/calibration-aggregate.ts:284-285` — CLI help text says records containing "install-local pepper fields" are rejected, but pepper keys are stripped after validation; align help wording with behavior.
- Consider adding tests for invalid JSONL error paths and `readContribFromStdin` to harden edge cases (not required at Review Level 2).

```json
{"verdict":"APPROVE","feedback":"Step 3 unit tests cover valid contrib, tainted rejection, and pepper stripping through ingest; all CI gates pass after reject/strip key split fix."}
```
