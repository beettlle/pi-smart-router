### Verdict: PASS

### Summary

SP-131 delivers privacy-safe richer P(success) training labels across the calibration aggregate and classifier training paths. All four failure proxies (tool-failure chains, invalid `stop_reason`, re-prompt rate, edit-distance proxy) are mapped in `calibration-aggregate.ts`, consumed by `p-success-classifier.ts`, and covered by unit tests. Contract verification passed, `npm run verify:ci` succeeded independently (1205 tests, coverage gate OK), and `router-pipeline.ts` was left unchanged.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS — build, typecheck, lint, 1205 tests, coverage:check |
| `fileScopeMustChange` | PASS — `scripts/calibration-aggregate.ts`, `src/domain/routing/p-success-classifier.ts` |
| `fileScopeMustNotChange` | PASS — no diff on `src/domain/pipeline/router-pipeline.ts` |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Aggregate export includes richer failure proxies | Met — `enrichContribRecordWithFailureLabels` exports `tool_failure_chain_count`, `stop_reason_invalid`, `reprompt_rate`, `edit_distance_proxy` and strips raw telemetry keys |
| Classifier training uses new labels when present | Met — `deriveSuccessLabelFromExportRow`, `deriveVerifierFailureSignals`, and `parseTrainingExportLine` wire proxies into training labels |
| Privacy constraint (no raw prompt text) | Met — existing reject/strip rules preserved; raw `stop_reason` and related scalars removed after proxy mapping; tests assert no `prompt_text` |
| `npm run verify:ci` passes | Met |

### Build / typecheck

Command: `npm run verify:ci` — exit 0. `p-success-classifier.ts` line coverage 90.85% (threshold 77%).

### Suggestions (non-blocking)

1. **Worktree hygiene** — Untracked `scripts/src/` contains compiled `.js` artifacts (likely accidental `tsc` output). Remove or add to `.gitignore` before integrate to avoid polluting future diffs.

```json
{"verdict":"PASS","feedback":"Richer failure-proxy labels are implemented end-to-end in calibration aggregate and classifier training with unit tests and privacy guards. Contract checks and verify:ci pass; router-pipeline untouched."}
```
