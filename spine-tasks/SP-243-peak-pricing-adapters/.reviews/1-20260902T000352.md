### Verdict: APPROVE

### Summary

The plan and implementation for Step 1 ("Adapters + injectable clock") cleanly fulfill all stated requirements in `PROMPT.md`. `src/domain/pricing/peak-pricing.ts` implements pure schedule adapters for Z.ai (UTC+8 window, default credits plan profile at 0.5× off-peak) and DeepSeek (UTC windows at 0.5× off-peak) with an injectable `now` date parameter, while non-target providers safely fall back to `{ window: 'none', cost_multiplier: 1 }`.

```json
{"verdict":"APPROVE","feedback":"Step 1 plan and implementation meet all requirements: pure adapters for Z.ai and DeepSeek with injectable clock, correct timezone and schedule boundaries, Z.ai credits default with legacy override support, and safe fail-open fallback for non-target models."}
```
