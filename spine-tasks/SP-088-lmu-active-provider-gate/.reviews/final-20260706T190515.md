### Verdict: PASS

### Summary

SP-088 correctly gates LMU footer display on the active `smart-router/auto` model: `isSmartRouterActive` guards `setLmuStatus`, `model_select` clears or restores LMU on provider switch, and `session_start` restores only when the session model is smart-router/auto. Seven unit tests in `tests/unit/smart-router-extension.test.ts` cover all PROMPT completion criteria, and machine contract verification passed. Build gate (`npm run typecheck && npm test`) succeeded with 893 tests passing.

### Contract verification

- **testCommand:** PASS — `npm run typecheck && npm test` completed successfully
- **fileScopeMustChange:** PASS — `.pi/extensions/smart-router/session-lifecycle.ts` modified
- **fileScopeMustNotChange:** PASS — `src/domain/pipeline/router-pipeline.ts` unchanged

### Build / typecheck

- **Command:** `npm run typecheck && npm test`
- **Result:** PASS — typecheck clean; 50 test files, 893 tests passed

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Footer shows `LMU:` only when active model is `smart-router/auto` | Met — `setLmuStatus` no-ops off smart-router/auto; tested |
| `/model cursor/auto` clears LMU immediately | Met — `model_select` calls `clearLmuStatus`; tested |
| Session restore with `cursor/auto` does not show stale LMU | Met — `session_start` gated; tested |
| Tests pass | Met |

```json
{"verdict":"PASS","feedback":"LMU active-provider gating implemented in session-lifecycle.ts with model_select/session_start hooks and comprehensive unit tests. Contract checks and build gate pass; all completion criteria satisfied."}
```
