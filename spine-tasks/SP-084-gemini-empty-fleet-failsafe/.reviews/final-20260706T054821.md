### Verdict: PASS

### Summary

SP-084 delivers the empty-fleet fail-safe for the Gemini tool-history guard: `resolveEffectiveFleet` flags `fleetEmptyAfterFilter`, `assertRoutableFleetAfterGeminiToolHistoryGuard` throws an actionable `GeminiToolHistoryEmptyFleetError`, and `route-and-delegate.ts` fail-fast wiring prevents `unknown` delegation. Unit and extension tests cover google-only error paths, `force_model_id` override, and `cursor/auto` routing; README troubleshooting documents remediation and cross-links. Machine contract checks pass and `npm run typecheck && npm test` succeeds (862 tests).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS — `npm run typecheck && npm test` (862/862) |
| fileScopeMustChange | PASS — `.pi/extensions/smart-router/route-and-delegate.ts` changed |
| fileScopeMustNotChange | PASS — `src/config/pi-model-mapper.ts` untouched |

### Completion criteria

| Criterion | Verified |
|-----------|----------|
| Tool-history sessions never delegate to Google/Gemini unless `force_model_id` | SP-077 exclusion preserved; override honored in tests |
| Sessions without tool history unchanged | Existing guard early-return paths unchanged |
| Filtered empty fleet never produces `selected_model_id: 'unknown'` | `assertRoutableFleetAfterGeminiToolHistoryGuard` throws before routing; secondary guard on unknown path |
| Actionable error when no non-Gemini model remains | `GeminiToolHistoryEmptyFleetError` with remediation text |
| Tests pass | 862 tests passed |

### Build / typecheck

```
npm run typecheck && npm test
```

Typecheck clean; 50 test files, 862 tests passed.

```json
{"verdict":"PASS","feedback":"Empty-fleet fail-safe implemented with actionable error, route-and-delegate fail-fast, unit/extension tests, and README docs. Contract checks and test gate pass."}
```
