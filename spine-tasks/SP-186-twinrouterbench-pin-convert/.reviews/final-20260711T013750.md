### Verdict: PASS

### Summary

SP-186 meets all completion criteria: upstream pin/license/tier map are documented in `PROVENANCE.md`, the converter maps `question_bank` rows into adapter-valid static-track JSON without inventing tiers/scores, and the corpus directory contains only provenance (no full ~970-row vendor). Machine contract checks passed; independent re-run of `testCommand` passed (typecheck + 16 tests). File scope is respected — must-not-change paths are untouched.

### Contract verification

- Machine verifier: **PASS**
- Independent `testCommand`: **PASS** — `npm run typecheck && npx vitest run tests/eval/twinrouterbench-adapter.test.ts tests/unit/ingest-twinrouterbench-corpus.test.ts` → 16 passed
- `fileScopeMustChange`: `scripts/eval/ingest-twinrouterbench-corpus.ts`, `tests/eval/corpus/twinrouterbench/PROVENANCE.md` present in branch diff
- `fileScopeMustNotChange`: `config/release-gates.json`, `src/domain/pipeline/router-pipeline.ts` — no diff vs `main`

### Completion criteria

| Criterion | Evidence |
|-----------|----------|
| Upstream pin + license documented | `PROVENANCE.md` pin `430acec…`, Apache-2.0 |
| Converter → adapter-valid JSON | ingest script + tests load via `loadTwinRouterBenchStaticTrack` |
| Tier map explicit; no invented labels | PROVENANCE + `UPSTREAM_TIER_TO_EVAL_TIER`; skip unmappable |
| Full corpus not vendored | `tests/eval/corpus/twinrouterbench/` = `PROVENANCE.md` only |
| Release-gate thresholds untouched | no diff on `config/release-gates.json` |

```json
{"verdict":"PASS","feedback":"SP-186 complete: pin/provenance, converter with --limit and skip-unmappable behavior, unit tests loadable by adapter, no full corpus vendored, gates untouched. Contract testCommand re-verified (16 passed)."}
```
