### Verdict: PASS

### Summary

SP-192 meets completion criteria: LLMRouterBench is pinned with license/slice/catalog policy in `PROVENANCE.md`, the offline converter emits a code/tool-only TwinRouterBench static-track subset, and a tiny 5-record CI fixture (SHA-256 matches provenance) is checked in without vendoring the full corpus. Machine contract verification passed; re-ran `npm run typecheck && npx vitest run tests/unit/ingest-llmrouterbench-subset.test.ts` (14/14 green). Forbidden paths (`config/release-gates.json`, `src/domain/pipeline/router-pipeline.ts`) are untouched; regret/community-bench CLIs are out of scope and absent.

### Contract verification

Machine verifier: **PASS**
- OK testCommand
- OK fileScopeMustChange: `scripts/eval/ingest-llmrouterbench-subset.ts`, `tests/eval/corpus/llmrouterbench/PROVENANCE.md`
- OK fileScopeMustNotChange: `config/release-gates.json`, `src/domain/pipeline/router-pipeline.ts`

Reviewer re-check: contract `testCommand` passed (typecheck + 14 unit tests). Fixture checksum `1647fc918c76936e08cb70d4553989aebf7087cbc616c9fa9d1365709cb1fa03` matches PROVENANCE; corpus dir ~16KB (not full HF bundle).

### Completion criteria

| Criterion | Result |
|-----------|--------|
| Pin + license documented | Met (`PROVENANCE.md` HF rev `0e5af1b…`, git `c77cb05…`, MIT, arXiv) |
| Code/tool subset converter offline | Met (`ingest-llmrouterbench-subset.ts` + adapter + synthetic CI path) |
| Chat-only paths excluded | Met (unit tests skip `mtbench` / classify chat-only; CODE_TOOL filter) |
| Full corpus not vendored | Met (5 synthetic records; no `bench-release.tar.gz`) |
| Gate thresholds untouched | Met (no diff on `config/release-gates.json`) |
| Contract + suite green | Met (machine + reviewer contract command) |

```json
{"verdict":"PASS","feedback":"SP-192 complete: pin/provenance, offline code/tool converter, tiny CI fixture with matching checksum, chat-only/unmappable skips covered by unit tests, gates untouched, contract testCommand green. Safe to create .DONE."}
```
