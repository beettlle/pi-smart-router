### Verdict: APPROVE

### Summary

Step 4 satisfies SP-057 at Review Level 2. The pipeline attaches a privacy-safe `RoutingFeatureSidecar` (triage summary, HyDRA requirements, candidates) on every `route()` exit path, the pi extension exports `getRoutingFeatureSidecar` and surfaces features in routing logs, and forbidden paths (`sqlite-store.ts`) are untouched. Build gates pass (`typecheck`, `lint`, 742 tests); dedicated unit and integration tests cover the new behavior and assert no prompt text leakage.

### Build / typecheck

Commands (from `.spine/spine-config.json` `testing.build`):

```
npm run typecheck && npm run lint && npm test
```

Result: **Pass** — typecheck clean, ESLint + models.yaml validation clean, 42 test files / 742 tests passed.

### Coverage

Command: `npm run coverage:check` (from `.spine/spine-config.json`)

Result: **Not runnable** — script absent from `package.json`; `@vitest/coverage-v8` not installed (pre-existing project gap). Manual inspection of changed paths:

| Changed path | Test coverage |
|---|---|
| `src/domain/types/entities.ts` (`RoutingFeatureSidecar`, triage/HyDRA types) | Exercised via router-pipeline SP-057 unit tests and pi-extension integration test |
| `src/domain/pipeline/router-pipeline.ts` (`attachFeatures`, `currentHydraResult`) | Four dedicated cases: fallback triage, triage-stage decision, hydra_match requirements/candidates, session-pin skip |
| `.pi/extensions/smart-router/index.ts` (`getRoutingFeatureSidecar`, log `features`) | Integration test reads sidecar post-dispatch; JSON assertion excludes prompt text |
| `src/domain/types/index.ts` | Re-exports only |

Estimated line coverage on new/changed logic exceeds the 77% threshold. Acceptable at Review Level 2 given PROMPT `testCommand` is typecheck + test only.

### Scope and contract

- **Must change:** `router-pipeline.ts`, `entities.ts` — updated as required.
- **Must NOT change:** `sqlite-store.ts` — unchanged.
- **No prompt text:** Sidecar fields are metadata-only; tests use `JSON.stringify` guards on representative prompts.
- **Extension path:** `getRoutingFeatureSidecar` exported; features available after dispatch without persistence (SP-058 scope).

### Suggestions (non-blocking)

1. **`src/domain/types/entities.ts:116-120` / `src/domain/matching/hydra-matcher.ts:23-27`** — `RequirementVector` is duplicated; consider re-exporting from `hydra-matcher` to avoid drift.
2. **`package.json` / `.spine/spine-config.json`** — Add `coverage:check` script and `@vitest/coverage-v8` so FR-REV-07 gate is automatable (recurring gap across spine tasks).

```json
{"verdict":"APPROVE","feedback":"Step 4 gates green: typecheck, lint, and 742 tests pass. Pipeline attaches triage/HyDRA feature sidecar on all exit paths; extension exposes getRoutingFeatureSidecar; no prompt text in sidecar fields; sqlite-store untouched. coverage:check unavailable project-wide (pre-existing)."}
```
