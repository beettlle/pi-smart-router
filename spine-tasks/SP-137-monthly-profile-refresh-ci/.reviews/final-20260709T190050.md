### Verdict: PASS

### Summary

SP-137 delivers the required monthly benchmark profile refresh workflow with PR-scoped fixture smoke testing, provenance-aware artifact handling, and documented operator policy. Machine contract checks pass, and an independent `npm run verify:ci` run succeeds (build, typecheck, lint, full test suite, coverage gate).

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS — exit 0 (1268 tests, coverage gate satisfied) |
| `fileScopeMustChange` (`.github/workflows/benchmark-profile-refresh.yml`) | PASS — new 136-line workflow added |
| `fileScopeMustNotChange` (`src/domain/pipeline/router-pipeline.ts`) | PASS — no diff vs `main` |

### Completion criteria

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Monthly refresh workflow exists | Met | `.github/workflows/benchmark-profile-refresh.yml`: cron `0 6 1 * *`, `workflow_dispatching_dispatch`, refresh job re-ingests and opens PR on score change |
| Fixture smoke test in CI | Met | PR `smoke` job runs `npm run routing:verify-benchmark-profiles`; unit test `checked-in artifact matches fixture ingest` added |
| Provenance/catalog date in artifact | Met | `config/benchmark-profiles.json` header includes `source_urls`, `scrape_date`, `catalog_freeze_date` (from SP-136 dependency) |
| `npm run verify:ci` passes | Met | Verified locally in this review session |

### Build / typecheck

Command: `npm run verify:ci`  
Result: PASS (exit 0) — includes `npm run build`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run coverage:check`.

Workflow YAML parsed successfully via project `yaml` dependency.

### Suggestions (non-blocking)

1. **`package.json:96`** — `@earendil-works/pi-ai` dependency was changed from `^0.80.3` to `*`. Allowed under "may change" scope, but pinning a semver range avoids accidental drift in downstream installs.
2. **`.github/workflows/benchmark-profile-refresh.yml:94-97`** — Monthly refresh skips PR when only `catalog_freeze_date`/provenance metadata changes but model scores are unchanged. Aligns with PROMPT wording ("when scores change"); document if date-only refreshes are intentionally suppressed.

```json
{"verdict":"PASS","feedback":"Monthly benchmark profile refresh workflow, PR smoke test, provenance artifact, README operator policy, and verify:ci all satisfy SP-137 completion criteria and contract checks."}
```
