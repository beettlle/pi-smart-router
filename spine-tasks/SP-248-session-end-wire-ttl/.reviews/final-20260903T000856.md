### Verdict: PASS

### Summary
The task successfully wires the session teardown logic to the `session_shutdown` lifecycle API and evicts in-memory session state via the SP-247 helper. The orphan-session TTL fallback is properly implemented with clear documentation and tests ensuring robust eviction without causing failures, completely satisfying the prompt requirements and file scopes.

```json
{"verdict":"PASS","feedback":"Well done on a robust fail-open implementation and exhaustive test coverage."}
```