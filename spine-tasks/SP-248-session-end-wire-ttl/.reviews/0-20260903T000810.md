### Verdict: APPROVE

### Summary

The Step 0 preflight plan (and its execution, as recorded in STATUS) is sound and aligned with PROMPT.md. Both preflight goals are satisfied with verified evidence: the SP-247 helper `evictInMemorySessionState` is importable at `src/api/session-eviction.ts`, and the real pi lifecycle hook is `session_shutdown` (ExtensionAPI `SessionShutdownEvent`), not an invented hook — satisfying the "stop with a blocker if unclear" requirement by resolving cleanly instead. Discovery notes also correctly identify blast radius (manual grep verification of `setupSessionHooks` callers) after GitNexus tooling issues, and scope constraints (no README, no middleware file, no version bump) were respected.

Verification performed in this review:
- `evictInMemorySessionState` exported from `src/api/session-eviction.ts:29`; imported in `.pi/extensions/smart-router/session-lifecycle.ts:3`.
- `session_shutdown` handler registered at `session-lifecycle.ts:198`, eviction called at line 119.
- `ORPHAN_SESSION_TTL_MS` documented constant at `session-lifecycle.ts:26`.
- Recent commits confined to SP-248 scope; no Must-NOT-change files touched in commit titles/paths.

The plan uses the discovered API rather than fabricating one, keeps the TTL fallback optional and fail-open, and hands off cleanly to Step 1. No scope creep, no missing dependencies, no premature completion claims beyond what the evidence supports.

### Suggestions (non-blocking)

- STATUS notes GitNexus string-param mangling; consider filing an upstream note so future preflights don't burn time on it, but this does not affect this task.

```json
{"verdict":"APPROVE","feedback":"Preflight plan and evidence verified: SP-247 helper importable at src/api/session-eviction.ts, real pi hook (session_shutdown) identified and wired in session-lifecycle.ts, TTL constant documented, file scope respected. Approach satisfies Step 0 and enables Step 1."}
```
