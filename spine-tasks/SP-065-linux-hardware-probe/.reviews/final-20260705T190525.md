### Verdict: PASS

### Summary

SP-065 delivers the required Linux hardware probe support: platform-specific `SystemInfoPort` providers (macOS, Linux, generic fallback), platform-aware `probeHardware()` policy for darwin/arm64 and linux x64/arm64, a Linux fixture matrix in unit tests, and README documentation of experimental Linux support. Machine contract checks passed, `router-pipeline.ts` was not modified, and `npm run typecheck && npm test` completed successfully (794 tests passed).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS — `npm run typecheck && npm test` |
| fileScopeMustChange | PASS — `src/infrastructure/hardware/hardware-probe.ts` changed |
| fileScopeMustNotChange | PASS — `src/domain/pipeline/router-pipeline.ts` unchanged |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Non-macOS Linux no longer hard-disabled; tested policy via fixtures | Met — `isSupportedPlatform()` enables linux x64/arm64; 6 Linux matrix cases in `hardware-probe.test.ts` |
| CI covers Linux probe via injected fixtures | Met — unit matrix plus `local-zero-tier.test.ts` Linux x64 zero-tier integration case |
| macOS Apple Silicon behavior unchanged | Met — existing macOS tests retained and passing |
| README documents experimental Linux support | Met — Prerequisites table lists Linux (x64/arm64) as Experimental |

### Build / typecheck

```
npm run typecheck && npm test
```

- Typecheck: passed
- Tests: 45 files, 794 passed

### Suggestions (non-blocking)

1. **`src/infrastructure/hardware/hardware-probe.ts:93-143`** — `readLinuxPowerInfo()` is not directly unit-tested (only exercised indirectly if run on Linux). Consider mocked `/sys/class/power_supply` fixtures in a follow-up if provider coverage becomes important.
2. **`tests/unit/local-zero-tier.test.ts`** — Changed outside original File Scope but appropriately documented in STATUS.md; integration test update was necessary after policy change.

```json
{"verdict":"PASS","feedback":"Linux probe refactor complete: platform providers wired, linux x64/arm64 policy replaces darwin-only guard, fixture matrix and README experimental note in place. Contract checks and npm run typecheck && npm test pass; router-pipeline untouched."}
```
