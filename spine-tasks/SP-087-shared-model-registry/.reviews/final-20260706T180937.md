### Verdict: PASS

### Summary

SP-087 successfully wires the smart-router extension to pi's shared `ModelRegistry`, replaces local scope filtering with `resolveModelScope`, and adds fleet caching with fingerprint-based invalidation. Contract verification passed, `npm run typecheck && npm test` completed with 886/886 tests passing, and `src/domain/pipeline/router-pipeline.ts` was not modified. Unit tests cover shared-registry parity (cursor/lmstudio), cache skip/rebuild behavior, status fleet listing, and the `ensureFleetFresh` pre-route hook.

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS — `npm run typecheck && npm test` (886 tests) |
| fileScopeMustChange | PASS — `.pi/extensions/smart-router/fleet-bootstrap.ts` modified |
| fileScopeMustNotChange | PASS — `src/domain/pipeline/router-pipeline.ts` unchanged |

### Build / typecheck

- **Command:** `npm run typecheck && npm test`
- **Result:** Exit 0; 50 test files, 886 tests passed

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Scoped fleet includes package-registered models (cursor/lmstudio) | Met — `discoverFleet` uses shared registry + `resolveModelScope`; tested in `smart-router-pricing.test.ts` |
| `/smart-router status` lists fleet members | Met — `formatStatusMessage` emits `Fleet members:` with `provider/id` lines |
| Tool-history session retains non-Gemini models when cursor in scope | Met — existing SP-077/SP-086 extension tests cover cursor/auto delegation |
| Mid-session enabledModels change on next routed request | Met — `ensureFleetFresh` wired at session_start and before route; fingerprint-change test present |
| No per-turn full rebuild when fingerprint unchanged | Met — `ensureFleetFresh` early-returns on matching fingerprint |
| Tests pass | Met |

### Suggestions (non-blocking)

1. **`pi-model-scope.ts`** — The dynamic import shim for `resolveModelScope` is pragmatic given the missing public export; consider replacing with a direct import once pi-coding-agent exposes it on the package surface.

```json
{"verdict":"PASS","feedback":"Shared ModelRegistry wiring, fleet cache invalidation, status fleet listing, and SP-087 unit/extension tests are complete. Contract checks and npm run typecheck && npm test pass."}
```
