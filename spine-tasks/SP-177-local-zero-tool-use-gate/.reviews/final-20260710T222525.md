### Verdict: PASS

### Summary

SP-177 meets completion criteria: a cheap pre-`local_zero` tool-use estimate (`estimateCheapToolUseRequirement`) gates dispatch when predicted need exceeds `min(local.tool_use, max_tool_use_requirement)`, with telemetry reason `tool_use_capability_shortfall` and operator knobs `local_zero.enabled` / `local_zero.max_tool_use_requirement` (safe defaults preserve trivial traffic). Machine contract checks PASS; scoped typecheck + vitest (109 tests) passed independently; agentic turn-1 tests assert no `local_zero` / `local_model_ready` win while trivial prompts still win.

### Contract verification

- Machine verifier: **PASS**
  - OK `testCommand`
  - OK `fileScopeMustChange`: `src/domain/pipeline/router-pipeline.ts`
  - OK `fileScopeMustNotChange`: `src/domain/triage/**`, `src/domain/pinning/loop-escalation.ts`
- Independent re-run: `npm run typecheck && npx vitest run tests/unit/local-zero-tier.test.ts tests/unit/router-pipeline.test.ts` → typecheck OK, **109 passed**

### Completion criteria checklist

| Criterion | Evidence |
|-----------|----------|
| Pre-`local_zero` heuristic | `router-pipeline.ts` — `estimateCheapToolUseRequirement` (git/bash/edit/explore/delete/repo cues) |
| Skip when over capacity/max | `localZeroTierStage` — `predictedToolUse > ceiling` → skip |
| Operator knobs + safe defaults | `schemas.ts` / `defaults.ts` — `enabled: true`, `max_tool_use_requirement: 0.25`; documented in `operator-config.json.example` + README |
| Telemetry skip reason | `TOOL_USE_CAPABILITY_SHORTFALL` = `tool_use_capability_shortfall` |
| Agentic turn-1 does not win local_zero | `local-zero-tier.test.ts` SP-177 describe block |

### Suggestions (non-blocking)

1. Extension wiring of `local_zero` knobs into `.pi/extensions/smart-router/index.ts` remains out of File Scope (noted in STATUS); operators get schema/defaults via `PipelineOptions.localZeroConfig` until a follow-up wires the extension.

```json
{"verdict":"PASS","feedback":"Completion criteria satisfied: cheap pre-local_zero tool-use gate, operator knobs with safe defaults, telemetry tool_use_capability_shortfall, and agentic turn-1 tests. Contract verifier PASS; scoped typecheck+vitest 109 passed. Worker may create .DONE."}
```
