### Verdict: APPROVE

### Summary

SP-097 Step 3 delivers solid gateway-layer quota failover with comprehensive unit coverage: classification helpers, circuit-breaker isolation, `selectCursorQuotaFailover` with `cursor_quota_exhausted` telemetry, and deterministic economical-failover tests (single-candidate case plus separate weighted multi-candidate assertion). The prior REVISE flake is resolved, build/typecheck/lint/test and coverage gates pass, and changes stay within PROMPT file scope (`gateway-dispatch.ts` + tests only; `pi-model-mapper.ts` untouched).

### Build / typecheck

Command: `npm run typecheck && npm run lint && npm test`

- All passed: 967 tests across 53 files.
- Coverage (`npm run coverage:check`): passed; `gateway-dispatch.ts` line coverage 95.88% (≥77% threshold).

### Scope and tests

- **Changed paths:** `src/infrastructure/gateway/gateway-dispatch.ts`, `tests/unit/gateway-dispatch.test.ts`, `spine-tasks/SP-097-cursor-quota-failover/STATUS.md`.
- **Failover tests** cover dogfood usage-limit messages, 429 quota paths, `cursor/auto` preference, economical fallback, circuit-breaker non-tripping, and `shouldFailoverOnProviderError` classification.
- **Contract alignment:** unit tests exercise `recordOutcome` → `selectFailover` with `cursor_quota_exhausted`, satisfying PROMPT completion criteria for "integration or unit test covers failover path" at Review Level 2.

### Suggestions (non-blocking)

- **`.pi/extensions/smart-router/route-and-delegate.ts:206-240`** — Failover retry is still gated on `isInfraAssistantError` only; plain-text usage-limit messages (#70) won't trigger `selectFailover` at runtime. `shouldFailoverOnProviderError` is exported but unused. STATUS documents this as out of SP-097 file scope; wire in a follow-up task.
- **`src/infrastructure/delegation/provider-error.ts:39-47`** — `parseProviderError` returns `undefined` for non-JSON plain-text errors, so `delegate-stream.ts` may call `recordOutcome` with no error shape for dogfood messages. Consider extending plain-text parsing in a downstream integration task.
- **`src/infrastructure/gateway/gateway-dispatch.ts:106-109`** — `isCursorAutoModel` matches all `cursor/*` IDs, overlapping `isCursorSubscriptionModel`; monitor for misclassification if subscription frontier models share the `cursor/` prefix.

```json
{"verdict":"APPROVE","feedback":"Step 3 complete: flaky economical-failover test fixed, gateway quota classification/failover well-tested, build and coverage gates pass within file scope. Delegation-layer E2E wiring deferred per PROMPT scope — track as follow-up."}
```
