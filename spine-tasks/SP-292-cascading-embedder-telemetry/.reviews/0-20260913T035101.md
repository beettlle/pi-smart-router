### Verdict: APPROVE

### Summary
The preflight plan for Step 0 accurately confirms the SP-291 gate API contract (`selectEncoderForPrompt` returning `{encoder, reason_code, token_estimate}`) and maps the established resource disposal patterns from SP-260 (idempotent fail-closed embedding after disposal). The scope, fail-closed boundaries, and degrade-never-mix fallback requirements strictly align with the task packet outcomes.

```json
{"verdict":"APPROVE","feedback":"Step 0 preflight approach is sound. SP-291 gate integration and disposal lifecycle patterns are correctly mapped without scope violations."}
```
