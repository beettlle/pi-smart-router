### Verdict: REVISE

### Summary

Step 3 delivers the intended runtime isotonic calibration: a new `isotonic-calibrator.ts` module with O(log n) lookup and latency guard, pipeline wiring that gates on calibrated P(success), and telemetry/explain fields for raw vs calibrated scores. Build, lint, and all 1254 tests pass. The new module’s line coverage is 62.56%, below the 77% bar for changed in-scope code — error-path and resolver fallback logic remain largely untested.

### Build / typecheck

Commands run: `npm run typecheck`, `npm run lint`, `npm test`, `npm run coverage:check`.

All passed (68 test files, 1254 tests). Project line coverage: 92.36%.

### Blocking issues

1. **`src/domain/routing/isotonic-calibrator.ts` (62.56% line coverage)** — New must-change module is below the 77% coverage threshold. Uncovered paths include `parseIsotonicCalibratorJson` invalid JSON/schema errors (165–182), `parseIsotonicCalibratorFromBundle` missing/invalid bundle errors (186–204), `loadIsotonicCalibrator` read/parse failures (220–238), and `resolveIsotonicCalibrator` catch-and-fallback (247–254).
2. **Missing test:** `tests/unit/isotonic-calibrator.test.ts` — add cases for malformed JSON, missing `isotonic_calibrator` key, invalid knot schema, unreadable bundle file, and `resolveIsotonicCalibrator` returning null with a warning when the on-disk artifact is invalid.

### Suggestions (non-blocking)

- PROMPT Step 3 lists `tests/integration/router-pipeline.test.ts`; gate thresholding is covered in `tests/unit/router-pipeline.test.ts` SP-133 block — acceptable, but an integration-path test would mirror the PROMPT artifact list.
- `router-explain.ts` was not edited; `p_success_raw` / `p_success_calibrated` flow through `enrichRoutingDecisionForExplain` → `low_intensity_breakdown`, satisfying observability without a direct explain change.

```json
{"verdict":"REVISE","feedback":"Implementation and gates look correct, but isotonic-calibrator.ts line coverage is 62.56% (below 77%). Add unit tests for loader/parser error paths and resolveIsotonicCalibrator invalid-artifact fallback before approval."}
```
