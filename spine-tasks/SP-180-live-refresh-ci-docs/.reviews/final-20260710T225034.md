### Verdict: PASS

### Summary

SP-180 meets completion criteria: monthly/dispatch refresh attempts live ingest with fixture fallback and provenance in the bot PR, PR smoke stays fixture-only/offline, README documents operator commands and cadence, and the mapper smoke asserts ingested (non-pattern-default) floors for a scoped fleet ID. Machine contract verification passed; re-ran `npm run typecheck && npx vitest run tests/unit/pi-model-mapper.test.ts tests/unit/ingest-benchmark-profiles.test.ts` (57 tests passed). File scope respected — must-change paths updated; `router-pipeline.ts` untouched.

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS (re-verified: typecheck + 57 unit tests) |
| fileScopeMustChange: `.github/workflows/benchmark-profile-refresh.yml` | PASS |
| fileScopeMustChange: `README.md` | PASS |
| fileScopeMustNotChange: `src/domain/pipeline/router-pipeline.ts` | PASS |

### Completion criteria

| Criterion | Evidence |
|-----------|----------|
| Live path with fixture fallback | `benchmark-profile-refresh.yml` schedule/dispatch uses `--live` then fixture fallback; `use_live` dispatch input |
| PR smoke fixture-only | `smoke` job runs `routing:verify-benchmark-profiles` only; no `--live` |
| README refresh command + cadence | Operator modes table + monthly/dispatch/PR cadence linked to workflow |
| Mapper smoke ingested floors | `ingested fleet floors smoke (SP-180)` asserts `capability_source === 'benchmark'` and non-pattern floors |
| #100 closable | Acceptance checklist covered by workflow + docs + smoke |

```json
{"verdict":"PASS","feedback":"SP-180 complete: live-or-fixture CI refresh with provenance, fixture-only PR smoke, README operator docs/cadence, and mapper smoke for ingested fleet floors. Contract checks and scoped typecheck+vitest passed; file scope respected."}
```
