# Code Review — SP-029, Step 2: Testing and verification

### Verdict: REVISE

### Summary

The pipeline hook implementation in `src/domain/pipeline/router-pipeline.ts` is clean, type-safe, and follows existing pipeline patterns. Build and typecheck pass (`npm run typecheck && npm test`, 488 tests green). Contract constraints are satisfied — `router-pipeline.ts` changed, `hydra-matcher.ts` untouched. However, the new `hydraMatcher` method's active code paths (lines 324–350) have zero test coverage: no test provides a `hydraMatcher` option, so only the early-return guard (`!matcher`) is exercised.

### Build / typecheck

```
npm run typecheck   → tsc --noEmit  ✓ clean
npm test            → vitest run    ✓ 488 passed (24 files)
```

Coverage tooling (`npm run coverage:check`) is unavailable — `@vitest/coverage-v8` not installed and script missing from `package.json`. This is a pre-existing project gap, not task-introduced.

### Contract verification

| Check | Result |
|-------|--------|
| `fileScopeMustChange`: `src/domain/pipeline/router-pipeline.ts` | ✓ Changed |
| `fileScopeMustNotChange`: `src/domain/matching/hydra-matcher.ts` | ✓ Untouched |
| `completionCriteria`: Ambiguous prompts route through Step 5 matcher | ✓ Pipeline wires `hydraMatcher` stage |
| No loop escalation (SP-030 scope) | ✓ Not introduced |

### Blocking issues

1. **`tests/unit/router-pipeline.test.ts`** — No test exercises the `hydraMatcher` integration when a matcher IS provided. Lines 324–350 of `src/domain/pipeline/router-pipeline.ts` (matcher delegation, no-selection pass-through, model-not-in-fleet guard, and success decision path) are unreachable in the current test suite. At Review Level 2, changed paths must have tests exercising new behavior.

   **Missing tests** — `tests/unit/router-pipeline.test.ts`, add a `describe('hydraMatcher stage (T050)')` block covering:
   - **`hydraMatcher returns a decision when matcher selects a fleet model`** — provide a mock `HydraMatcher` that returns `{ selected: { model_id: 'gpt-4o-mini', score: 0.9, shortfall: 0, rejected_reason: null }, candidates: [...], requirements: ..., elapsedMs: 5, budgetExceeded: false }`. Assert `decision.stage === 'hydra_match'`, `reason_code === 'hydra_embedding_match'`, and `candidates` is populated.
   - **`hydraMatcher passes through when matcher returns no selection`** — mock matcher returns `{ selected: null, ... }`. Assert pipeline falls through to fallback.
   - **`hydraMatcher passes through when selected model is not in fleet`** — mock matcher returns a `model_id` not present in fleet. Assert pass-through to fallback.

### Suggestions (non-blocking)

- The `result.selected!.model_id` non-null assertion on line 331 is safe because of the `!result.selected` guard on line 326, but a local binding (e.g., `const { model_id } = result.selected`) before the fleet lookup would be clearer and eliminate the assertion.
- `routing_latency_ms: result.elapsedMs` — consider whether this should measure the full stage time (including fleet lookup) rather than just the matcher's self-reported elapsed time.

```json
{"verdict":"REVISE","feedback":"New hydraMatcher pipeline code paths (lines 324-350 in router-pipeline.ts) have zero test coverage. Add tests for: matcher selects fleet model (success), matcher returns no selection (pass-through), and selected model not in fleet (pass-through). Build/typecheck pass; contract checks satisfied."}
```
