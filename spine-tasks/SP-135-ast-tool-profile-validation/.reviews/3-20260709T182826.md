### Verdict: APPROVE

### Summary

SP-135 Step 3 delivers a Switchcraft-style AST validator (`scripts/lib/ast-tool-validation.ts`), wires it into benchmark ingest for `terminal_bench`/`bfcl` rows with reason-coded skips, and documents the false-negative tradeoff in script stderr. Unit and ingest integration tests cover valid shapes, malformed snippets, paraphrase tolerance, and fixture-driven cases; `src/domain/pipeline/router-pipeline.ts` is untouched.

### Build / typecheck

Commands run (all exit 0):

- `npm run typecheck`
- `npm run lint`
- `npm test` — 67 files, 1236 tests passed
- `npm run coverage:check` — overall line coverage 93% (threshold met)

### Scope and contract

| Check | Result |
|-------|--------|
| `scripts/lib/ast-tool-validation.ts` added | Yes |
| Ingest integration with skip logging | Yes (`aggregateBenchmarkProfiles`, `main` stderr) |
| Positive/negative fixtures | Yes (`tests/fixtures/tool-call-samples/`, 11 unit tests) |
| False-negative note in script output | Yes (`AST_VALIDATION_FALSE_NEGATIVE_NOTE`) |
| `router-pipeline.ts` unchanged | Yes (no diff vs baseline `f51987b`) |

### Diff review (baseline `f51987b..HEAD`)

- **Validator module** — Parses OpenAI `tool_calls`, direct `{name,arguments}`, `function_name`/`parameters`, and fenced JSON; validates function name plus argument key/type fingerprint without literal equality.
- **Ingest** — Optional `tool_call_snippet` on leaderboard entries; AST failure skips the row, logs reason code, relaxes `benchmark_sources` to `partialRecord` so skipped benchmarks do not block artifact emission.
- **Tests** — Dedicated `ast-tool-validation.test.ts` plus three new ingest tests for tool-use benchmark gating and valid-snippet acceptance.

### Suggestions (non-blocking)

1. **`scripts/lib/ast-tool-validation.ts:267-268`** — `missing_function_name` appears unreachable because `normalizeToolCall` returns `null` for empty/missing names before `sawMissingName` is set; consider a targeted test if that branch is intentional, or simplify reason-code handling.
2. **`tests/unit/ast-tool-validation.test.ts`** — No explicit case for `invalid_arguments_shape` (e.g., arguments as a JSON array); low risk given ingest tests cover the primary failure modes.

```json
{"verdict":"APPROVE","feedback":"AST validator, ingest wiring, fixtures, and tests satisfy SP-135 Step 3 and completion criteria. Build, lint, test, and coverage gates pass; file scope and router-pipeline contract respected."}
```
