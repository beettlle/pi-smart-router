### Verdict: PASS

### Summary

SP-135 delivers a Switchcraft-style AST tool-call validator in `scripts/lib/ast-tool-validation.ts`, integrates it into benchmark profile ingest for `terminal_bench`/`bfcl` rows with reason-coded skips, and documents the false-negative tradeoff in ingest stderr. Unit and integration tests cover valid shapes, malformed snippets, paraphrase tolerance, and ingest gating; machine contract checks pass and `npm run verify:ci` succeeds. `src/domain/pipeline/router-pipeline.ts` is unchanged.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS (exit 0) |
| `fileScopeMustChange` (`scripts/lib/ast-tool-validation.ts`) | PASS — new 276-line module |
| `fileScopeMustNotChange` (`src/domain/pipeline/router-pipeline.ts`) | PASS — zero diff vs `main` |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| AST validator module with tests | Met — `validateToolCallAst`, 11 unit tests + fixtures |
| Ingest pipeline uses validation | Met — `aggregateBenchmarkProfiles` skips invalid `tool_call_snippet` rows with reason codes |
| Documented validation approach | Met — module header, `AST_VALIDATION_FALSE_NEGATIVE_NOTE`, `main` stderr |
| `npm run verify:ci` passes | Met |

### Build / typecheck

Commands run (all exit 0):

- `npm run typecheck`
- `npm run lint`
- `npm test` — 67 files, 1236 tests passed
- `npm run coverage:check` — overall line coverage 93% (threshold met)
- `npm run verify:ci` — full CI pipeline passed

### Scope and quality

- **Validator** — Parses OpenAI `tool_calls`, direct `{name,arguments}`, `function_name`/`parameters`, and fenced JSON; validates function name plus argument key/type fingerprint without literal equality.
- **Ingest** — Optional `tool_call_snippet` on leaderboard entries; AST failure skips the row, logs reason code, and `benchmark_sources` uses `partialRecord` so skipped benchmarks do not block artifact emission.
- **Tests** — `tests/unit/ast-tool-validation.test.ts` plus three new ingest tests for tool-use benchmark gating and valid-snippet acceptance; four JSON fixtures under `tests/fixtures/tool-call-samples/`.
- **Do NOT constraints** — No runtime mapper or online serving changes; scope limited to scripts and tests.

### Suggestions (non-blocking)

1. **`scripts/lib/ast-tool-validation.ts:267-268`** — `missing_function_name` branch may be unreachable because `normalizeToolCall` returns `null` for empty names before `sawMissingName` is set.
2. **`vitest.config.ts:10`** — Coverage `include` is `src/**/*.ts` only; `scripts/lib/ast-tool-validation.ts` is exercised by tests but not measured in `coverage:check`. Consider adding `scripts/**/*.ts` if script modules become recurring deliverables.

```json
{"verdict":"PASS","feedback":"AST validator, ingest wiring, fixtures, tests, and false-negative documentation satisfy SP-135 completion criteria. Contract checks pass; build, lint, test, and coverage gates succeed; router-pipeline file-scope constraint respected."}
```
