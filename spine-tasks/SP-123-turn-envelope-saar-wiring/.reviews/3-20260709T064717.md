### Verdict: APPROVE

### Summary

Step 3 delivers the required SAAR integration tests and verification. Four new cases in `tests/integration/session-pinning.test.ts` cover planning-within-buffer frontier routing without pin overwrite, post-buffer hard-lock on execution and planning turns, and regressions for loop escalation and context overflow. Build, lint, targeted integration tests, and full coverage gates all pass; changes stay within file scope and `turn-envelope.ts` is untouched.

### Build / typecheck

Commands run:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test -- tests/integration/session-pinning.test.ts` — 25/25 pass
- `npm run coverage:check` — 1181/1181 pass; `src/domain/pipeline/router-pipeline.ts` line coverage 92.31% (≥77%)

### Coverage

`router-pipeline.ts` SAAR wiring (`shouldDeferPlanningForSaar`, `finalizeRoute`, `enrichRequestWithSaarCandidate`, `saar_route`/`saar_hard_lock` reason codes) is exercised by the new integration suite. `saar_tier_upgrade` pipeline mapping is covered indirectly via existing `session-pinner` unit tests.

### Suggestions (non-blocking)

1. **`tests/integration/session-pinning.test.ts`** — Consider a follow-up integration case for `saar_tier_upgrade` / `saar_route` when capability requires a tier bump inside the buffer; currently only covered at the pinner unit layer.

```json
{"verdict":"APPROVE","feedback":"Step 3 integration tests and verification gates pass. SAAR buffer, hard-lock deferral, and pin-break regressions are covered; file scope respected."}
```
