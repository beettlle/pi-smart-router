### Verdict: PASS

### Summary

SP-051 satisfies all completion criteria and contract checks. Lifecycle compaction and `model_select` hooks now populate `LifecycleHookState`, `buildRoutingRequest()` consumes those flags on the next routing request, and prior no-op middleware handlers are replaced with real implementations. Extension-path integration tests assert pin breaks without manually injecting `compaction_flag` or `force_model_id`. Build gates pass (719 tests); `session-pinner.ts` is untouched per contract.

### Contract verification

Machine verifier: **PASS**
- OK `testCommand`: `npm run typecheck && npm test` passed
- OK `fileScopeMustChange`: `.pi/extensions/smart-router/index.ts` changed
- OK `fileScopeMustNotChange`: `src/domain/pinning/session-pinner.ts` unchanged

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Compaction and model override break pins in extension path | ✅ `tests/integration/session-pinning.test.ts` SP-051 block (3 tests) |
| No exported no-op hook handlers remain | ✅ `pi-router-middleware.ts` registers compaction and model_select handlers with state mutations |
| Tests pass | ✅ 719/719 |

### Build / typecheck

Commands run:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — pass (41 files, 719 tests)

### Coverage

Command: `npm run coverage:check` (from `.spine/spine-config.json`)

Result: **Not runnable** — script absent from `package.json`; pre-existing project gap noted in prior spine reviews. Manual inspection confirms adequate coverage of changed paths:

| Changed path | Tests |
|---|---|
| `src/api/middleware/pi-router-middleware.ts` | `tests/unit/pi-router-middleware.test.ts` — compaction, before-compact, model_select set/ignore, one-shot consume |
| `.pi/extensions/smart-router/index.ts` | `tests/unit/smart-router-extension.test.ts` — `buildRoutingRequest` consumes lifecycle flags |
| Extension-path pin break | `tests/integration/session-pinning.test.ts` — compaction break, model override, status after break |

Acceptable at Review Level 2; PROMPT `testCommand` is typecheck + test only.

### Scope

- **Must change:** `.pi/extensions/smart-router/index.ts`, `src/api/middleware/pi-router-middleware.ts` — implemented in commit `7a3a669`
- **May change:** `tests/integration/session-pinning.test.ts`, unit test updates — present
- **Must NOT change:** `src/domain/pinning/session-pinner.ts` — no diff
- **Supporting:** `src/index.ts` exports `LifecycleHookState` and threads shared state through `createRouterFromFleet` — necessary wiring, not domain scope creep

### Suggestions (non-blocking)

1. **`package.json`** — Add `coverage:check` script and `@vitest/coverage-v8` so FR-REV-07 gate is automatable (recurring gap across spine tasks).

```json
{"verdict":"PASS","feedback":"All SP-051 completion criteria met: lifecycle hooks wire compaction_flag and force_model_id through LifecycleHookState into buildRoutingRequest; extension-path integration tests assert pin breaks without manual flag injection; no-op handlers removed. Contract verifier PASS; typecheck, lint, and 719 tests green; session-pinner.ts untouched."}
```
