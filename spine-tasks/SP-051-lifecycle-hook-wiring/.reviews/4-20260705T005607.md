### Verdict: APPROVE

### Summary

Step 4 verification gates pass: `npm run typecheck`, `npm run lint`, and `npm test` all succeed (719 tests). SP-051 correctly wires compaction and `model_select` lifecycle hooks through shared `LifecycleHookState`, propagates flags via `buildRoutingRequest()`, and replaces prior no-op middleware handlers. Extension-path integration tests assert pin breaks without manually injecting `compaction_flag` or `force_model_id` on routing requests. `session-pinner.ts` is untouched per contract.

### Build / typecheck

Commands run:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — pass (41 files, 719 tests)

### Coverage

Command: `npm run coverage:check` (from `.spine/spine-config.json`)

Result: **Not runnable** — script missing from `package.json`; `@vitest/coverage-v8` not installed (pre-existing project gap). Manual inspection of changed paths:

| Changed path | Test coverage |
|---|---|
| `src/api/middleware/pi-router-middleware.ts` | `tests/unit/pi-router-middleware.test.ts` — compaction, before-compact, model_select set/ignore, consume one-shot |
| `.pi/extensions/smart-router/index.ts` | `tests/unit/smart-router-extension.test.ts` — `buildRoutingRequest` consumes compaction and force-model flags |
| Extension-path pin break | `tests/integration/session-pinning.test.ts` — 3 SP-051 tests (compaction break, model override, status after break) |

Estimated line coverage on new/changed logic exceeds the 77% threshold. Acceptable at Review Level 2 given PROMPT `testCommand` is typecheck + test only.

### Scope and contract

- **Must change:** `.pi/extensions/smart-router/index.ts`, `src/api/middleware/pi-router-middleware.ts` — implemented
- **Must NOT change:** `src/domain/pinning/session-pinner.ts` — no diff
- **Completion criteria:** hooks set flags on next routing request; integration tests cover extension path without manual flag injection; no no-op lifecycle handlers remain

`src/index.ts` export/passthrough of `LifecycleHookState` is a reasonable supporting change for shared state between middleware and extension.

### Suggestions (non-blocking)

1. **`package.json` / `.spine/spine-config.json`** — Add `coverage:check` script and `@vitest/coverage-v8` so FR-REV-07 gate is automatable (noted across prior spine tasks).

```json
{"verdict":"APPROVE","feedback":"Step 4 gates green: typecheck, lint, and 719 tests pass. LifecycleHookState wiring, middleware hook handlers, and buildRoutingRequest flag propagation satisfy FR-008 extension-path requirements with adequate unit and integration tests. session-pinner.ts untouched. coverage:check unavailable project-wide (pre-existing)."}
```
