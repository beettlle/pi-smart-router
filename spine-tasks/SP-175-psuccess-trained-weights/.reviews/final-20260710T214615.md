### Verdict: PASS

### Summary

SP-175 ships a non-example `config/p-success-weights.json` with `trained_sample_count: 40` (≥30), synthetic provenance, operator train/reload docs in README (no prompt text), and pipeline/telemetry exposure of `p_success_raw` vs `p_success_calibrated`. Machine contract verification passed; independent re-run of `npm run typecheck && npx vitest run` on the scoped unit suites also passed (54 tests). File scope is respected (must-change weights present; `.pi/extensions/smart-router/**` untouched).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS (machine + re-run: typecheck + 54 scoped tests) |
| fileScopeMustChange (`config/p-success-weights.json`) | PASS — present, `trained_sample_count: 40`, `min_training_samples: 30` |
| fileScopeMustNotChange (`.pi/extensions/smart-router/**`) | PASS — no changes in diff |

### Completion criteria

1. **Dogfood trained weights (≥30 samples)** — `config/p-success-weights.json` with provenance `synthetic_fixture` / SP-175; fixture at `scripts/fixtures/p-success-synthetic-train.jsonl` (feature vectors + labels only).
2. **Trained scores + safe fallback** — pipeline tests assert non-neutral scores when weights load and neutral `0.5` when missing; isotonic gap documented (logistic-only dogfood until calibration bundle).
3. **Raw vs used P(success)** — explain/telemetry fields `p_success_raw` / `p_success_calibrated` covered in `tests/unit/router-pipeline.test.ts`.
4. **Operator train/reload docs** — README P(success) section + `npm run routing:train-p-success`; no prompt text in artifacts.

```json
{"verdict":"PASS","feedback":"Completion criteria met: shipped config/p-success-weights.json (trained_sample_count=40), README train/reload docs without prompt text, raw vs calibrated explain fields, safe fallback, contract checks PASS. Worker may create .DONE."}
```
