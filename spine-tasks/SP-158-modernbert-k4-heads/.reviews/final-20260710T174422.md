### Verdict: PASS

### Summary

SP-158 delivers the ModernBERT K=4 capability heads module with independent sigmoid heads on [CLS] for reasoning, code_gen, tool_use, and debugging, plus ONNX runtime integration via `@huggingface/transformers`. The `hydra_heads` config flag (`learned_projection` | `modernbert_k4`) is wired through `HydraConfigSchema` and defaults; unit tests cover K=4 output shape, [0,1] bounds, schema validation, and mocked ONNX predictor paths. Machine contract checks pass, `router-pipeline.ts` is untouched, and `npm run verify:ci` succeeds (1427 tests, coverage gate passed; `modernbert-heads.ts` at 90.24% line coverage).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run verify:ci`) | PASS — build, typecheck, lint, test, coverage:check all succeeded |
| fileScopeMustChange (`modernbert-heads.ts`) | PASS — new 323-line module |
| fileScopeMustNotChange (`router-pipeline.ts`) | PASS — zero diff lines |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| K=4 sigmoid heads module on ModernBERT [CLS] | Met |
| Head output shape tests | Met — 16 tests in `tests/unit/modernbert-heads.test.ts` |
| Config flag for head mode selection | Met — `HydraHeadsSchema` + `defaults.ts` |
| `npm run verify:ci` passes | Met |

### Build / typecheck

Command run: `npm run verify:ci`

```
Test Files  78 passed (78)
     Tests  1427 passed (1427)
modernbert-heads.ts | 90.24% line coverage
```

### Scope and constraints

- Module-only deliverable; no matcher wiring (deferred to SP-159) — confirmed.
- Allowed companion changes: `schemas.ts`, `defaults.ts`, `modernbert-heads.test.ts` — all in File Scope.

```json
{"verdict":"PASS","feedback":"ModernBERT K=4 heads module, hydra_heads config flag, and shape/ONNX tests are complete. Contract checks pass; verify:ci green; router-pipeline untouched."}
```
