### Verdict: PASS

### Summary

SP-173 wires `resolveOperatorConfigFromEnv` into the live extension path: `createSmartRouterRuntime` builds an operator-aware `SessionPinner`, and `createDispatchOptions` passes `saarConfig`, `planningDelegateConfig`, `pinOnlyFallback`, and optional catalog/quota fields into the router. Machine contract verification passed; scoped typecheck + vitest (104 tests) passed independently; must-not-change paths (`router-pipeline.ts`, `README.md`, `session-pinner.ts`) are untouched. Unit and integration tests prove SAAR/planning env knobs affect dispatch options, `pin_only_fallback` defaults to false, and is honored when configured.

### Contract verification

Machine verifier: PASS
- OK testCommand: testCommand passed
- OK fileScopeMustChange: `.pi/extensions/smart-router/extension-setup.ts`, `.pi/extensions/smart-router/fleet-bootstrap.ts`
- OK fileScopeMustNotChange: `src/domain/pipeline/router-pipeline.ts`, `README.md`

Independent re-run: `npm run typecheck && npx vitest run tests/unit/smart-router-extension.test.ts tests/integration/pi-extension.test.ts` — typecheck OK; 104 tests passed.

### Completion criteria

- [x] Extension loads operator config into SessionPinner and gateway dispatch options (`extension-setup.ts`, `fleet-bootstrap.ts`)
- [x] Documented SAAR / planning-delegate env vars affect live path (test-proven in unit + integration)
- [x] `pin_only_fallback` honored when set; default false
- [x] Integration/unit coverage for wiring

```json
{"verdict":"PASS","feedback":"Operator SAAR/planning/pin-only wiring is complete and test-proven. Contract checks PASS; scoped typecheck+vitest 104/104; must-not-change paths clean. Worker may keep .DONE."}
```
