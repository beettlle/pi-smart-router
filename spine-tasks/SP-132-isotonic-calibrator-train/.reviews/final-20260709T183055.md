### Verdict: PASS

### Summary

SP-132 completion criteria are satisfied. The branch adds an in-repo PAV isotonic calibrator (`scripts/lib/isotonic-calibrator.ts`), integrates it into bundle v2 training with holdout ECE logging, extends the JSON schema and verify script, and includes unit tests for monotonicity and edge cases. Machine contract verification passed, `router-pipeline.ts` was not modified, and all build/test/coverage gates pass independently.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS (exit 0) |
| `fileScopeMustChange`: `scripts/train-routing-calibration.ts` | Changed |
| `fileScopeMustChange`: `scripts/lib/isotonic-calibrator.ts` | Created |
| `fileScopeMustNotChange`: `src/domain/pipeline/router-pipeline.ts` | No diff vs `main` |

### Completion criteria

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Isotonic artifact in versioned bundle | Met | `ROUTING_CALIBRATION_BUNDLE_VERSION = 2`; `isotonic_calibrator` in bundle schema, train output, and example config |
| Holdout ECE reported in train logs | Met | `trainRoutingCalibrationBundleWithMetrics` + stderr logging in `scripts/train-routing-calibration.ts:604-620` |
| Verify script passes with new artifact | Met | `validateIsotonicCalibratorArtifact` + lookup sanity check in `scripts/verify-routing-calibration.ts:212-230` |
| `npm run verify:ci` passes | Met | Ran independently; exit 0 |

### Build / typecheck

Commands run (all exit 0):

- `npm run typecheck && npm run lint && npm test` — 1231 tests passed (67 files)
- `npm run coverage:check` — 92.95% line coverage on `src/**` (≥77% threshold)
- `npm run verify:ci` — full CI pipeline passed

### Scope and deliverables

Committed changes vs `main` align with PROMPT.md:

- **Must change:** `scripts/lib/isotonic-calibrator.ts` (new PAV fit, ECE, lookup, validation), `scripts/train-routing-calibration.ts` (bundle v2 integration, holdout ECE logging)
- **May change (appropriate):** schema, verify script, example config, calibration-aggregate sync, unit tests
- **Must NOT change:** `src/domain/pipeline/router-pipeline.ts` — respected (online lookup deferred to SP-133 per Do NOT)

### Testing

- `tests/unit/isotonic-calibrator.test.ts` — 8 cases: PAV monotonicity, lookup clamping, ECE bounds, deterministic holdout split, insufficient-data default, full fit with holdout ECE, valid default artifact, invalid knot rejection
- `tests/unit/train-routing-calibration.test.ts` — bundle v2 serialization and isotonic integration with labeled rows

Review Level 2 scrutiny met; no blocking gaps for final land.

### Suggestions (non-blocking)

1. **`vitest` coverage config** — `scripts/lib/isotonic-calibrator.ts` is tested but excluded from the coverage report (`src/**` only). Consider extending `coverage.include` if script modules remain long-lived deliverables.
2. **`scripts/verify-routing-calibration.ts:224-229`** — Negative-path verify failure for invalid isotonic knots is not directly unit-tested.

```json
{"verdict":"PASS","feedback":"All SP-132 completion criteria and contract checks satisfied: isotonic calibrator module, bundle v2 with holdout ECE logging, schema/verify integration, unit tests, verify:ci and coverage gates pass; pipeline unchanged."}
```
