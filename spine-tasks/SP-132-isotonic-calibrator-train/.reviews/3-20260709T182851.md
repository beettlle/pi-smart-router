### Verdict: APPROVE

### Summary

Step 3 deliverables are complete and aligned with PROMPT.md. The verify script validates and applies the isotonic calibrator artifact, dedicated unit tests cover PAV monotonicity, lookup clamping, ECE, holdout splitting, and validation edge cases, and all required gates pass. Changes stay within file scope and `router-pipeline.ts` is untouched.

### Build / typecheck

Commands run (all exit 0):

- `npm run typecheck && npm run lint && npm test`
- `npm run coverage:check` (92.95% line coverage on `src/**`)
- `npm run verify:ci` (build, typecheck, lint, test, coverage:check)

1231 tests passed across 67 files.

### Scope and diff

Changed paths vs `main` (committed on branch `task/spine-lane-1-20260709T182448`):

- `scripts/lib/isotonic-calibrator.ts` (new)
- `scripts/train-routing-calibration.ts`
- `scripts/verify-routing-calibration.ts`
- `scripts/calibration-aggregate.{ts,js,d.ts}`
- `specs/.../routing-calibration.schema.json`
- `config/routing-calibration.json.example`
- `tests/unit/isotonic-calibrator.test.ts` (new, 8 cases)
- `tests/unit/train-routing-calibration.test.ts` (isotonic integration + bundle v2)

`src/domain/pipeline/router-pipeline.ts` — no diff (contract respected).

### Step 3 checklist

| Requirement | Status |
|-------------|--------|
| `verify-routing-calibration.ts` loads and sanity-checks calibrator | `validateIsotonicCalibratorArtifact` + `applyIsotonicLookup` in `verifyArtifactShapes` (`scripts/verify-routing-calibration.ts:212-230`) |
| Unit tests for monotonicity and edge cases | `tests/unit/isotonic-calibrator.test.ts` |
| `npm run verify:ci` | Passed |

### Suggestions (non-blocking)

1. **`scripts/verify-routing-calibration.ts:224-229`** — The isotonic verify failure path (catch block) is not directly unit-tested; consider a negative-case test with invalid knots.
2. **`vitest.config.ts:10`** — Coverage includes only `src/**`; `scripts/lib/isotonic-calibrator.ts` is exercised by tests but excluded from the coverage report. Consider extending `coverage.include` if script modules become long-lived deliverables.

```json
{"verdict":"APPROVE","feedback":"Step 3 complete: verify script checks isotonic artifact, 8 unit tests cover monotonicity/edge cases, typecheck/lint/test/coverage/verify:ci all pass, scope and Must-NOT constraints respected."}
```
