### Verdict: PASS

### Summary

SP-054 satisfies all completion criteria: `SessionPinner` optionally persists pins through `StorePort`, the pi extension wires `createExtensionStore(cwd)` into `new SessionPinner({ store })`, and `restoreSessionPin` runs on session start so pins survive restart. Unit and integration tests cover persist, restore, break, router-path reload, and SQLite file persistence. Contract verification passed; `npm run typecheck && npm test` passes (729 tests). Forbidden files are untouched.

### Contract verification

Machine verifier: **PASS**

- OK `testCommand`: `npm run typecheck && npm test` — verified independently (41 files, 729 tests)
- OK `fileScopeMustChange`: `.pi/extensions/smart-router/index.ts` modified
- OK `fileScopeMustNotChange`: `src/domain/pipeline/router-pipeline.ts` unchanged

### Build / typecheck

Commands run:

- `npm run typecheck` — pass
- `npm test` — pass (41 files, 729 tests)

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Pins survive pi restart within same project/db path | Met — `restoreSessionPin` + SQLite file cross-instance test |
| Extension wiring uses SQLite-backed pinner when store is active | Met — `new SessionPinner({ store })` with `createExtensionStore(cwd)` |
| Tests pass | Met — 729/729 |

### Implementation review

- **`src/domain/pinning/session-pinner.ts`** — Optional `StorePort` in config; `persistPin`/`deletePersistedPin` on record/break/load; `restoreSessionPin` hydrates in-memory Map for synchronous `lookupPin`; in-memory fallback preserved when no store.
- **`.pi/extensions/smart-router/index.ts`** — Extension passes store into `SessionPinner`; calls `restoreSessionPin(sessionId)` on session hook so pins reload after restart.
- **Tests** — `tests/unit/session-pinner.test.ts` (4 SP-054 cases); `tests/integration/session-pinning.test.ts` (extension-path reload via `createRouterFromFleet` + `restoreSessionPin`, SQLite file persistence).

### Coverage

`npm run coverage:check` is not defined in `package.json` (pre-existing project gap). At Review Level 2, changed paths have direct unit and integration test coverage for all new behavior; acceptable given contract `testCommand` is typecheck + test only.

### Suggestions (non-blocking)

1. Add `coverage:check` script and `@vitest/coverage-v8` so spine coverage gate is automatable.
2. Consider a `tests/integration/pi-extension.test.ts` case that imports the extension entrypoint to exercise the bootstrap `restoreSessionPin` hook directly.

```json
{"verdict":"PASS","feedback":"All SP-054 completion criteria met: StorePort-backed SessionPinner, extension wiring with restoreSessionPin on session start, and integration tests confirming pin survival across simulated reload. Contract checks pass; typecheck and 729 tests green. Forbidden files untouched."}
```
