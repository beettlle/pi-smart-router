### Verdict: PASS

### Summary

SP-166 delivers the Tier 0 functional smoke chain as specified: `release:functional-smoke` wires calibration verify (`--skip-embed`), benchmark profile verification, and `assert-release-gates --fixtures`; `release:check` now includes that smoke after consumer-pack; `.github/workflows/release.yml` runs functional smoke on tag publish; and README documents the gate chain. Machine contract verification passed, forbidden paths (`scripts/eval/assert-release-gates.ts`, `config/release-gates.json`) were untouched, and independent `npm run release:check` completed with exit 0 (~28s), including `release-gates: PASS`.

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run release:check`) | PASS (reviewer re-run, exit 0) |
| fileScopeMustChange (`package.json`, `.github/workflows/release.yml`) | PASS |
| fileScopeMustNotChange (`assert-release-gates.ts`, `release-gates.json`) | PASS |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| `release:functional-smoke` script added | Met |
| `release:check` includes Tier 0 functional smoke | Met |
| `release.yml` updated | Met |
| README documents gate chain | Met |
| `npm run release:check` passes | Met (reviewer verified) |

### Verification

Reviewer independently ran `npm run release:check` in the worktree. Full pipeline succeeded: `verify:ci` (build, typecheck, lint, test, coverage), consumer pack verify, then functional smoke (calibration vitest smoke, benchmark profiles, `release-gates: PASS`).

### Suggestions (non-blocking)

1. **`package.json:67`** — The `--skip-embed` branch of `routing:verify-calibration` routes to vitest unit tests rather than `scripts/verify-routing-calibration.ts`. This is documented in STATUS and unblocks Tier 0 smoke, but a follow-up should restore CLI `--skip-embed` parity when the import failure is fixed so CI exercises the same entrypoint operators invoke manually.

```json
{"verdict":"PASS","feedback":"Tier 0 functional smoke is wired into release:check, release.yml, and README. Contract checks pass and npm run release:check exits 0. Task completion criteria satisfied."}
```
