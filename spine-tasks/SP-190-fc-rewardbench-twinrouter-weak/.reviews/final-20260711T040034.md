### Verdict: PASS

### Summary

SP-190 delivers the FC-RewardBench offline converter, optional TwinRouterBench weak-label path, tiny CI fixtures, provenance pins/policy, and unit tests within File Scope. Machine contract verification passed; independent re-run of `npm run typecheck` plus vitest for FC/schema/weak ingest suites is green (15/15). Pack outputs are privacy-safe (no conversation/prompt/call bodies), fixtures are tiny (no full HF corpora), and must-not-change paths are untouched.

### Contract verification

| Check | Result |
|-------|--------|
| Machine verifier | PASS |
| `testCommand` (re-run) | PASS — typecheck + `ingest-fc-rewardbench-labels` + `label-pack-schema` (+ weak tests) |
| `fileScopeMustChange` | PASS — `scripts/ingest-fc-rewardbench-labels.ts`, `tests/unit/ingest-fc-rewardbench-labels.test.ts` |
| `fileScopeMustNotChange` | PASS — no diff on `config/release-gates.json`, `src/domain/pipeline/router-pipeline.ts` |
| Completion criteria | PASS — see below |

### Completion criteria

1. **FC-RewardBench ingest offline + tested** — converter + `fc-rewardbench/ci-fixture.jsonl` + 6 unit tests; preference pairs → correct/incorrect pack rows; skips unmappable; taint stripped.
2. **TwinRouterBench weak-label path** — `scripts/ingest-twinrouterbench-weak-labels.ts` + fixture + tests; `weak_tier_proxy` / `exclude_from_holdout_ece` documented in PROVENANCE.
3. **Provenance updated** — FC pin `269929c…` / Apache-2.0; weak-label policy and holdout ECE exclusion documented.
4. **No prompt leakage** — pack JSONL lacks conversation/chosen_output/PLACEHOLDER text; tests assert `serializedPackContainsPromptLeakage`.
5. **No full datasets vendored** — fixtures ~1–2KB only under `label-packs/`.
6. **Contract green** — machine + local re-run.

### Suggestions (non-blocking)

1. **`scripts/lib/label-pack-schema.ts` → `calibration-aggregate.js` side-effect** — CLI ingest scripts (including SP-189 swe-gym) print `calibration-aggregate failed: Unknown argument: --input` and may exit non-zero after successful write. Pre-existing; fix by guarding aggregate `isMain` or importing types-only. Not required for SP-190 completion.

```json
{"verdict":"PASS","feedback":"Completion criteria and contract checks satisfied. FC-RewardBench converter, TwinRouterBench weak-label path, provenance, privacy-safe fixtures, and unit tests are in scope and verified. Machine verifier PASS; typecheck + ingest unit suites green. Worker may create .DONE."}
```
