### Verdict: PASS

### Summary
The `TextEmbedder` implementation now correctly disposes of resources using the strongest available release handle (`pipeline.dispose()` or `model.dispose()`). The new lifecycle tests properly verify this behavior, ensuring it fails closed on subsequent `embed()` calls and avoids silent resource leaks. All tests pass, typechecks are clean, and coverage goals are met.

```json
{"verdict":"PASS","feedback":"All completion criteria and contract requirements have been met. Safe to proceed."}
```