### Verdict: REVISE

### Summary

Source changes correctly implement Option B: the middleware ghost layer is removed (`getLastDecision`, no-op `context` hook, and `void options.fleet` are gone), lifecycle hooks remain functional, README documents extension vs library embedder paths, and tests assert the narrowed hook surface. Build and test gates pass locally. However, Step 4 requires `npm run build` when the public API changes, and committed `dist/` artifacts still expose the removed API — the rebuilt output exists only as uncommitted working-tree changes.

### Build / typecheck

Commands run:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — pass (41 files, 729 tests)
- `npm run build` — pass (`tsc --project tsconfig.build.json`)

### Coverage

Command: `npm run coverage:check` (from `.spine/spine-config.json`)

Result: **Not runnable** — script missing from `package.json`; `@vitest/coverage-v8` not installed (pre-existing project gap). Manual inspection of changed paths:

| Changed path | Test coverage |
|---|---|
| `src/api/middleware/pi-router-middleware.ts` | `tests/unit/pi-router-middleware.test.ts` — compaction/model_select lifecycle hooks, shared `LifecycleHookState` |
| `src/index.ts` | `tests/unit/create-router.test.ts`, `tests/unit/fleet-factory.test.ts` — register hook list, no `context`/`getLastDecision` |
| `README.md` | Documentation-only; no test required |
| Integration callers | `tests/integration/pi-extension.test.ts`, `tests/integration/session-pinning.test.ts` — updated for lifecycle-only middleware |

Estimated line coverage on changed logic exceeds the 77% threshold. Acceptable at Review Level 2 given PROMPT `testCommand` is typecheck + test only.

### Scope and contract

- **Must change:** `src/api/middleware/pi-router-middleware.ts`, `src/index.ts` — implemented
- **May change:** `README.md`, middleware/factory/integration tests — updated appropriately
- **Must NOT change:** `src/domain/pipeline/router-pipeline.ts` — no diff
- **Completion criteria (source):** ghost layer resolved; embedder path documented; tests updated — met in `src/` and `README.md`

### Blocking issues

1. **`dist/api/middleware/pi-router-middleware.d.ts` (committed HEAD)** — Public package types still declare `getLastDecision`, required `fleet` in `PiRouterMiddlewareOptions`, and stale middleware JSDoc; `npm run build` was run locally but `dist/` was not committed. Consumers importing from the tracked `dist/` export surface get the pre-SP-055 API.

2. **`dist/index.d.ts` / `dist/index.js` (committed HEAD)** — Factory output still passes `{ fleet, lifecycleHookState }` into `createPiRouterMiddleware`; does not match `src/index.ts` after SP-055. Rebuild artifacts are present only as uncommitted changes (20 files under `dist/`, including unrelated SP-053/SP-054 drift).

```json
{"verdict":"REVISE","feedback":"Source and tests satisfy SP-055 middleware cleanup, but Step 4 build verification is incomplete: committed dist/ still exposes removed getLastDecision and fleet option. Run npm run build and commit refreshed dist/ (or document why dist is intentionally excluded) before Step 4 can pass."}
```
