### Verdict: PASS

### Summary

SP-055 completion criteria are satisfied. The middleware ghost layer is resolved: `getLastDecision`, the no-op `context` hook, and `void options.fleet` are removed; `createPiRouterMiddleware()` now registers only functional lifecycle hooks (compaction and `model_select`). README documents the extension vs library embedder paths, exports and tests are updated, and committed `dist/` matches the narrowed public API. Contract verification passed, and build/typecheck/test gates pass independently.

### Contract verification

Machine verifier: **PASS**

- `testCommand`: `npm run typecheck && npm test` — pass (729 tests)
- `fileScopeMustChange`: `src/api/middleware/pi-router-middleware.ts` — changed
- `fileScopeMustNotChange`: `src/domain/pipeline/router-pipeline.ts` — no source diff

### Build / typecheck

Commands run during final review:

- `npm run typecheck` — pass
- `npm test` — pass (41 files, 729 tests)
- `npm run build` — pass (`tsc --project tsconfig.build.json`)

Committed `dist/` no longer exposes `getLastDecision` or required `fleet` in middleware options (prior Step 4 REVISE item resolved in commits `9e4b8e6` and `9c939f1`).

### Completion criteria

| Criterion | Status |
|-----------|--------|
| No ghost-layer no-op middleware API | Met — only lifecycle hooks registered; no routing/context/before_provider_request handlers |
| Embedder integration path documented | Met — README "Embedder integration paths" table and lifecycle-only middleware note |
| Exports and tests updated | Met — `src/index.ts`, middleware unit tests, create-router/factory/integration tests |
| Tests and build pass | Met — verified above |

### Coverage note

`npm run coverage:check` is not defined in `package.json` (pre-existing project gap). PROMPT contract `testCommand` is typecheck + test only; changed middleware paths have dedicated unit and integration tests asserting non-no-op lifecycle behavior.

```json
{"verdict":"PASS","feedback":"SP-055 complete: middleware ghost layer removed, lifecycle hooks functional and tested, embedder paths documented, exports/dist aligned, contract checks and build/test gates pass."}
```
