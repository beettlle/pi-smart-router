### Verdict: PASS

### Summary

SP-085 delivers the inverted-economics fix end-to-end: Gemini 3.x Pro models map to `frontier-cloud`, `turnEnvelope` and sub-route policy select the lowest-cost economical candidate instead of first-match, and `estimated_cost_usd` is populated on `turn_envelope`, `session_pin`, and `hydra_match` decisions via `estimateRoutingCost`. Contract checks passed, `route-and-delegate.ts` was not modified, and `npm run typecheck && npm test` completed with 871 passing tests.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` | PASS — `npm run typecheck && npm test` (871 tests) |
| `fileScopeMustChange` | PASS — `src/config/pi-model-mapper.ts` updated |
| `fileScopeMustNotChange` | PASS — `.pi/extensions/smart-router/route-and-delegate.ts` unchanged |

### Build / typecheck

- **Command:** `npm run typecheck && npm test`
- **Result:** PASS — `tsc --noEmit` clean; Vitest 50 files / 871 tests passed

### Completion criteria

| Criterion | Status | Evidence |
|-----------|--------|----------|
| `gemini-3.1-pro-preview` → `frontier-cloud` | Met | `src/config/pi-model-mapper.ts:108-109`; `tests/unit/pi-model-mapper.test.ts:91-97` |
| `tool_result` routes economical, not pro | Met | `turnEnvelope` uses `selectLowestCostModel` (`router-pipeline.ts:475-478`); sub-route policy (`sub-route-policy.ts:107`); tests in `router-pipeline.test.ts` and `turn-envelope.test.ts` |
| `estimated_cost_usd` populated when pricing available | Met | `withEstimatedCost` on `turn_envelope`, `session_pin`, `hydra_match` (`router-pipeline.ts:222-234, 400, 418, 486, 562`); `estimateRoutingCost` in `routing-telemetry.ts:27-35`; tests at `router-pipeline.test.ts:669-770` |
| Mapper and turn-envelope tests | Met | `pi-model-mapper.test.ts`, `router-pipeline.test.ts`, `turn-envelope.test.ts`, `full-pipeline.test.ts` |
| Tests pass | Met | Independent gate run succeeded |

```json
{"verdict":"PASS","feedback":"All SP-085 completion criteria and contract checks satisfied. Mapper tier fix, cost-aware turn envelope/sub-route selection, and estimated_cost_usd telemetry are implemented with unit and integration coverage; build and tests pass."}
```
