### Verdict: PASS

### Summary

SP-150 completion criteria are satisfied. `README.md` adds a comprehensive Virtual cost v2 operator section covering λ decay, window position (`quotaWindowPosition` / `deriveRemainingWindowFraction`), cache credit, operator knobs, deterministic-only scope (no MDP), and dogfood/explain verification steps. Regression tests in `tests/unit/expected-cost.test.ts` and `tests/unit/virtual-cost-v2.test.ts` cover late-window economical-tier preference and cache-credit pin preservation respectively. Machine contract checks passed; independent `npm run verify:ci` run succeeded (build, typecheck, lint, 1344 tests, coverage check). `src/domain/pipeline/router-pipeline.ts` was not modified.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`npm run verify:ci`) | PASS (reviewer re-run) |
| `fileScopeMustChange` (`README.md`) | PASS |
| `fileScopeMustNotChange` (`router-pipeline.ts`) | PASS |

### Build / typecheck

Command: `npm run verify:ci`

- Build (`tsc --project tsconfig.build.json`): passed
- Typecheck (`tsc --noEmit`): passed
- Lint: passed
- Tests: 74 files, 1344 tests passed
- Coverage check: passed

```json
{"verdict":"PASS","feedback":"README documents virtual cost v2 operator knobs (λ decay, window position, cache credit, deterministic-only scope) with dogfood/explain steps. Regression tests cover late-window economical preference and cache-credit pin hold. verify:ci passes; contract checks satisfied; router-pipeline.ts untouched."}
```
