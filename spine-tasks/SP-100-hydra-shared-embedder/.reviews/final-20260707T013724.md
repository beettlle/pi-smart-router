### Verdict: PASS

### Summary

SP-100 successfully extracts the MiniLM ONNX embedding pipeline into `embedding-provider.ts` with a reusable `TextEmbedder` API, refactors `hydra-matcher.ts` to consume it via `wrapHydraEmbeddingProvider`, and adds focused unit tests for embedder isolation and shared dispose lifecycle. Contract verification passed, `router-pipeline.ts` was untouched, and independent `npm run verify:ci` completed with 962 tests passing and in-scope module line coverage well above 77%.

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Shared embedder extracted (`embedding-provider.ts`) | Met |
| HyDRA matcher uses shared provider | Met |
| `dispose()` shares lifecycle | Met (`wrapHydraEmbeddingProvider` delegates; `HydraMatcher.dispose` chains) |
| Provider exported for cluster-matcher consumption | Met (`createOnnxTextEmbedder`, `TextEmbedder`, `EMBEDDING_DIM` exported from matching module) |
| Existing HyDRA tests pass unchanged behavior | Met (32 tests in `hydra-matcher.test.ts`) |
| Embedder isolation unit tests | Met (`embedding-provider.test.ts`, 4 tests with mocked ONNX) |
| `npm run verify:ci` passes | Met |

### Contract verification

Machine verifier: **PASS**
- OK `testCommand`: `npm run verify:ci` passed (build, typecheck, lint, 962 tests, coverage check)
- OK `fileScopeMustChange`: `src/domain/matching/hydra-matcher.ts` modified
- OK `fileScopeMustNotChange`: `src/domain/pipeline/router-pipeline.ts` unchanged

### Build / verification

Command run: `npm run verify:ci` — exit 0. Coverage on changed modules: `embedding-provider.ts` 85.71% lines, `hydra-matcher.ts` 95.15% lines.

```json
{"verdict":"PASS","feedback":"Shared TextEmbedder extracted to embedding-provider.ts; hydra-matcher refactored with wrapHydraEmbeddingProvider for shared lifecycle; unit tests and verify:ci pass; contract checks satisfied."}
```
