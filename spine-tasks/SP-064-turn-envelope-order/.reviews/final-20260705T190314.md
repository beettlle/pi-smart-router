### Verdict: PASS

### Summary

SP-064 is complete. The pipeline stages array in `router-pipeline.ts` now runs `turn_envelope` before `session_pin`, with a `persistPinIfNeeded` guard so envelope decisions do not overwrite existing pins. Unit and integration tests assert planning-over-economical-pin routes frontier (`stage: turn_envelope`, `reason_code: turn_planning`), tool_result downgrade with frontier pin, pin preservation, and loop-escalation interaction; SP-050 ordering (`local_zero` before triage cloud fallback) is unchanged. Contract verification passed; build gate passes.

### Contract verification

Machine verifier: **PASS**
- OK `testCommand`: `npm run typecheck && npm test` passed
- OK `fileScopeMustChange`: `src/domain/pipeline/router-pipeline.ts` modified
- OK `fileScopeMustNotChange`: `.pi/extensions/smart-router/index.ts` unchanged in SP-064 commits

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Planning turn with economical pin routes frontier via `turn_envelope` | Met — unit, session-pinning, and full-pipeline tests |
| Telemetry `stage: turn_envelope`, `reason_code: turn_planning` | Met — asserted in SP-064 tests |
| Tool_result downgrade when pinned to frontier | Met — `turn_tool_result` + economical tier asserted |
| Pin/cache tests pass with updated semantics | Met — 787 tests pass |
| SP-050 triage/local-zero ordering preserved | Met — only envelope/pin order swapped |

### Build / typecheck

Commands run: `npm run typecheck && npm run lint && npm test`

- **typecheck:** pass
- **lint:** pass
- **test:** pass — 45 files, 787 tests

### Coverage

`npm run coverage:check` is referenced in `.spine/spine-config.json` but not defined in `package.json`; PROMPT.md specifies `npm run typecheck && npm test`. Changed-path behavior is covered by new and updated tests in `tests/unit/router-pipeline.test.ts`, `tests/integration/session-pinning.test.ts`, and `tests/integration/full-pipeline.test.ts`.

```json
{"verdict":"PASS","feedback":"Global turn_envelope-before-session_pin reorder is implemented with pin-preservation guard; acceptance tests verify telemetry and downgrade semantics; contract checks and full test suite pass."}
```
