### Verdict: APPROVE

### Summary

Step 3 verification is complete. The global reorder of `turn_envelope` before `session_pin` in `router-pipeline.ts` is implemented correctly, with a guard in `persistPinIfNeeded` so envelope decisions do not overwrite existing pins. Unit and integration tests cover planning-over-economical-pin (frontier via `turn_planning`), tool_result downgrade with frontier pin, pin preservation, and loop-escalation interaction; SP-050 stage ordering (`local_zero` before triage cloud fallback) is unchanged. Build gate `npm run typecheck && npm run lint && npm test` passes (787 tests).

### Build / typecheck

Commands run: `npm run typecheck && npm run lint && npm test`

- **typecheck:** pass
- **lint:** pass (including `models.yaml.example` validation)
- **test:** pass — 45 files, 787 tests

### Coverage

`npm run coverage:check` is referenced in `.spine/spine-config.json` but is not defined in `package.json`; PROMPT.md specifies `npm run typecheck && npm test` only. Changed-path coverage is exercised by new and updated tests in `tests/unit/router-pipeline.test.ts`, `tests/integration/session-pinning.test.ts`, and `tests/integration/full-pipeline.test.ts` asserting `stage`, `reason_code`, `tier`, and pin preservation for SP-064 scenarios.

### Scope

- **Must change:** `src/domain/pipeline/router-pipeline.ts` — stage reorder + `persistPinIfNeeded` guard
- **May change:** test files updated as expected
- **Must NOT change:** `.pi/extensions/smart-router/index.ts` — no diff vs `main`

```json
{"verdict":"APPROVE","feedback":"Stage reorder and pin-preservation guard are correct; SP-064 acceptance tests assert turn_envelope telemetry (turn_planning / turn_tool_result) and pin semantics; build and full test suite pass."}
```
