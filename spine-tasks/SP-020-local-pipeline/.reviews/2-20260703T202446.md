# Code Review — SP-020 Step 2: Testing and verification

### Verdict: APPROVE

### Summary

The diff integrates hardware probe (Step 1) and local zero-tier (Step 4) into `RouterPipeline`, enforcing SC-007 (classification_only MUST NOT dispatch full local). All 341 tests pass and typecheck succeeds. Tests cover the required T046 (Steps 1+4 integration) and T047 (8GB/16GB/battery/unreachable) scenarios thoroughly. Changes are within declared File Scope with no modifications to `src/domain/matching/`.

### Build / typecheck

```
npm run typecheck → tsc --noEmit → exit 0
npm test → vitest run → 341 passed (19 test files), exit 0
```

### Coverage gate

`npm run coverage:check` is not available (script missing from `package.json`; `@vitest/coverage-v8` not installed). This is a pre-existing project gap, not introduced by this task. Cannot run coverage gate; noting as non-blocking since the infrastructure does not exist.

### Contract verification

| Contract field | Status |
|---|---|
| `fileScopeMustChange: src/domain/pipeline/router-pipeline.ts` | Changed ✓ |
| `fileScopeMustNotChange: src/domain/matching/**` | No changes ✓ |
| `completionCriteria: Classification-only MUST NOT dispatch full local; SC-007` | Enforced at `router-pipeline.ts:112` — `currentHardwareResult !== 'full_local'` guard. Tested in 8GB classification_only test (line 269). ✓ |
| `testCommand: npm run typecheck && npm test` | Passes ✓ |

### Diff analysis

**`src/domain/pipeline/router-pipeline.ts`** — Added `PipelineOptions` interface for dependency injection of hardware config, local config, system info provider, and HTTP fetch port. `hardwareProbeStage` runs `probeHardware()` and stores result in transient `currentHardwareResult` (reset each `route()` call). `localZeroTierStage` gates on `full_local` only, then checks service readiness and fleet availability before routing. Clean early-return pattern with three fallthrough guards.

**`tests/unit/local-zero-tier.test.ts`** — Added 12 integration tests covering: 16GB full_local routing, 8GB classification_only rejection (SC-007), battery-low-on-battery disabled, battery-low-on-AC-power allowed, unreachable services fallback, no zero-tier model fallback, non-darwin platform disabled, non-arm64 arch disabled. All use injected ports (no real network calls).

### Suggestions (non-blocking)

1. **`router-pipeline.ts:42`** — `currentHardwareResult` is shared mutable state between stages on the instance. If `route()` were ever called concurrently on the same pipeline instance, this would race. Currently safe because the pipeline is sequential and JS is single-threaded, but a per-invocation context object would be more robust for future async changes.

2. **Coverage infrastructure** — The `testWithCoverage` script referenced in `.spine/spine-config.json` (`npm run coverage:check`) does not exist. Consider adding `@vitest/coverage-v8` and the corresponding script to enable coverage gating.

```json
{"verdict":"APPROVE","feedback":"Clean integration of hardware probe + local zero-tier into RouterPipeline. SC-007 properly enforced — classification_only cannot dispatch full local. All 341 tests pass, typecheck clean. File scope respected. Coverage gate could not run (pre-existing gap — coverage tooling not installed)."}
```
