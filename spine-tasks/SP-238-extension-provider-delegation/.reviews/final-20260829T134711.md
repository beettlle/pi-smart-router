### Verdict: PASS

### Summary
The delegation resolution fix is implemented correctly in `.pi/extensions/smart-router/delegate-stream.ts`, routing stream requests through `registry.getProvider` with a proper fallback to `compatDelegateStream`. The provided unit tests cover all necessary scenarios, including the extension provider resolution, built-in fallback, and override precedence. The scope restrictions and completion criteria have been fully satisfied.

```json
{"verdict":"PASS","feedback":"All completion criteria and contract checks are fully satisfied. The fix is accurate and safely scoped. The `release:check` test flakes (pi-model-scope and training calibration) are acknowledged as out-of-scope and documented pre-existing conditions."}
```