### Verdict: PASS

### Summary

SP-159 completes the hydra matcher integration for ModernBERT K=4 heads while preserving the SP-115 learned-projection and placeholder paths. `createHydraEmbeddingProvider` branches on `hydra_heads`, routes `modernbert_k4` through `wrapModernBertHeadsEmbeddingProvider` (dropping the debugging dimension before shortfall), and keeps `learned_projection` on the existing ONNX + linear projection path. Migration guidance is documented in `config/operator-config.json.example`, and unit tests cover both K=4 and learned paths including factory wiring from hydra config.

Machine contract verification passed (`testCommand`, `fileScopeMustChange`, `fileScopeMustNotChange`). Independent `npm run verify:ci` succeeded (51 hydra-matcher tests; `hydra-matcher.ts` at 95.9% line coverage). `src/domain/pipeline/router-pipeline.ts` was not modified.

```json
{"verdict":"PASS","feedback":"K=4 heads wired via hydra_heads branching; SP-115 and placeholder paths preserved; migration documented; dual-path tests and verify:ci pass; contract checks satisfied."}
```
