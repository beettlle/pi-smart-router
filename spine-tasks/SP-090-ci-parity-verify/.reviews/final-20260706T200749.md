### Verdict: PASS

### Summary

SP-090 delivers a `verify:ci` script in `package.json` that mirrors `.github/workflows/ci.yml` step order (build → typecheck → lint → test → coverage:check), and updates the orchestrator skill, spine-autonomous-operator skill, and packet template to require full CI-parity verification before success. Machine contract checks passed, `src/domain/pipeline/router-pipeline.ts` was untouched, and independent `npm run verify:ci` exited 0 (893 tests, 93.47% line coverage).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand (`npm run verify:ci`) | PASS — exited 0 |
| fileScopeMustChange (`package.json`) | PASS — `verify:ci` script added |
| fileScopeMustNotChange (`src/domain/pipeline/router-pipeline.ts`) | PASS — no diff vs main |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| `verify:ci` mirrors CI order | PASS — matches `ci.yml` steps 28–41 |
| Orchestrator requires full verify before Phase 8 success | PASS — Phase 6 land loop and Phase 8 block on `verify:ci` |
| spine-autonomous-operator post-integrate verify aligned | PASS — land loop and Phase 6 use `npm run verify:ci` |
| Packet template default `testCommand` updated | PASS — `packet-from-issue.md` defaults to `npm run verify:ci` |
| `npm run verify:ci` passes | PASS — verified in this review |

```json
{"verdict":"PASS","feedback":"All completion criteria and contract checks satisfied. verify:ci mirrors CI order; skills and packet template require full verify before orchestrator success; verify:ci passes with 893 tests."}
```
