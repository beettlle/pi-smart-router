# Plan Review — Step 0: Preflight (SP-275)

### Verdict: APPROVE

### Summary

The Step 0 plan is complete and well-evidenced: the domain→infra import inventory covers 11 import sites across all three target ports, and the port file names map 1:1 to the mission (`HardwareProbePort`, `LocalRuntimePort`, `TelemetryEmitterPort`). The pricing coupling (`session-pinner.ts`, `expected-cost.ts` → `price-broker`) is correctly identified as out of scope (not one of the three named ports) and documented rather than silently expanded — consistent with the "Do NOT expand beyond the three ports without STATUS note" rule. The design notes (pure kernels move to domain, impure adapters stay in infra, `estimateRoutingCost` via an options seam) are sound and stay within File Scope (`src/domain/**`, infra adapters, wiring, tests).

### Alignment with PROMPT.md

- **Outcomes:** Inventory and port-file naming directly satisfy both Step 0 checkboxes.
- **File Scope:** Planned artifacts live under `src/domain/ports/` (Must change) with infra re-exports and composition-root wiring in the "May change" bucket. No forbidden paths touched.
- **Completion criteria:** The plan's direction (domain imports interfaces only; infra adapters wired at the root) matches "Domain no longer imports infra concrete modules for those ports."
- **Later steps enabled:** Step 1 has a concrete, dependency-ordered mapping (which symbol moves vs. which stays behind a seam) — no missing prerequisites identified.

### Suggestions (non-blocking)

1. The Step 0 design note moves `enrichRoutingDecision*` builders into the telemetry port module; when Step 1 lands, verify the STATUS amendment is reflected before the Step 1 checkpoint so the seam count stays auditable.
2. Consider an import-lint check (e.g., a test asserting `src/domain/**` has no `infrastructure/` imports for the three port areas) during Step 2 to make the inversion contract durable.

```json
{"verdict":"APPROVE","feedback":"Step 0 plan is thorough: full domain→infra import inventory, correct port file naming for all three ports, pricing coupling explicitly scoped out, and design seams documented within File Scope. No blocking issues."}
```
