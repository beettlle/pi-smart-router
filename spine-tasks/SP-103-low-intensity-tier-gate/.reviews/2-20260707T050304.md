### Verdict: APPROVE

### Summary

Step 2 adds five focused `low_intensity tier gate (SP-103)` tests in `tests/unit/router-pipeline.test.ts` that cover high-confidence low_stakes → zero-tier, ambiguous defer-band null hints, frontier cluster hints on features, HyDRA fleet constraint when local is unavailable, and structural reason codes. Build, lint, test, and coverage gates all pass (`npm run verify:ci` exit 0); `router-pipeline.ts` line coverage is 95.26%, well above the 77% threshold.

Supporting type/schema updates (`entities.ts`, `schemas.ts`, `defaults.ts`) and test fixture fixes are justified to attach `tier_hint` fields and configurable thresholds per completion criteria; `.pi/extensions/smart-router/index.ts` was not modified.

### Build / typecheck

Commands run (all passed):

- `npm run typecheck && npm run lint && npm test`
- `npm run coverage:check`
- `npm run verify:ci`

997 tests passed across 56 files. Coverage on changed module `src/domain/pipeline/router-pipeline.ts`: 95.26% lines.

### Scope and tests

| Step 2 criterion | Covered by |
|------------------|------------|
| High-confidence low_stakes → economical/zero tier hint | `sets zero-tier hint for high-confidence low_stakes cluster with full_local hardware`; `constrains HyDRA fleet to economical tier when local is not ready` |
| Ambiguous prompt → tier_hint null | `leaves tier_hint null for ambiguous prompts in the defer band` |
| tier_hint on decision features | `attaches tier_hint fields on every routing decision`; feature sidecar fields in `buildFeatures` |
| verify:ci | Confirmed passing in this review |

### Suggestions (non-blocking)

1. **`src/domain/types/schemas.ts:229-236`** — `LowIntensityConfigSchema` threshold ordering validation is uncovered; a small contract test would lock the `high_threshold > low_threshold` invariant.
2. **`src/domain/pipeline/router-pipeline.ts:767-774`** — `constrainFleetToTierHint` empty-filter fallback path is not exercised; a test with a tier hint but no matching fleet models would document the degrade-to-full-fleet behavior.
3. **`tests/unit/router-pipeline.test.ts`** — `high_intensity_structural` reason code is implemented but only `cluster_architecture` is asserted in the frontier test; a low-confidence high-intensity case would mirror the structural low-intensity test.

```json
{"verdict":"APPROVE","feedback":"Step 2 tests comprehensively cover SP-103 tier-gate behavior (high/low/ambiguous paths, feature attachment, HyDRA fleet constraint). All CI gates pass with 95%+ line coverage on router-pipeline.ts. Minor uncovered edge paths are non-blocking at Review Level 2."}
```
