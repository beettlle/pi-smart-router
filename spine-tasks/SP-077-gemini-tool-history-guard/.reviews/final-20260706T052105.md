### Verdict: PASS

### Summary

SP-077 delivers the Gemini tool-history routing guard end-to-end: `resolveEffectiveFleet` excludes Google/Gemini profiles when prior tool calls are detected, honors `force_model_id`, and is wired through `route-and-delegate.ts` into pipeline dispatch so safe-default, HyDRA matching, and pin lookup all use the filtered fleet. Telemetry (`reason_code: gemini_tool_history_excluded`), README cross-links (#37, #38, pi#6342), and unit/extension tests satisfy the PROMPT completion criteria; contract checks and `npm run typecheck && npm test` (834 tests) both pass.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` | PASS — `npm run typecheck && npm test` (834/834) |
| `fileScopeMustChange` | PASS — `.pi/extensions/smart-router/index.ts` exports guard helpers |
| `fileScopeMustNotChange` | PASS — `src/infrastructure/gateway/circuit-breaker.ts` unchanged in SP-077 commits |

### Build / typecheck

- `npm run typecheck` — exit 0
- `npm test` — 47 files, 834 tests passed

### Completion criteria

| Criterion | Status |
|-----------|--------|
| Tool-history sessions never select Google/Gemini unless `force_model_id` | Met — `resolveEffectiveFleet` + extension integration test |
| Non-tool sessions unchanged | Met — `leaves fleet unchanged` tests |
| Tests cover filter | Met — `tool-history-guard.test.ts` (13), extension tests (SP-077 block) |
| Telemetry `gemini_tool_history_excluded` | Met — `route-and-delegate.ts:104-111` |
| README cross-links | Met — `README.md:322-335` |

```json
{"verdict":"PASS","feedback":"Gemini tool-history guard is fully implemented: fleet filtering wired before dispatch (safe-default, HyDRA, pin paths via effectiveFleet), force_model_id override tested, telemetry and README docs present, contract checks and full test suite pass."}
```
