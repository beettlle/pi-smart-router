### Verdict: APPROVE

### Summary

Sub-route policy implementation (`sub-route-policy.ts`) is a clean, pure policy function that correctly enforces all four FR-024 rules: tool_result gate, size threshold, same-provider economical model lookup, and self-sub-route prevention. Tests in `turn-envelope.test.ts` provide thorough coverage across 24 cases spanning every branch and rejection reason, plus immutability and performance checks. Build and typecheck pass cleanly (488/488 tests, zero type errors).

### Build / typecheck

```
npm run typecheck && npm test
```

- `tsc --noEmit` — clean, no errors.
- `vitest run` — 24 test files, 488 tests passed (946ms).

### Scope verification

- **fileScopeMustChange** `src/domain/pinning/sub-route-policy.ts` — new file, changed ✅
- **fileScopeMustNotChange** `src/domain/matching/**` — no changes ✅
- **Do NOT** "Modify session-pinner break rules" — no session-pinner changes ✅
- Changed files: `src/domain/pinning/sub-route-policy.ts`, `tests/unit/turn-envelope.test.ts`, `spine-tasks/SP-023-sub-route-policy/STATUS.md` — all within File Scope.

### Coverage gate

Coverage tooling (`coverage:check` script, `@vitest/coverage-v8`) is not configured in this project. This is a pre-existing gap, not introduced by SP-023. Manual branch analysis of `sub-route-policy.ts` shows all 5 exit paths are exercised by the test suite (not_tool_result, payload_exceeds_threshold, pinned_model_not_in_fleet, no_same_provider_economical, eligible).

### Suggestions (non-blocking)

1. **Boundary test at exact threshold (2048):** The tests check 2000 (below) and 2100 (above) but not exactly 2048. The `>` comparison means 2048 is eligible — a test at the exact boundary would document this intentional behavior.
2. **Coverage tooling:** Consider adding `@vitest/coverage-v8` and the `coverage:check` script to `package.json` to enable automated coverage gates for future reviews.

```json
{"verdict":"APPROVE","feedback":"Clean pure-function implementation of FR-024 sub-route policy with comprehensive tests (24 cases, all branches covered). Build and typecheck pass. Scope respected. No blocking issues."}
```
