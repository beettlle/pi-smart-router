### Verdict: PASS

### Summary
The requested test suites have been successfully updated with suite-scoped and test-scoped `retry: 2` configurations alongside a generous `timeout` adjustment for the `pi-model-scope.test.ts` test. Contract checks have passed, no global retries were added to `vitest.config.ts`, and all load-sensitive assertions maintain their intended validation logic without skips or deletions. 

```json
{"verdict":"PASS","feedback":"All completion criteria met."}
```