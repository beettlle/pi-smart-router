# Plan Review — SP-241 Step 1: Capture usage at delegation completion

### Verdict: APPROVE

### Summary

The Step 1 approach — extract pi assistant `usage` at delegation/turn completion in the extension, attach `actual_cost_usd` plus token breakdown to routing telemetry via a post-turn UPDATE keyed on `request_id`, and retain `estimated_cost_usd` — satisfies the PROMPT mission and completion criteria. File Scope is respected (changes touch `.pi/extensions/smart-router/**`, `src/domain/types/**`, `routing-telemetry.ts`, `sqlite-store.ts`, all in-scope). The key design decisions are sound: the post-turn UPDATE is necessary because telemetry rows are written at routing-decision time before delegation completes; making the `StorePort.updateTelemetryUsageActuals` method optional keeps the fail-open contract for `MemoryStore` and library embeds; no-op when usage is absent satisfies the missing-usage requirement. Subscription handling (`cost.total === 0` → token actuals with `actual_cost_usd = null`) matches the PROMPT without inventing USD. The worktree already reflects the implementation of this plan and it is consistent with what was planned; calibration bias is correctly deferred to SP-242.

Notes (non-blocking): failover semantics (last attempt's actuals win, earlier failed-attempt spend not summed) and the GitNexus `target` truncation workaround (manual caller review) are both reasonably documented in STATUS Discoveries.

```json
{"verdict":"APPROVE","feedback":"Plan for Step 1 is sound and aligned with PROMPT.md: post-turn UPDATE keyed on request_id, optional StorePort method for fail-open, additive optional fields, estimated fields retained, subscription rule (cost.total==0 -> tokens only, null USD). File Scope respected; calibration correctly deferred to SP-242."}
```
