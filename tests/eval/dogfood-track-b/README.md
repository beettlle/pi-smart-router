# Dogfood Track B export example (SP-203 / #111)

`synthetic-labeled-export.json` — fully labeled privacy-safe rows for
`npm run routing:community-bench -- --dogfood-export …`.

See also `tests/eval/fixtures/dogfood-track-b/README.md` (pointer kept under the
PROMPT May-change fixtures path without polluting harness fixture scans).
